# Implementation Plan: DECISION version 3 с цитатами хоста, limitations ролей и прошлый вердикт хоста в `outcomes[]` (§25)

Branch: codex/review-v3-citations-outcomes
Created: 2026-09-18

## Original Request
Уроки пятого прогона 2026-09-17, пункты 1 + 2 + 3 (решение владельца 2026-09-18: сначала эти три — все про шаг 6 согласования, снимают возврат к version 1 и зависимость от памяти хоста): (1) собственные цитаты хоста в DECISION version 2 — по норме, где вердикт хоста держится на строках, которых роли не цитируют (run 04, REQ-AI-023: обе роли supported/relevant, хост contradicted по координатору/исполнителю/тесту), хост должен иметь возможность добавить свои цитаты `spec/code/tests` внутри verdict с той же валидацией, что у Assessment §7 (существующие файлы snapshot, точные строки/quote, test_id), при сохранении `concur` (объединение: цитаты названных ролей + собственные) — без возврата ко всей форме v1 на 700 KB; форма v1 остаётся действительной; requiredJSON требует все поля — значит либо новая версия (3) с обязательным полем `citations` (пустые массивы допустимы), либо расширение v2; предпочтение — минимальный контракт по прецеденту §23 (решить в плане); `draft` печатает поле пустым; (2) `outcomes[]` показывает limitations ролей по норме (ключевой сигнал по 023 лежал в limitations mapper при supported) — в `roles.<role>` добавить `limitations []string` из текущего результата роли (statement и цитаты по-прежнему только в entries); (3) прошлый current host-вердикт по той же норме: в `outcomes[]` поле `previous_host` — состояния (specification/implementation/assertion) из последнего решения предыдущего run того же CONFIG с тем же snapshot_id и тем же accepted head (если такой run есть и его решение было current на момент записи), с `run_id`/`review_id` источника; иначе поле отсутствует; чтобы «роли откатились относительно прошлого решения хоста» было видно без памяти хоста; бинарник только показывает, не переносит вердикты (RULES «stale-свидетельства не становятся текущими автоматически»); поиск — по каталогам reports_dir того же CONFIG, только чтение. Ограничения: frozen §1–10 не меняются; §23/§24 расширяются новым разделом tool-spec (редакция 1.5) по прецеденту; host-decision.md — зеркало §14+§23 (+ новый раздел, если он меняет контракт решения — решить в плане); никаких путей/имён пилота (TestSkillFiles). См. ROADMAP «Уроки пятого прогона 2026-09-17», docs/smsplace-adoption.md «Чистая AI-сессия, 2026-09-17 (четвёртый цикл)», tool-spec §7, §14, §23, §24, tool/review.go (validateReviewV2, adoptEvidence, outcomes, draftDecision), tool/main.go (validateResult/checkAssessment, checkSources), skills/spec-audit/SKILL.md шаг 6.

## Settings
- Testing: yes
- Logging: standard
- Docs: yes

## Roadmap Linkage
Milestone: "Уроки пятого прогона 2026-09-17 (решение владельца по каждому пункту)"
Rationale: решение владельца 2026-09-18 — пункты 1 + 2 + 3 первыми: они закрывают возврат хоста к version 1 (726 KB) и зависимость итога от памяти хоста; 4 + 5 + 7 — следующий план мелочей, 6 — protocol.txt отдельно.

## Requirements Reconciliation
Authority: [docs/tool-spec.md](../../docs/tool-spec.md) frozen §1–10 (§7 Assessment и правила цитат — не меняются; §9 границы) > §14 (основание, журнал точных байт, «запись только при полной доставке», состояния) > §23 REQ-SA-044 (форма v2: `verdicts`/`concur`/`counts`; свидетельства = цитаты названных ролей; «собственных цитат форма не содержит — для них используется version 1» — новый раздел уточняет: для них используется version 3) > §24.1 (`outcomes[]` — производная таблица без цитат/statement/limitations; «таблица — подсказка для чтения, не оценка») и REQ-SA-045 (`draft`) > [RULES.md](../RULES.md) («stale-свидетельства не становятся текущими автоматически»; «не превращай … в доказательство»; спецификация прежде реализации; universality) > [rules/base.md](../rules/base.md) (ранние проверки, лимиты, `os.Root`, ошибки с контекстом) > ROADMAP (объём) > отчёт `sa-clean` run 04 (docs/smsplace-adoption.md «четвёртый цикл»).

Разведка по коду: `ReviewVerdict{RequirementID, Specification, Implementation, Assertion, Concur, Statement, Limitations}` и `ReviewDecisionV2` (`tool/review.go:47-79`); `validateReview` — диспетчер по `reviewVersion` (`:171-200`: 2 → `validateReviewV2`, 1 → v1, иначе «неверная версия/идентичность»); `validateReviewV2(data, runID, m, state)` — форма, `checkVerdict`, `counts`, затем `adoptEvidence(verdict, m, state)` (объединение цитат названных ролей с дедупом, `:262-310`); `reviewRecord{Version, …, Assessments, Concur, Limitations}` и `record.form()` (`"verdicts"` при 2); `summarizeReviews` — replay с state и fallback без state; `submitReview` — форма → дубликат → доставка/basis → при v2 повторная валидация со state (`:436-466`). Цитаты §7 проверяет `checkAssessment(assessment, req, m, root, checkSources)` (`tool/main.go:763-828`): группы spec/code/tests, ≤128 на группу, `findSource`/`localPath`/kind, диапазон, spec внутри блока нормы, при `checkSources` — sha и точная quote через `readRoot`/`lineQuote`; `root = os.OpenRoot(m.Config.ProjectRoot)` (`:734-740`), `m.Config.ProjectRoot` выставляется при чтении run (`:1178`). `outcomes(m, state)` (`:143-190`) — `RoleOutcome{TaskID, Attempt, Specification, Implementation, Assertion}`; `Manifest.SnapshotID`, `Manifest.Accepted *AcceptedSummary{Head, History}` (`tool/main.go:92-100`, `tool/accepted.go:85-88`); каталоги run — `reports` root (`:1115`), у каждого run `manifest.json`, `host-reviews.json`, `tool-versions.json` с `ToolVersionRecord{Command, ToolVersion, RecordedAt}` (`:900-904`); `readReviews(run, runID, m)` валидирует журнал под манифест run. `draftDecision` печатает v2 (`:225-245`). Зеркало `host-decision.md` = §14 + §23, тест режет §23 до «## 24.» (`tool/skill_test.go:133`). Пилот: журнал run 03 — v2, run 04 — v1 (726 KB), оба должны читаться после изменения.

### Решения (по итогам разведки)
| Решение | Обоснование | Проверка |
| --- | --- | --- |
| Новый §25 tool-spec «Уроки пятого прогона: цитаты хоста в решении (version 3), limitations и прошлый вердикт в `outcomes[]`» (редакция 1.5): REQ-SA-046 «Решение version 3» + 25.1 `roles.<role>.limitations` + 25.2 `previous_host`. §23 — одна фраза «для них используется `version: 1`» → «для них используется `version: 3` (§25) или полная форма `version: 1`»; `host-decision.md` — зеркало §14 + §23 + §25 (третий срез в `TestHostDecisionMirror`: между «## 25.» и «## См. также»). §24.1 — фраза «цитаты, statement и limitations в таблицу не копируются» → «цитаты и statement не копируются; limitations ролей и прошлый вердикт хоста — §25» | Владелец: version 3; RULES «спецификация прежде реализации»; зеркало должно нести весь контракт решения | `TestHostDecisionMirror` с тремя срезами; `shasum -a 256 -c acceptance/*.sha256` OK; `git diff codex/bootstrap -- docs/tool-spec.md` — шапка, фраза §23, фраза §24.1, §25 |
| `ReviewVerdictV3 = ReviewVerdict + Spec []Citation, Code []Citation, Tests []TestCitation` (json `spec`/`code`/`tests`, обязательны, пустые массивы допустимы); `ReviewDecisionV3{Version:3, …, Verdicts []ReviewVerdictV3, Counts, Limitations}`. `validateReview`: `case 3` → `validateReviewV3`; общая логика v2/v3 выносится в `validateVerdicts(runID, m, state, identity, verdicts []ReviewVerdictV3, counts)`; v2 конвертируется в v3-вердикты с пустыми цитатами. Свидетельства нормы = `adoptEvidence` (цитаты названных ролей) ∪ собственные цитаты хоста (дедуп по значению, порядок: роли, затем хост). Валидация собственных цитат — вынесенный из `checkAssessment` `checkCitations(spec, code, tests []..., req, m, root, checkSources) error` (группы, ≤128, `findSource`/kind, диапазон, spec внутри блока нормы, при `checkSources` — sha/quote); `checkAssessment` вызывает его же (поведение §7 без изменений). Требование полноты §7 («нужна spec; supported/contradicted требуют code; assertion требует тестовый источник») проверяется на объединённой оценке при `state != nil` — цитаты ролей уже покрывают её, собственные только добавляют. `checkSources`: при submit — true (root = `os.OpenRoot(m.Config.ProjectRoot)`), при replay — false. `reviewRecord.form()`: 3 → `"verdicts"` (та же нормализованная форма отчёта); `ReviewSummary.Form` для v3 — `"verdicts"`; `Concur` как у v2 | Хост в run 04 вернулся к 726 KB v1 ради трёх цитат по одной норме; requiredJSON не допускает необязательных полей, v2-журнал run 03 должен читаться; прецедент §23 | `TestReviewV3`: v3 с пустыми цитатами принимается как v2 (assessments = цитаты ролей); v3 с собственными `code`+`tests` по одной норме → `latest.assessments[i]` содержит цитаты ролей ∪ хоста без повторов, HTML показывает цитату хоста; цитата с неверной quote / вне группы / файла нет / >128 → отказ с `requirement_id`; журнал с v1+v2+v3 читается, `history` 3; после `retry` — outdated, отчёт читаем; `draft` печатает v3 с пустыми `spec/code/tests`; `TestReviewV2` без изменений ожиданий |
| `RoleOutcome.Limitations []string json:"limitations"` — из `assessment.Limitations` текущего результата роли (пустой массив, не null); statement и цитаты по-прежнему только в `entries` | Сигнал по 023 лежал в limitations mapper при supported; таблица без него прячет главное | `TestReviewOutcomes`: limitations роли в `outcomes[i].roles.<role>.limitations`, у роли без limitations — `[]` |
| `Outcome.PreviousHost *PreviousHost json:"previous_host,omitempty"` — `{run_id, review_id, specification, implementation, assertion}`; `previousHost(reports *os.Root, runID string, m Manifest) map[string]PreviousHost` в `reviewContext`: перебирает каталоги `reports` (только чтение, кроме текущего run и не-каталогов), для каждого читает `manifest.json` (строгий JSON, `snapshot_id == m.SnapshotID` и `accepted.head == m.Accepted.head` — при `Accepted == nil` у обоих равенство nil), `host-reviews.json` (последняя запись — она была current на момент записи по §14, форма записи разбирается лёгкой структурой `{version, review_id, assessments[]?, verdicts[]?}` без полной валидации под чужой манифест) и `tool-versions.json` (recorded_at последней записи `review` — для выбора самого позднего кандидата; при равенстве/отсутствии — лексически больший run_id); повреждённые/неполные каталоги пропускаются с DEBUG «review: прошлый вердикт: каталог пропущен» {run_id, error}; в `outcomes` подставляется по `requirement_id`. Бинарник только показывает; `previous_host` не участвует в basis/CAS, `draft` его не копирует | RULES «stale-свидетельства не становятся текущими автоматически»; хост без памяти записал бы 51/51 supported | `TestReviewPreviousHost`: два run на одном fixture (одинаковый snapshot, без accepted): run A с решением v2 → run B `review` показывает `previous_host{run_id:A, review_id, состояния}`; повреждённый `manifest.json` третьего каталога — пропущен; run с другим snapshot (правка исходника + новый prepare) — `previous_host` отсутствует; три run — берётся последний по `recorded_at` review; для accepted-режима — `TestReviewPreviousHostAccepted` на fixture с accepted-index (по образцу `TestCheckAcceptance`/`TestReanchor`): совпадение head → есть, после нового accept (другой head) — нет |
| `draft` печатает `ReviewDecisionV3` (пустые `spec/code/tests` в каждом verdict); SKILL.md шаг 6: «fill … and, only for a norm whose verdict rests on lines no role cited, the host's own `spec`/`code`/`tests` citations (same rules as a role's); leave them `[]` otherwise; `outcomes[]` also shows each role's `limitations` and `previous_host` — the last host verdict on the same norm for the same snapshot and accepted head from an earlier run; it is a reading aid, never copy it as your verdict» | REQ-SA-045; уроки run 04 | `TestReviewDraft` — тип v3, `spec/code/tests` пустые; `TestSkillFiles` |
| Отчёт/HTML: host-цитаты хоста уже попадают через синтезированные assessments (`Navigation.Host`) — правок в `report.go` нет; подпись «свидетельства …» остаётся, для v3 с собственными цитатами добавляется « и хоста» (`hostConcurLabel(concur, own bool)`) | REQ-SA-021 | `TestReviewV3`: HTML содержит «свидетельства обеих ролей и хоста» у нормы с цитатами хоста |
| Поставка: релиз `v0.1.11`, `update`, `skill update` в синтетике/пилоте (коммиты по запросу); read-only на пилоте: `review CONFIG finance-2026-09-17-04` → `outcomes[]` с `limitations` и `previous_host` из run 03 (тот же snapshot, accept-003; ожидаем `previous_host` у всех 51, у 023 — contradicted/contradicts из run 03); журналы run 03 (v2) и run 04 (v1) читаются; `draft` → v3 | §18 | `gh release view v0.1.11` — 5 ассетов; `git status` пилота — только tracked-копия skill |

### Поддерживаемые комбинации
| Комбинация | Вход / валидация | Результат | Проверка |
| --- | --- | --- | --- |
| v3, все `spec/code/tests` пустые | как v2 | принято; свидетельства = цитаты ролей | `TestReviewV3` |
| v3, собственные цитаты по норме, `concur: both` | `checkCitations` + полнота §7 на объединении | принято; assessments = роли ∪ хост без повторов; HTML «… и хоста» | `TestReviewV3` |
| v3, неверная quote / файл вне snapshot / цитата вне группы / >128 | `checkCitations` | отказ с `requirement_id`, журнал без записи | `TestReviewV3` |
| v3 с `null` вместо массива цитат | strictJSON/requiredJSON | отказ | `TestReviewV3` |
| журнал v1 + v2 + v3 | replay | `history` 3, `latest` — последняя | `TestReviewV3` |
| `draft` | — | v3 с пустыми цитатами | `TestReviewDraft` |
| `outcomes[].roles.<role>.limitations` | — | массив из результата роли | `TestReviewOutcomes` |
| `previous_host`: есть run с тем же snapshot и head и журналом | чтение каталогов | поле с run_id/review_id/состояниями последней записи | `TestReviewPreviousHost` |
| другой snapshot или другой accepted head | — | поля нет | `TestReviewPreviousHost`, `…Accepted` |
| несколько кандидатов | `recorded_at` review, затем run_id | последний | `TestReviewPreviousHost` |
| повреждённый соседний run | — | пропуск с DEBUG, контекст строится | `TestReviewPreviousHost` |
| прошлое решение v1/v2/v3 | лёгкий разбор | состояния из assessments или verdicts | `TestReviewPreviousHost` |

Представительные реальные артефакты: журналы пилота run 03 (v2) и run 04 (v1, 726 KB) — после релиза `review` на run 04 читает оба и показывает `previous_host` из run 03; `acceptance/declared-v01` — `review` без соседей (поля нет).

## Commit Plan
- **Commit 1** (after tasks 1-2): "feat(review): §25 — DECISION version 3 с цитатами хоста"
- **Commit 2** (after task 3): "feat(review): limitations ролей и previous_host в outcomes[]"
- **Commit 3** (after task 4): "docs(launcher): SKILL.md о version 3, limitations и previous_host; README/ROADMAP/adoption"
- **Commit 4** (after task 5): "docs(dist): релиз v0.1.11"

## Tasks

### Phase 1: Контракт и version 3
- [x] Task 1: §25 tool-spec (REQ-SA-046, 25.1, 25.2), редакция 1.5; фразы §23/§24.1; зеркало `host-decision.md` с тремя разделами; AGENTS/ARCHITECTURE
  - Deliverable: шапка «1.5 добавляет §25 …». §23: «собственных цитат форма не содержит — для них используется `version: 1`» → «… — для них используется `version: 3` (§25) или полная форма `version: 1`». §24.1: «Цитаты, statement и limitations в таблицу не копируются — они остаются в `entries`» → «Цитаты и statement в таблицу не копируются — они остаются в `entries`; limitations ролей и прошлый вердикт хоста добавляет §25». §25 перед «См. также»: причина (run 04: v1 726 KB ради трёх цитат по одной норме; сигнал в limitations; итог совпал только благодаря памяти хоста); REQ-SA-046 «Решение version 3»: Условие — `version: 3`; Требование — форма v2 (§23), в каждом verdict обязательные `spec`, `code`, `tests` (пустые массивы допустимы) с правилами цитат §7 (группы, ≤128, файлы snapshot, точные строки/quote при записи, spec внутри блока нормы, test_id), свидетельства нормы = цитаты названных `concur`-ролей ∪ собственные без повторов, полнота §7 на объединении; отчёт помечает нормы с собственными цитатами; v1/v2 действительны, журнал может содержать все три; Проверка — комбинации плана; Источник/Обоснование/Зависимость (SA-044, §7, §14). 25.1 «Limitations ролей в `outcomes[]`»: `roles.<role>.limitations` из текущего результата. 25.2 «Прошлый вердикт хоста»: `previous_host{run_id, review_id, specification, implementation, assertion}` — последняя запись журнала другого run того же `reports_dir` с тем же `snapshot_id` и тем же accepted head (последнего по времени записи `review`, затем по run_id); только чтение, повреждённые каталоги пропускаются; подсказка для чтения, не перенос вердикта (RULES); в basis и `draft` не входит. `host-decision.md`: заголовок «Mirror of sections 14, 23 and 25 …», тело = §14 + §23 + §25; `tool/skill_test.go` — третий срез («## 25.» → «## См. также»). AGENTS.md — строка §25, «§13–25»; ARCHITECTURE — review.go (v1/v2/v3, previous_host).
  - Files: `docs/tool-spec.md`, `skills/spec-audit/references/host-decision.md`, `tool/skill_test.go`, `AGENTS.md`, `.ai-factory/ARCHITECTURE.md`
  - Verify: `go test ./tool -run 'TestHostDecisionMirror|TestSkillFiles' -count=1`; `shasum -a 256 -c acceptance/*.sha256 acceptance/legacy/baseline.sha256` OK; `rg -n 'smsplace|FinanceSystem' docs/tool-spec.md` без новых вхождений.
  - Logging: документ фиксирует: DEBUG «review: свидетельства по concur» дополняется {own_spec, own_code, own_tests}; DEBUG «review: прошлый вердикт» {run_id, review_id, requirements}; DEBUG «review: прошлый вердикт: каталог пропущен» {run_id, error}.
  - REQ: REQ-SA-044, REQ-SA-045, §7, §14, §24.1

- [x] Task 2: типы и валидация version 3, `checkCitations`, объединение цитат, `draft` v3, подпись «и хоста»
  - Deliverable: `tool/main.go`: из `checkAssessment` выносится `checkCitations(spec, code []Citation, tests []TestCitation, req Requirement, m Manifest, root *os.Root, checkSources bool) error` (test_id, группы/лимит/kind/диапазон/блок нормы/sha/quote); `checkAssessment` вызывает его после проверок состояний и полноты. `tool/review.go`: `ReviewVerdictV3{RequirementID, Specification, Implementation, Assertion, Concur, Statement, Limitations, Spec []Citation json:"spec", Code []Citation json:"code", Tests []TestCitation json:"tests"}`, `ReviewDecisionV3{Version, ReviewID, RunID, SnapshotID, BasisSHA256, Reviewer, Summary, Verdicts []ReviewVerdictV3, Counts, Limitations}`; `validateReview`: `case 3` → `validateReviewV3(data, runID, m, state, checkSources)`; общая часть v2/v3 — `validateVerdicts(identity…, verdicts []ReviewVerdictV3, counts, m, state, checkSources)`; `validateReviewV2` конвертирует в `ReviewVerdictV3` с пустыми массивами и вызывает её; для каждого verdict: `checkVerdict`, при непустых собственных цитатах — `checkCitations(…, req, m, root, checkSources)` с ошибкой `fmt.Errorf("%s: %w", id, err)` (root открывается один раз при `checkSources` и наличии хотя бы одной собственной цитаты); при `state != nil` — `adoptEvidence` ∪ собственные (дедуп по значению, `mergeCitations`), затем полнота §7 на объединении (`len(Spec)==0`, `Implementation != unknown && len(Code)==0`, assertion relevant/weak/contradicts без tests → отказ «нужна spec; supported/contradicted требуют code»/«оценка assertion требует тестовый источник» с `requirement_id`); DEBUG «review: свидетельства по concur» получает {own_spec, own_code, own_tests}. `reviewRecord.form()`: 2 и 3 → `"verdicts"`; `record.decision()` без изменений; `submitReview`: повторная валидация со state при `Version >= 2`. `draftDecision` печатает `ReviewDecisionV3` с `Spec/Code/Tests: []`. `tool/report.go`: `hostConcurLabel(concur string, own bool)` — при `own` добавляет « и хоста»; `own` = у синтезированной оценки есть цитаты, которых нет у ролей — проще: `reviewRecord.Own map[string]bool` (requirement_id → есть собственные цитаты) → `ReviewSummary.Own map[string]bool json:"own_citations,omitempty"`; `buildNavigation` использует `report.HostReview.Own[req.ID]`. Тесты `tool/review_test.go` `TestReviewV3` (по образцу `TestReviewV2`; `reviewV3Input`): пустые цитаты → как v2; собственные `code`+`tests` (цитаты из `sampleResult` другой нормы того же файла — т.е. строки, которых роли по этой норме не цитируют) → union без повторов, `own_citations` в `review`, HTML содержит «свидетельства обеих ролей и хоста»; отказы: quote с изменённой строкой, путь вне snapshot, `code`-цитата в `spec`, 129 цитат, `"spec": null`; журнал v1+v2+v3 → `history` 3; `retry` → outdated; `TestReviewDraft` — тип `ReviewDecisionV3`.
  - Files: `tool/main.go`, `tool/review.go`, `tool/report.go`, `tool/review_test.go`
  - Depends: 1
  - Verify: `go test ./tool -run 'TestReview|TestReport|TestHostDecisionMirror' -count=1`; `go vet ./tool`; журнал пилота run 03 (v2) и run 04 (v1) читаются бинарником в read-only `review` на копии (после Task 5 — на пилоте).
  - Logging: DEBUG «review: свидетельства по concur» {requirement_id, concur, spec, code, tests, own_spec, own_code, own_tests}; отказы — тексты с `requirement_id` и путём цитаты, без содержимого.
  - REQ: REQ-SA-046, REQ-SA-044, §7, §14
<!-- Commit checkpoint: tasks 1-2 -->

### Phase 2: `outcomes[]` — limitations и прошлый вердикт
- [x] Task 3: `RoleOutcome.Limitations`, `Outcome.PreviousHost`, `previousHost(reports, runID, m)`
  - Deliverable: `tool/review.go`: `RoleOutcome.Limitations []string json:"limitations"` (из `assessment.Limitations`, `[]` при отсутствии); `type PreviousHost struct{RunID, ReviewID, Specification, Implementation, Assertion string}` (json snake_case); `Outcome.PreviousHost *PreviousHost json:"previous_host,omitempty"`; `outcomes(m, state, previous map[string]PreviousHost)`. `previousHost(reports *os.Root, runID string, m Manifest) map[string]PreviousHost`: `fs.ReadDir(reports.FS(), ".")` — только каталоги, имена по `slugRE`, кроме `runID`; для кандидата: `manifest.json` → лёгкая структура `{snapshot_id, accepted{head}}` (strictJSON не нужен — только сравнение), совпадение `snapshot_id` и `head` (оба nil — совпадение); `host-reviews.json` → `reviewJournal`, последняя запись → лёгкий разбор `{version, review_id, assessments:[{requirement_id,specification,implementation,assertion}], verdicts:[…те же поля…]}`; `tool-versions.json` → `recorded_at` последней записи с `command == "review"` (пусто, если нет); выбор кандидата: максимальный `recorded_at`, при равенстве — лексически больший run_id; DEBUG «review: прошлый вердикт» {run_id, review_id, requirements}; ошибки чтения/разбора отдельного каталога — DEBUG «review: прошлый вердикт: каталог пропущен» {run_id, error}, каталог пропускается. `reviewContext` получает `reports *os.Root` (уже открыт в `execute`, `tool/main.go:1115`; сигнатура `reviewContext(reports, run, runID, m, state, fresh)`; три вызова — `status` `:1250`, `review` `:1261`, `report` `:1270`; `draft` использует `draftDecision` и не меняется). Тесты: `TestReviewOutcomes` — limitations; `TestReviewPreviousHost` (`tool/review_test.go`): run `a` (prepare/submit/review v2) → run `b` (тот же fixture, prepare/submit) → `review b`: `previous_host` у всех норм `{run_id:a, review_id, состояния}`; каталог `runs/broken/manifest.json` = мусор → пропуск, контекст строится; run `c` после правки исходника (`prepare c` на новом snapshot) → у `c` `previous_host` нет; третий run `d` на исходном snapshot с решением после `a` → у нового run `previous_host.run_id == d`; `TestReviewPreviousHostAccepted` на `acceptedFixture(t)` (`tool/accepted_test.go:15`) по образцу apply в `TestCheckAcceptance`/`TestReanchor` (`:954`): run после `reconcile apply` того же head → есть; после нового `apply` (другой head) → нет.
  - Files: `tool/review.go`, `tool/main.go`, `tool/review_test.go`
  - Depends: 2
  - Verify: `go test ./tool -run 'TestReview' -count=1`; `go vet ./tool`; `./bin/spec-audit review` на копии `acceptance/declared-v01` → `previous_host` отсутствует, `limitations` — массивы.
  - Logging: DEBUG «review: прошлый вердикт» {run_id, review_id, requirements}; DEBUG «review: прошлый вердикт: каталог пропущен» {run_id, error}.
  - REQ: 25.1, 25.2, REQ-SA-013, RULES «Свидетельства и сохранность»
<!-- Commit checkpoint: task 3 -->

### Phase 3: Документация и поставка
- [ ] Task 4: launcher SKILL.md; README, ROADMAP, документ применения
  - Deliverable: `skills/spec-audit/SKILL.md` шаг 6: DECISION `version: 3` (draft печатает v3): «fill … ; for a norm whose verdict rests on lines no role cited, add the host's own `spec`/`code`/`tests` citations under the same rules as a role's and keep `concur`; leave them `[]` otherwise — never fall back to version 1 for that. `outcomes[]` also lists each role's `limitations` (read them: a supported verdict with a limitation about another branch is a signal) and `previous_host` — the last host verdict on the same norm for the same snapshot and accepted head from an earlier run: a reading aid, never copy it as your verdict»; ограничения `TestSkillFiles`. README «Первый запуск» — v3, `limitations`, `previous_host`; «Приёмка и ограничения» — строка §25 (реализовано; регрессии; не проверено до релиза: живой цикл). ROADMAP «Уроки пятого прогона» — 1, 2, 3 `[x]` с датой и свидетельствами; строка таблицы этапов. docs/smsplace-adoption.md «Обычный рабочий цикл» п.5 — с 0.1.11 `version: 3` с собственными цитатами; «четвёртый цикл» — сноска. AGENTS.md — если не сделано в Task 1.
  - Files: `skills/spec-audit/SKILL.md`, `README.md`, `.ai-factory/ROADMAP.md`, `docs/smsplace-adoption.md`
  - Depends: 2, 3
  - Verify: `go test ./tool -run 'TestSkillFiles|TestHostDecisionMirror|TestSkill' -count=1`; `rg -n 'version: 3|previous_host|limitations' skills/spec-audit/SKILL.md README.md`; полный `go test ./tool`, `go vet ./tool`, `CGO_ENABLED=0 go build -o bin/spec-audit ./tool`.
  - Logging: нет нового.
  - REQ: REQ-SA-037, REQ-SA-046, 25.1, 25.2

- [ ] Task 5: релиз `v0.1.11`, обновление хоста/пилота/синтетики, read-only проверка на пилоте
  - Deliverable: merge в `codex/bootstrap` (по подтверждению владельца), тег `v0.1.11`, approve deployment; `spec-audit update` → 0.1.11; `skill update --host both` в `spec-audit-synthetic` и пилоте (коммиты — по запросу); read-only на пилоте: `review CONFIG finance-2026-09-17-04` — журналы run 03 (v2) и run 04 (v1) читаются, `outcomes[]` с `limitations` (у 023 mapper — про recovery-путь) и `previous_host` из run 03 (`review_id host-review-001`, у 023 — contradicted/contradicts); `review CONFIG finance-2026-09-17-03` → `previous_host` из run 04 (последнее по времени); `draft` → v3; журналы не тронуты. README «Приёмка и ограничения» — строка релиза 0.1.11.
  - Files: `README.md`; (пилот и синтетика — вне репозитория)
  - Depends: 4
  - Verify: `gh release view v0.1.11` — 5 ассетов; `spec-audit version` → 0.1.11; `spec-audit update` → `updated:false`; `git -C <pilot> status --porcelain` — только tracked-копия skill.
  - Logging: нет нового.
  - REQ: REQ-SA-035, REQ-SA-037, REQ-SA-046
<!-- Commit checkpoint: tasks 4-5 -->

## Открытые вопросы (не блокируют)
1. До T2: полнота §7 на объединении — проверять при записи (state есть) и не проверять при replay (state nil): при replay цитаты ролей недоступны без state, форма уже была принята.
2. До T3: `previous_host` при `Accepted == nil` у обоих run (declared-режим) — сравнивать только snapshot_id; выбрано так.
3. После T5: если хосту понадобится `concur: none` (вердикт целиком на собственных цитатах без свидетельств ролей) — отдельно; сейчас union с ролями обязателен, `none` не вводится.

## Риски
- `previous_host` читает соседние каталоги `reports_dir` — только чтение, повреждения пропускаются; при сотнях run стоимость линейна (по одному `manifest.json` на каталог, ≈1 MB на пилоте) — приемлемо для `review`; при необходимости позже кэшировать по `snapshot_id`.
- Собственные цитаты хоста проходят ту же проверку quote/sha при записи — как у роли; при replay (без state) форма только структурно проверяется, как у v2.
- Изменение сигнатуры `reviewContext` затрагивает `status`/`report`/`review`/`draft` — покрыто существующими тестами.
- `host-decision.md` растёт на §25; зеркало остаётся точным по тесту.
