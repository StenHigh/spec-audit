# Implementation Plan: Уроки третьего прогона — reanchor, подсказки с unique_shared, диагностика scopes, признак base_index (§22)

Branch: codex/reanchor-hints-scope-errors
Created: 2026-09-17

## Original Request
Уроки третьего прогона 2026-09-17, пункты 1–4 (решение владельца 2026-09-17; пункт 5 — облегчённый host DECISION — отдельный план, остаётся в ROADMAP): (1) операция приёмки «перепривязка с сохранением прежнего принятого содержания» — хост утверждает смысловую эквивалентность нового кандидата прежней active-норме, ID/revision/content hash/title/verification/condition/statement/exceptions/clarity/unresolved сохраняются из прежней записи, цитаты берутся из кандидата; иначе после каждого переизвлечения revision растёт у всего набора (33 нормы на rev 3) и ambiguous растёт из-за перефразировки unresolved; затрагивает frozen контракт accepted-index п.3–4 — оформить как расширение по прецеденту §19–§21 с отдельным контролем; (2) подсказки `check`: 4 ложных top-1 из 50 из-за строк-заголовков, общих для многих кандидатов, два верных ID вне трёх подсказок — по офлайн-замеру взвешивание/исключение общих строк ухудшает первый переход (top-1 48→43), поэтому ранжирование по Жаккару сохранить, лимит поднять до 4 (hit@4 = 49/49 и 50/50) и добавить поле `unique_shared` (общие строки, которых нет у других active-записей: у верных top-1 > 0 в 85/91 случаев, у ложных — 0 из 3 на первом переходе); (3) `index`/`prepare` при устаревших `scopes` — называть scope, неизвестные/повторные ID и нераспределённые ID вместо «неизвестное или повторно назначенное требование»; (4) `check CONFIG RAW DECISION` — явный признак `base_index_current` и пояснение, что `freshness` относится к текущему индексу до применения. См. ROADMAP «Уроки третьего прогона 2026-09-17», docs/smsplace-adoption.md «Чистая AI-сессия, 2026-09-17 (второй цикл)», tool-spec §21, docs/accepted-index.md (frozen).

## Settings
- Testing: yes
- Logging: standard
- Docs: yes

## Roadmap Linkage
Milestone: "Уроки третьего прогона 2026-09-17 (решение владельца по каждому пункту)"
Rationale: решение владельца 2026-09-17 — пункты 1–4 одним планом (новое действие `reanchor`, `unique_shared` и лимит 4 подсказок, диагностика `scopes`, признак `base_index_current`); пункт 5 (облегчённый host DECISION, frozen §14) и мелочи 6–7 остаются открытыми в том же пункте ROADMAP.

## Requirements Reconciliation
Authority: [docs/tool-spec.md](../../docs/tool-spec.md) frozen §1–10 (§6 scopes: «каждую норму ровно одним владельцем», «неизвестные, повторные или нераспределённые IDs отклоняются»; §9) и §14 (не трогается) > [docs/accepted-index.md](../../docs/accepted-index.md) (frozen хэшем: п.3 «Rebind сохраняет ID, revision и content hash только при побайтово одинаковом принятом содержании … Даже эквивалентное перефразирование принятого содержания требует revise **либо сохранения прежней принятой формулировки**. Бинарник не вычисляет смысловую эквивалентность»; п.4 revise; п.1 «каждый прежний active ID — ровно одно явное продолжение/снятие»; таблица действий; «неизвестные/повторные ключи … запрещены») > §21 (`check`, подсказки, `stageAcceptance`) > [RULES.md](../RULES.md) («не переписывай frozen…; новая семантика — обоснованное расширение и отдельный контроль»), [rules/base.md](../rules/base.md), [ARCHITECTURE.md](../ARCHITECTURE.md) > ROADMAP (объём) > отчёт сессии `sa-clean` (docs/smsplace-adoption.md «Чистая AI-сессия, 2026-09-17 (второй цикл)») как свидетельство потребности. Разведка по коду: `applyAccepted` (`tool/accepted.go:111-202`: `switch operation.Action` :136-147, previous → retired :150-157, targets :158-195, rebind/revise :173-183 — content hash сравнивается с `old.ContentHash`, `req.ID/Revision/Parents` наследуются, `state.Records[position] = acceptedRecord(...)`), `acceptedRequirement` (:101-109), `acceptedRecord` (:66-72), `normativeAnchor` (:284-315, список действий), `matchHints`/`checkedCandidates`/`checkAcceptance` (:519-696), `assignRequirements` (`tool/specs.go:128-161`: ошибки :153 и :159), тесты `TestCheckAcceptance` (`tool/check_test.go`), `TestAcceptedLifecycle/Rejections/SplitMerge` (`tool/accepted_test.go`). Офлайн-замер на ledger пилота (три commits, два перехода с истиной `previous`): Jaccard top-1 48/49 и 46/50, hit@3 49/49 и 49/50, hit@4 49/49 и 50/50; log-IDF/исключение общих строк — 43–44/49 на первом переходе; `unique_shared > 0` у 41/41 и 44/47 верных top-1, у 0/0 и 3/3 ложных.

### Решения (по итогам разведки)
| Решение | Обоснование | Проверка |
| --- | --- | --- |
| Все четыре пункта оформляются новым §22 tool-spec «Уроки третьего прогона» (редакция 1.1): REQ-SA-043 `reanchor`, 22.1 подсказки (`unique_shared`, лимит 4), 22.2 диагностика `scopes`, 22.3 `check` с DECISION. §1–10, §14, `accepted-index.md` не редактируются: новое действие реализует уже разрешённый п.3 путь «сохранение прежней принятой формулировки» | RULES; прецедент §19–§21 | `shasum -a 256 -c` всех списков `acceptance/` OK; `git diff codex/bootstrap -- docs/tool-spec.md` — шапка и §22 |
| `reanchor`: `{action:"reanchor", previous:[ID], targets:[{candidate, title:"", verification:""}], reason}` — `p == 1 && n == 1`; title/verification обязаны быть пустыми (как у reject/defer; ошибка «reanchor сохраняет прежние title/verification; поля должны быть пустыми»); запись строится из прежней: `req := old` (ID, Title, Condition, Statement, Verification, ContentHash), `meta := *old.Accepted` (Revision, Exceptions, Clarity, Unresolved, Parents) с `Citations = copy(candidate.Citations)`, `req.Source = candidate.Citations[0]`; content hash не пересчитывается и не сравнивается; `history.Assignments` получает `{candidate, ID}`; previous помечается retired и тут же перезаписывается active (как rebind/revise, `:150-157` + `:184`). Guard `normativeAnchor` действует (действие добавляется в список). Формулировка кандидата не теряется — она в raw и истории пакета. Хост отвечает за эквивалентность; бинарник эквивалентность не вычисляет | accepted-index п.3 разрешает сохранение прежней формулировки, но сегодня это возможно только при побайтовом совпадении из raw; после двух переизвлечений 33 нормы на rev 3, ambiguous 8→12 | `TestReanchor`: revision/content hash/title/statement/clarity/unresolved прежние, citations и source новые; replay журнала детерминирован (`acceptedRead` == состояние после apply); title непустой → отказ; только-справочный кандидат → guard; retired previous → отказ; `check` предсказывает `{C001, REQ-AI-001, reanchor, rev}`; `prepare` Task несёт новые цитаты, assessment в новом диапазоне принимается, в старом — отказ |
| Подсказки 22.1: ранжирование по Жаккару сохраняется (взвешивание/исключение общих строк ухудшает первый переход), `matchHintLimit = 4`, новое поле `unique_shared` — число общих строк, которых нет ни у одной другой active-записи (частота строки считается один раз на вызов по всем active-записям); `TestCheckAcceptance/hints` дополняется | замер на двух переходах пилота: hit@4 = 49/49 и 50/50; `unique_shared > 0` отделяет верные top-1 от ложных | `TestCheckAcceptance/hints`: строка, общая для двух записей, даёт `unique_shared` 0 у обеих подсказок, уникальная — 1; `hint_limit`: 5 пересечений → 4 подсказки |
| Диагностика 22.2: `assignRequirements` собирает по каждому scope списки неизвестных и повторных ID, затем нераспределённые; ошибки `scope %s: неизвестные ID [..]; повторно назначенные [..]` и `не распределены по scope: [..]` (ID отсортированы, до 64 в списке); текст прежнего лимита «более 64 требований: задайте явные scope» не меняется | Пилот после apply: «неизвестное или повторно назначенное требование» без scope и ID | `TestScopeAssignmentErrors`: устаревший scope-конфиг → ошибка содержит `scope economics`, устаревший ID и «не распределены … REQ-DEMO-0NN» |
| `check` с DECISION 22.3: ответ дополняется `"base_index_current": true` (при успехе всегда истина — CAS уже проверен `applyAccepted`), а `freshness` документируется как свежесть текущего индекса **до** применения (после apply source_set пакета равен текущим источникам — `legacyValidate`); при `duplicate` ключа нет | Хоста сбило `freshness:"stale"` у пакета, который stale-ность закрывает | `TestCheckAcceptance/decision`: `base_index_current == true` в валидном dry-run, ключ отсутствует при duplicate и в RAW-режиме |
| Launcher (SKILL.md, абзац accepted): «use `reanchor` when a `check` hint pairs a candidate with an existing norm whose accepted wording still holds — the accepted content, ID and revision stay, only citations move; `revise` only for a change of meaning; `unique_shared: 0` means the hint rests on lines shared with other norms — verify before reanchoring»; после `index`-ошибки со списком ID — обновить `scopes` | rebind 0 из 49 и 0 из 50; ложные top-1 по общим строкам | `TestSkillFiles`; `rg -n 'reanchor|unique_shared' skills/spec-audit/SKILL.md` |
| Поставка: релиз `v0.1.7`, `update` на хосте, `skill update` в пилоте и синтетике; read-only приёмка на пилоте — `check CONFIG RAW` на временной копии журнала (оба перехода: усечение до commits[0] и commits[1], источники по хэшам raw) → hit@4 и распределение `unique_shared`; `reanchor` на пилоте — только следующим циклом (запись в ledger — решение владельца) | §18; представительный артефакт | `gh release view v0.1.7` — 5 ассетов; sha256 журнала пилота и `git status` неизменны |

### Поддерживаемые комбинации
| Комбинация | Вход / валидация | Состояние | Результат | Проверка |
| --- | --- | --- | --- | --- |
| `reanchor` с пустыми title/verification, previous active, кандидат с нормативной цитатой | `applyAccepted` + guard | запись перезаписана: содержание/hash/revision прежние, citations/source новые | `assignments: [{candidate, ID}]`; `records[i].revision` прежняя | `TestReanchor` |
| `reanchor` с непустым title или verification | `applyAccepted` | без записи | отказ «reanchor сохраняет прежние title/verification; поля должны быть пустыми» | `TestReanchor` |
| `reanchor`, previous не active / повторный / отсутствует | `applyAccepted` | без записи | прежние отказы «previous требует неповторяющиеся прежние active ID» / «недопустимая операция» | `TestReanchor` |
| `reanchor`, кандидат только со справочными цитатами (`references`) | `normativeAnchor` | без записи | «кандидат … цитирует только справочные источники» | `TestReanchor` |
| `reanchor` через `check CONFIG RAW DECISION` | `stageAcceptance` | без записи | `assignments[].action == "reanchor"`, `revision` прежняя, `base_index_current: true` | `TestReanchor` + `TestCheckAcceptance/decision` |
| replay журнала с `reanchor` | `readAccepted` | — | состояние после перечитывания равно состоянию после apply; `TestAcceptedExactHistory`-подобный пин | `TestReanchor` |
| run после `reanchor`: Task, assessment в новом/старом диапазоне | `prepare`/`submit` | run создан | новые цитаты в Task; цитата в старом диапазоне — «цитата вне блока назначенного требования» | `TestReanchor` |
| подсказки: общая строка у двух записей / уникальная строка | `check RAW` | — | `unique_shared` 0 / 1; лимит 4 | `TestCheckAcceptance/hints`, `/hint_limit` |
| `index`/`prepare` с устаревшими `scopes` (неизвестный ID, повторный ID, нераспределённый ID) | `assignRequirements` | — | ошибка с именем scope и списками ID; `scopes: []` и корректные scope — без изменений | `TestScopeAssignmentErrors`, существующие `TestContract`/`TestScopeAdvisories` |
| `check RAW DECISION`: валидный / duplicate / RAW-only | — | — | `base_index_current` true / ключ отсутствует / ключ отсутствует | `TestCheckAcceptance/decision` |

Представительные реальные артефакты: ledger пилота (три commits) на временной копии для hit@4/`unique_shared`; `acceptance/accepted-index.json` frozen-сценарии (apply прежних действий не меняется); `acceptance/declared-v01` (declared-профиль без scopes — сообщения не меняются).

## Commit Plan
- **Commit 1** (after tasks 1-2): "feat(accepted): §22 — reanchor: перепривязка с сохранением принятого содержания"
- **Commit 2** (after tasks 3-4): "feat(check): unique_shared и лимит 4 подсказок; диагностика scopes; base_index_current"
- **Commit 3** (after task 5): "docs(launcher): reanchor и unique_shared в SKILL.md, README, ROADMAP, adoption"
- **Commit 4** (after task 6): "docs(dist): релиз v0.1.7 и оценка подсказок на журнале пилота"

## Tasks

### Phase 1: Контракт и reanchor
- [x] Task 1: §22 tool-spec «Уроки третьего прогона» (REQ-SA-043, 22.1–22.3), редакция 1.1; AGENTS/ARCHITECTURE
  - Deliverable: новый раздел перед «См. также», по профилю §4. Причина редакции 1.1 — третий прогон 2026-09-17 (0 rebind из 50, 33 нормы на rev 3, ambiguous 8→12; 4 ложных top-1 по общим строкам; `index` без имени scope/ID; `freshness:"stale"` в dry-run). REQ-SA-043 «Перепривязка с сохранением принятого содержания»: Условие — DECISION содержит `reanchor` с одним прежним active ID и одним кандидатом; Требование — принятое содержание (title, condition, statement, verification, exceptions, clarity, unresolved), ID, revision, content hash и parents сохраняются из прежней записи, цитаты и source берутся из кандидата, title/verification в target пусты, guard нормативной цитаты действует, кандидат учитывается ровно раз, формулировка кандидата остаётся в raw/истории, хост отвечает за смысловую эквивалентность, бинарник её не вычисляет (контракт п.3); Проверка — комбинации плана; Источник/Обоснование/Зависимость (accepted-index п.1, п.3–4, п.8; SA-016, SA-041, SA-042). 22.1 подсказки: лимит 4, `unique_shared`, основание — замер (hit@4 49/49 и 50/50; взвешивание ухудшает: 43–44/49; `unique_shared>0` у 85/88 верных top-1, у 0/3 ложных); ранжирование не меняется. 22.2 диагностика `scopes`: формат ошибок. 22.3 `check` с DECISION: `base_index_current`, семантика `freshness`. Шапка: редакция 1.1. AGENTS.md — строка §22, «§13–22»; ARCHITECTURE.md — accepted.go (reanchor), specs.go (диагностика scopes).
  - Files: `docs/tool-spec.md`, `AGENTS.md`, `.ai-factory/ARCHITECTURE.md`
  - Verify: `mdq tree docs/tool-spec.md` показывает §22 с REQ-SA-043 и 22.1–22.3; `git diff codex/bootstrap -- docs/tool-spec.md` — шапка и §22; `shasum -a 256 -c acceptance/*.sha256 acceptance/legacy/baseline.sha256` OK; `rg -n 'smsplace|FinanceSystem' docs/tool-spec.md` без новых вхождений; `go test ./tool -short`.
  - Logging: документ фиксирует: `reconcile`/`check` — DEBUG «reconcile: reanchor» {id, candidate}; `index`/`prepare` — ошибка с scope/ID; `check` — без новых INFO.
  - REQ: accepted-index п.1/п.3/п.4/п.8, §6, REQ-SA-016, REQ-SA-041, REQ-SA-042

- [x] Task 2: действие `reanchor` в `applyAccepted` и guard; регрессии
  - Deliverable: `tool/accepted.go`: `case "reanchor": valid = p == 1 && n == 1`; в цикле targets перед проверкой «принятие требует title/verification»: если `reanchor` — title/verification должны быть пустыми (ошибка «reanchor сохраняет прежние title/verification; поля должны быть пустыми»), `position := active[operation.Previous[0]]`, `old := state.Records[position].Requirement`, `req := old`, `meta := *old.Accepted`, `meta.Citations = append([]Citation{}, candidate.Citations...)`, `req.Accepted = &meta`, `req.Source = candidate.Citations[0]`, `state.Records[position] = acceptedRecord(req, "active", operation.Reason)`, `history.Assignments` += `{candidate.ID, req.ID}`, `seenCandidates` учтён; DEBUG «reconcile: reanchor» {id, candidate}. `normativeAnchor`: `reanchor` в списке действий. Тесты `tool/accepted_test.go` `TestReanchor`: `acceptedFixture` — accept C001 (rules.md:2, статья «Лимит 8 МиБ», title/verification из `acceptOperation`); правка rules.md (вставка строки → сдвиг) + новый raw с перефразированным кандидатом C001 (statement «Ограничение размера 8 МиБ», citations rules.md:3) → decision `reanchor [REQ-AI-001] → C001` с пустыми title/verification → запись: ID/revision 1/content hash/title/statement/verification/clarity/unresolved прежние, `accepted.citations` == новые, `source` == новая первая цитата, `history[1].assignments == [{C001, REQ-AI-001}]`; `acceptedRead` после apply == состояние (replay детерминирован); `check CONFIG RAW DECISION` до apply → `assignments == [{C001, REQ-AI-001, reanchor, 1, [REQ-AI-001]}]`; мутации: title непустой → отказ с текстом; previous retired/повторный → прежние тексты; `referenceFixture`: reanchor на только-справочного кандидата → guard; `prepare` → Task с новыми citations; `submit` с spec-цитатой в новом диапазоне OK, в старом (rules.md:2 после сдвига — другая строка) → «цитата вне блока назначенного требования». Существующие `TestAccepted*`, frozen-сценарии — без изменений.
  - Files: `tool/accepted.go`, `tool/accepted_test.go`
  - Depends: 1
  - Verify: `go test ./tool -run 'TestReanchor|TestAccepted|TestCheckAcceptance|TestReferenceSources|TestReconcileApplyView' -count=1`; `go vet ./tool`.
  - Logging: DEBUG «reconcile: reanchor» {id, candidate}; ошибки — новый текст только для title/verification; цитаты в лог не попадают.
  - REQ: REQ-SA-043, accepted-index п.1/п.3/п.4, REQ-SA-041
<!-- Commit checkpoint: tasks 1-2 -->

### Phase 2: Подсказки, диагностика scopes, признак base_index
- [x] Task 3: `unique_shared` и лимит 4 в подсказках `check`
  - Deliverable: `tool/accepted.go`: `matchHintLimit = 4` (комментарий: hit@4 49/49 и 50/50 на двух переходах пилота); `matchHint.UniqueShared int json:"unique_shared"`; `lineFrequency(records []AcceptedRecord) map[string]int` — число active-записей, содержащих строку (по `accepted.citations`); `matchHints(candidate, records, frequency)` считает `unique_shared` = общие строки с `frequency == 1`; `checkedCandidates` вычисляет frequency один раз на вызов; порог и ранжирование прежние. Тесты `tool/check_test.go`: `/hints` — две записи с общей строкой (rules.md:2-3 и 2-2) → у обеих подсказок `unique_shared` по уникальным строкам (0 для общей), кандидат с уникальной строкой → 1; `/hint_limit` — пять записей с одинаковыми цитатами → 4 подсказки; обновить ожидания `matchHint{...}` (новое поле).
  - Files: `tool/accepted.go`, `tool/check_test.go`
  - Depends: 1
  - Verify: `go test ./tool -run 'TestCheckAcceptance' -count=1`; `go vet ./tool`.
  - Logging: DEBUG «check: подсказки сопоставления» дополняется {unique_shared} у первой подсказки.
  - REQ: 22.1, REQ-SA-042

- [x] Task 4: диагностика `scopes` в `assignRequirements`; `base_index_current` в `check`
  - Deliverable: `tool/specs.go` `assignRequirements`: для каждого scope — `unknown`, `duplicate` (по `ids`/`assigned`), после цикла — `unassigned` (ID из `ids` без `assigned`); ошибки: `fmt.Errorf("scope %s: неизвестные ID %v; повторно назначенные %v", scope.ID, unknown, duplicate)` (первый scope с проблемой; пустой список печатается `[]`) и `fmt.Errorf("не распределены по scope: %v", unassigned)`; списки отсортированы, не более 64 элементов (`…` при усечении); прочие тексты не меняются. `tool/accepted.go` `checkAcceptance`: в DECISION-ответе `view["base_index_current"] = true` (после успешного `stageAcceptance`; не добавляется при duplicate и в RAW-режиме). Тесты: `tool/specs_test.go` `TestScopeAssignmentErrors` — fixture с 5 нормами и `scopes: [{id: a, requirements: [REQ-DEMO-001, REQ-DEMO-009]}, {id: b, requirements: [REQ-DEMO-001, REQ-DEMO-002]}]` → ошибка содержит `scope a`, `REQ-DEMO-009`; вариант с повтором → `повторно назначенные [REQ-DEMO-001]`; вариант, где не хватает ID → `не распределены по scope: [REQ-DEMO-003 …]`; `tool/check_test.go` `/decision`: `base_index_current == true`, отсутствие ключа при duplicate и в RAW-режиме.
  - Files: `tool/specs.go`, `tool/accepted.go`, `tool/specs_test.go`, `tool/check_test.go`
  - Depends: 3
  - Verify: `go test ./tool -count=1` (полный); `go vet ./tool`; `CGO_ENABLED=0 go build -o bin/spec-audit ./tool`; ручная проверка на копии scope-конфига с устаревшими ID (временный CONFIG в scratchpad) → сообщение с scope и ID.
  - Logging: ошибки `index`/`prepare` — новые тексты (stderr JSON); `check` — без новых записей.
  - REQ: 22.2, 22.3, §6, REQ-SA-002, REQ-SA-042
<!-- Commit checkpoint: tasks 3-4 -->

### Phase 3: Документация и поставка
- [x] Task 5: launcher SKILL.md; README, ROADMAP, документ применения
  - Deliverable: `skills/spec-audit/SKILL.md` абзац accepted: `reanchor` (когда подсказка `check` указывает на существующую норму, чья принятая формулировка по-прежнему верна — сохранить содержание/ID/revision, переместить только цитаты; `revise` — только при смене смысла; `unique_shared: 0` — подсказка держится на строках, общих с другими нормами, проверить перед reanchor); после ошибки `index` со списком ID — обновить `scopes`. Ограничения `TestSkillFiles` (без `#<цифры>`, `smsplace`, `/Users/`). README: accepted-абзац — `reanchor`, `unique_shared`, лимит 4, `base_index_current`, новые ошибки `index`; «Приёмка и ограничения» — строка §22 (реализовано; регрессии; не проверено до релиза: подсказки на пилоте и `reanchor` в живом цикле). ROADMAP: пункт «Уроки третьего прогона» — подпункты 1–4 `[x]` с датой и свидетельствами, 5–7 остаются `[ ]`; строка таблицы этапов. docs/smsplace-adoption.md «Обычный рабочий цикл» п.2 — `reanchor` при переприёмке.
  - Files: `skills/spec-audit/SKILL.md`, `README.md`, `.ai-factory/ROADMAP.md`, `docs/smsplace-adoption.md`
  - Depends: 2, 4
  - Verify: `go test ./tool -run 'TestSkillFiles|TestHostDecisionMirror|TestSkill' -count=1`; `rg -n 'reanchor|unique_shared' skills/spec-audit/SKILL.md README.md`; `shasum -a 256 -c acceptance/*.sha256` OK; `go test ./tool -short`.
  - Logging: нет нового.
  - REQ: REQ-SA-037, REQ-SA-043, 22.1–22.3

- [x] Task 6: релиз `v0.1.7`, обновление хоста/пилота/синтетики, оценка подсказок на журнале пилота
  - Deliverable: merge в `codex/bootstrap` (по подтверждению владельца), тег `v0.1.7`, approve deployment; `spec-audit update` → 0.1.7; `skill update --host both` в `spec-audit-synthetic` и пилоте (коммиты — по запросу). Read-only оценка на временной копии `reports/` пилота (scratchpad): журнал усечён до commits[0] и до commits[1], источники восстановлены из git пилота по хэшам raw (как в T6 прошлого плана) → `check CONFIG RAW` → сравнить `matches` с `previous` из decision-accept-002/003: зафиксировать top-1, hit@4, долю верных top-1 с `unique_shared > 0` и ложных с 0; sha256 живого журнала и `git status` пилота неизменны; `reanchor` на пилоте не выполняется (решение владельца, следующий цикл). README «Приёмка и ограничения» — строка релиза 0.1.7 с цифрами.
  - Files: `README.md`; (пилот и синтетика — вне репозитория)
  - Depends: 5
  - Verify: `gh release view v0.1.7` — 5 ассетов; `spec-audit version` → 0.1.7; `spec-audit update` → `updated:false`; `git -C <pilot> status --porcelain` пуст до/после (кроме tracked-копии skill до коммита).
  - Logging: нет нового.
  - REQ: REQ-SA-035, REQ-SA-037, 22.1
<!-- Commit checkpoint: tasks 5-6 -->

## Открытые вопросы (не блокируют T1–T6)
1. До T2: `reanchor` на ambiguous-норму сохраняет `clarity`/`unresolved` прежними — это и есть цель (перефразировка unresolved не плодит ambiguous); если хост хочет изменить clarity — `revise`. Подтверждение владельца желательно, но план исходит из этого.
2. До T4: формат списков ID в ошибке — Go `%v` (`[REQ-AI-013 REQ-AI-020]`); при желании — через запятую.
3. После T6: пункт 5 ROADMAP (облегчённый host DECISION, frozen §14) — отдельный план; `reanchor` в живом цикле пилота — следующий чистый прогон.

## Риски
- `reanchor` меняет цитаты без смены content hash: `snapshot_id` run после reanchor всё равно меняется (Requirement содержит citations), прежние run становятся историей — ожидаемо (REQ-SA-010), но README должен это назвать.
- `reanchor` даёт хосту способ «протащить» перефразированного кандидата под старой формулировкой без смысловой проверки — ответственность хоста явно записана в §22 и launcher; бинарник по-прежнему требует полного учёта и нормативной цитаты.
- Лимит 4 и `unique_shared` замерены на двух переходах одного scope; ранжирование не меняется, поэтому регресса hit@3 нет по построению.
- Изменение текстов ошибок `assignRequirements` затрагивает всех потребителей `snapshot` (index/prepare/submit/…) — тесты не пинят тексты, но launcher-проза README упоминает старое сообщение? (проверить `rg 'повторно назначенное'`).
