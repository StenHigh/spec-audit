# Implementation Plan: Справочные источники ТЗ и деление большого accepted-набора (§20)

Branch: codex/reference-specs-scope-split
Created: 2026-09-17

## Original Request
Уроки чистой сессии — отложенные пункты (решение владельца, принято 2026-09-17): (1) reference-only файлы ТЗ в source_set без учёта в лимите 64 кандидатов accepted-index — пилот исключил FinanceSystem.md, 5 норм остались ambiguous; контракт docs/accepted-index.md frozen, нужна обоснованная редакция/расширение; (2) разбиение большого accepted-набора при scopes: [] на несколько заданий — 49 норм × 2509 файлов ≈ 30 мин и 0,5 M токенов на роль; рекомендация в launcher или авто-разбиение в prepare. См. ROADMAP «Уроки чистой сессии — отложенные пункты», tool-spec §13/§19, docs/accepted-index.md, docs/smsplace-adoption.md.

## Settings
- Testing: yes
- Logging: standard
- Docs: yes

## Roadmap Linkage
Milestone: "Уроки чистой сессии — отложенные пункты (решение владельца)"
Rationale: решение владельца 2026-09-17: пункт 1 — группа `references` в CONFIG (файлы в snapshot/source_set с флагом, без кандидатов, с проверкой нормативной цитаты в `reconcile`); пункт 2 — правило launcher плюс предупреждение бинарника, без авто-разбиения (§6 frozen: «слишком большой scope требует осознанного деления»).

## Requirements Reconciliation
Authority: [docs/tool-spec.md](../../docs/tool-spec.md) frozen §1–10 (§6: группы specs/code/tests, «файл не может одновременно попасть в разные категории», «пустой scopes назначает все объявленные нормы одному scope `all`», лимиты 16 scope / 64 нормы на scope, «нормы не разрезаются по числу токенов; слишком большой scope требует осознанного деления»; §9 неизменяемый manifest; REQ-SA-003 переносимая идентичность; REQ-SA-006 привязка цитат; REQ-SA-010 устаревание не скрывается) > [docs/accepted-index.md](../../docs/accepted-index.md) (frozen хэшем `acceptance/accepted-index.sha256`: «RAW … source_set содержит весь выбранный набор specs, до 64 кандидатов», «Изменение вводной/другого файла тоже делает пакет stale», «до 64 active-норм в одном полностью согласуемом пакете … нужен отдельно спроектированный следующий срез») и [docs/legacy-extraction.md](../../docs/legacy-extraction.md) (формат raw, «несколько цитат из разных файлов разрешены») > [RULES.md](../RULES.md) («не переписывай frozen §1–10, контракты accepted-index/legacy-extraction; новая семантика — обоснованное расширение и отдельный контроль»; «урок пилота закрепляй независимой синтетической регрессией») > [rules/base.md](../rules/base.md), [ARCHITECTURE.md](../ARCHITECTURE.md) > ROADMAP (объём) > [SKILL.md](../../skills/spec-audit/SKILL.md) (текущая проза launcher). Разведка: два независимых Explore-агента по `tool/accepted.go`, `tool/legacy.go`, `tool/specs.go`, `tool/main.go`, `tool/report.go`, тестам и документации; ключевые места проверены вручную.

### Решения (по итогам разведки)
| Решение | Обоснование | Проверка |
| --- | --- | --- |
| Оба пункта оформляются новым §20 tool-spec «Справочные источники и деление большого набора» (редакция 0.9): REQ-SA-041 «Справочный источник без кандидатов» и подраздел 20.1 «Деление большого набора: предупреждение бинарника и правило launcher». §1–10, `docs/accepted-index.md`, `docs/legacy-extraction.md` не редактируются; фраза §19 «остаются отдельным решением владельца» заменяется ссылкой на §20 (§19 не frozen) | RULES: спецификация прежде реализации; прецедент §19 (расширение без правки frozen) | `shasum -a 256 -c` всех списков `acceptance/` OK; `git diff codex/bootstrap -- docs/tool-spec.md` — только шапка, одна фраза §19, §20 |
| CONFIG получает необязательную четвёртую группу `references: {paths, include, exclude}` (`Config.References *Sources` с `omitempty`; nil сохраняет прежние байты manifest и `snapshot_id`, как `SDK *SDKConfig` в `main.go:73`). Допустима только при `index_mode: accepted`; `paths` непуст; include/exclude — те же glob-правила имени файла. Файлы группы входят в snapshot как `SourceFile{Kind:"spec", Reference:true}` (`Reference bool json:"reference,omitempty"`); файл одновременно в `specs` и `references` — существующая ошибка «файл попал в несколько категорий» (`seen` в `scanSnapshot`, `main.go:527-532`, ключ — метка группы, не kind) | Kind остаётся `spec`, поэтому существующие потребители (`acceptedSources` accepted.go:264, `acceptedFresh` :280, `acceptedView` :398, `checkAssessment` main.go:763-775, навигация report.go:246) автоматически включают справочные файлы в source_set, freshness и допустимые цитаты; отличается лишь то, что явно проверяет флаг. `snapshot_id` = digest(manifest без путей) (`main.go:478-483`) → включение/переименование справочного файла меняет идентичность детерминированно (REQ-SA-003) | `TestReferenceSources/config`: без `index_mode` → отказ; пустой `paths` → отказ; пересечение с `specs` → отказ; `TestDeclaredHistoricalRun` и пины manifest без изменений (поле не сериализуется при nil) |
| Extraction: raw по-прежнему перечисляет весь source_set (нормативные + справочные файлы, формат `{path, sha256}` без изменений); лимит 64 кандидатов на raw не меняется — справочные файлы не порождают кандидатов. Читающий и применяющий `reconcile` показывают `source_set[].reference: true` через view-структуру (не через `legacySource`, чтобы не ослабить строгий разбор raw). Guard при apply (`reconcile`, после `legacyValidate`, до `applyAccepted`): у каждого target операций accept/rebind/revise/split/merge хотя бы одна цитата из файла без флага; reject/defer/retire не проверяются. Guard не применяется при replay журнала (`readAccepted` не знает CONFIG) — старые ledger остаются читаемыми | Пилот: `FinanceSystem.md` исключён из scope только из-за лимита 64, 5 норм остались ambiguous с unresolved «см. FinanceSystem.md»; контракт accepted-index допускает цитаты из нескольких файлов и требует все определяющие фрагменты | `TestReferenceSources/reconcile`: кандидат с цитатами rules.md + clarification.md → принят, цитаты сохранены; кандидат только с цитатой clarification.md → отказ «кандидат C0xx цитирует только справочные источники», байты `._accepted-index.json` до/после равны; тот же кандидат reject/defer → принят; view read/apply содержит `reference:true` |
| Freshness: изменение справочного файла делает индекс stale ровно так же, как любого файла source_set (accepted-index п.6; REQ-SA-010). Цена — полная повторная приёмка пакета (новый raw + rebind); «rebind без нового raw» — вне плана (frozen) | Исключение справочных файлов из freshness скрыло бы устаревшие определения | `TestReferenceSources/stale`: правка clarification.md → `index`/`prepare` отказывают «принятый индекс отсутствует или stale», `reconcile` read → `freshness:"stale"` |
| Роли: `TaskBatch.Files[]` несёт `reference:true`; Task и цитаты норм не меняются; spec-цитата assessment в справочном файле допустима только внутри принятого диапазона (существующий `req.containsSource`, `main.go:775`). `protocol.txt` и `host-decision.md` не меняются (пины `TestSkillFiles`/`TestHostDecisionMirror`) | Роль читает справочный файл ради смысла; правило цитирования уже покрывает диапазоны | `TestReferenceSources/roles`: `submit` с spec-цитатой в принятом диапазоне clarification.md → принят; цитата вне диапазона → отказ |
| Отчёт: `AuditSection.Reference` (строка «справочный источник — кандидаты не извлекались», метрики 0 не читаются как «норм нет»), группа `reference` в `Navigation.Groups` (выбор путей группы; файл считается в группе `reference`, не `spec`), бейдж «справочный» и `data-reference` в карточке файла. Manifest/Result/review не мигрируют (15.1) | REQ-SA-025 честное основание карты; REQ-SA-028 сводка по источникам | `TestReferenceSources/report`: `navigation.sections[clarification.md].reference==true`, `total==0`; `groups` содержит `reference` с `files==1`, `spec` с `files==1`; HTML содержит «справочный» |
| Деление большого набора — **без** авто-разбиения: `scopes: []` по-прежнему один scope `all` (§6 frozen). Бинарник добавляет именованную константу `scopeAdvisoryRequirements = 24` (evidence: 18 норм/50 файлов — комфортный A/B `full-v2`; 49 норм × 2509 файлов — 27–36 мин и 0,5–0,6 M токенов на роль; 64 — жёсткий предел §6) и helper `scopeAdvisories(m Manifest) []string`: для каждого scope с числом норм > 24 — сообщение «scope X: N норм, F файлов; §6: слишком большой scope требует осознанного деления (рекомендация ≤ 24 норм)». `index` добавляет `advisories` в stdout JSON только при непустом списке; `index` и `prepare` пишут `slog.Warn` на каждое сообщение; stdout `prepare` (TaskBatch) не меняется | Хосту пилота не хватило сигнала до dispatch; §6 запрещает бинарнику резать нормы; авто-разбиение по порядку ID — механическое, не смысловое | `TestScopeAdvisories`: 25 declared-норм при `scopes: []` → `index` содержит `advisories` c одной строкой, WARN в stderr через процесс (`TestNativeCLI` или захват handler); 24 нормы → ключа нет; явные scope 13+12 → нет; 65 норм → прежняя ошибка «более 64 требований: задайте явные scope» |
| Launcher (SKILL.md, английский текст): (а) scope-конфиг — `references: {paths: [...]}` для определяющих смысл документов, которые в этом scope не аудируются; (б) extraction: передать extractor’у source_set с флагами: кандидаты только из нормативных файлов, справочные — читать и цитировать для определений; (в) после приёмки при `advisories` в `index` или > 24 active-норм — задать `scopes` до `prepare`: группировать active ID по подразделам ТЗ (порядок первой принятой цитаты), 16–24 нормы на scope, `focus` = подразделы, каждый active ID ровно один раз (бинарник отклоняет пропуск/повтор), обновлять scopes после решений, меняющих ID; (г) dispatch по scope параллельно в пределах слотов, review — одно DECISION на run. Список файлов ролям не сужается: сужение хостом скрывает свидетельства | SKILL.md:14 запрещает scopes как фильтр, но не говорит, как делить; :16 не различает нормативные и справочные файлы; :20 «respect package limits» без правила | `TestSkillFiles` (embed = skills/); временная `skill install` показывает новый текст; `rg -n 'references|advisories' skills/spec-audit/SKILL.md` |
| Поставка: launcher встроен в бинарник → релиз `v0.1.5` по README «Релиз», `spec-audit update` на хосте, `skill update` в пилоте и `spec-audit-synthetic` (коммит tracked-копий — по запросу). Изменение scope-конфига пилота (`references: [TZ/FinanceSystem.md]`, явные `scopes`) делает текущий accepted-индекс `02-finance` stale и требует новой полной приёмки в чистой сессии — отдельное разрешение владельца, в план не входит; рекомендация фиксируется в docs/smsplace-adoption.md | §18: dev-сборка не пишет управляемый receipt; бизнес-проект не меняется без разрешения | `gh release view v0.1.5` — 5 ассетов; `spec-audit version` → 0.1.5; `git -C <pilot> status --porcelain` пуст после коммита; ledger пилота байт-в-байт прежний |

### Поддерживаемые комбинации
| Комбинация | Вход / валидация | Состояние | Результат | Проверка |
| --- | --- | --- | --- | --- |
| `references` при отсутствующем `index_mode` | `loadConfig` | — | отказ «references поддерживаются только при index_mode: accepted» | `TestReferenceSources/config` |
| `references` с пустым `paths`, несуществующим путём, symlink | `loadConfig`/`scanSnapshot` | — | отказ теми же текстами, что для других групп | `TestReferenceSources/config` |
| один файл в `specs` и `references` | `scanSnapshot` | — | «файл попал в несколько категорий» | `TestReferenceSources/config` |
| accepted без `references` | как сейчас | как сейчас | manifest/state байт-в-байт прежние; `snapshot_id` прежний | существующие пины, `TestDeclaredHistoricalRun`, `TestAcceptedHashVector` |
| `reconcile CONFIG` (read) с `references` | — | — | `source_set` содержит справочный файл с `reference:true`, нормативные без поля; `freshness` считает оба | `TestReferenceSources/reconcile` |
| apply: кандидат с нормативной + справочной цитатами; accept/rebind/revise/split/merge | `legacyValidate` + guard | ledger записан | принят; все цитаты сохранены в `accepted.citations` | `TestReferenceSources/reconcile` |
| apply: кандидат только со справочными цитатами; accept/rebind/revise/split/merge | guard | без записи | отказ с ID кандидата; ledger неизменен | `TestReferenceSources/reconcile` |
| apply: тот же кандидат; reject/defer | guard пропускает | ledger записан | принят (история видит причину) | `TestReferenceSources/reconcile` |
| apply: raw без справочного файла в `source_set` | `legacyValidate` | без записи | прежний отказ «неверная версия, набор источников…» | `TestReferenceSources/reconcile` |
| `prepare` после приёмки | — | run создан | `Task.requirements[].accepted.citations` содержат справочный диапазон; `TaskBatch.files[]` справочный файл с `reference:true` | `TestReferenceSources/roles` |
| `submit`/`validate`: spec-цитата в принятом диапазоне справочного файла / вне диапазона | `checkAssessment` | результат записан / без записи | принят / отказ (REQ-SA-006) | `TestReferenceSources/roles` |
| правка справочного файла после приёмки | freshness | — | `index`/`prepare` отказ stale; `status`/`report` прежнего run — история | `TestReferenceSources/stale` |
| индекс принят без `references`, затем группа добавлена в CONFIG (путь пилота `02-finance`) | `acceptedFresh` — длина source_set (`accepted.go:285`) | — | `reconcile` read `freshness:"stale"`, новый файл в `source_set` с `reference:true`; `index`/`prepare` отказ до новой полной приёмки | `TestReferenceSources/stale` |
| `report` | — | — | секция справочного файла `reference:true, total:0`; группы `spec`/`reference`; HTML-бейдж | `TestReferenceSources/report` |
| `scopes: []`, ≤ 24 норм (declared или accepted) | — | — | `index` без `advisories`, WARN нет | `TestScopeAdvisories` |
| `scopes: []`, 25–64 норм | — | — | `index.advisories` = 1 строка; `prepare` — WARN, TaskBatch прежний | `TestScopeAdvisories` |
| явные scope, один > 24 норм | — | — | advisory для этого scope | `TestScopeAdvisories` |
| > 64 норм при `scopes: []` / > 64 в scope / > 16 scope | — | — | прежние отказы §6 | существующие проверки `assignRequirements`, `loadConfig` |

Представительные реальные артефакты: `acceptance/declared-v01` (исторический run без `references` — неизменные manifest/state), `acceptance/config.yaml` (корпус для `index`/`prepare` через процесс), scope-конфиг пилота `02-finance` (комментарий «FinanceSystem.md — отдельный scope» — цель `references`; правится только по отдельному разрешению), установка launcher в `spec-audit-synthetic` и пилоте (обновление SKILL.md).

## Commit Plan
- **Commit 1** (after tasks 1-2): "feat(config): §20 — группа references в snapshot и manifest"
- **Commit 2** (after tasks 3-4): "feat(accepted): справочные источники в reconcile, задании и отчёте"
- **Commit 3** (after tasks 5-6): "feat(launcher): предупреждение о большом scope и правило деления набора"
- **Commit 4** (after task 7): "docs(dist): релиз v0.1.5 и обновление launcher в пилоте и синтетическом проекте"

## Tasks

### Phase 1: Контракт и конфигурация
- [x] Task 1: §20 tool-spec «Справочные источники и деление большого набора» (REQ-SA-041, 20.1), редакция 0.9
  - Deliverable: новый раздел перед «См. также», по профилю §4. Причина редакции 0.9 — пилот 2026-09-16 (FinanceSystem.md вне scope из-за лимита 64 → 5 ambiguous; 49 норм × 2509 файлов в одном scope). REQ-SA-041 «Справочный источник без кандидатов»: Условие — CONFIG с `index_mode: accepted` содержит группу `references`; Требование — файлы группы входят в snapshot и source_set (хэш, freshness, допустимые цитаты) с признаком `reference`, не порождают кандидатов, лимиты 64/16/4 MiB не меняются, `reconcile` отклоняет принятие/rebind/revise/split/merge кандидата без цитаты из нормативного файла, отчёт показывает справочные файлы отдельно от норм; Проверка — комбинации из плана; Источник — пилот 02-finance, решение владельца 2026-09-17; Обоснование — определяющие смысл документы нужны для точной нормы, но не должны расходовать пакет; Зависимость — REQ-SA-003, SA-006, SA-010, SA-016, accepted-index п.6, п.8. 20.1: `scopes: []` остаётся одним scope `all`; константа 24 как рекомендация, `advisories` в `index`, WARN в `index`/`prepare`; правило launcher (группировка по подразделам, все active ID ровно один раз, обновление после решений); граница — бинарник не режет нормы (§6). В §19 фраза «Пункты о reference-only файлах … остаются отдельным решением владельца» → «решены в §20». Шапка: редакция 0.9, 2026-09-17. AGENTS.md: строка таблицы «Справочные источники и деление набора: §20» и в пункте 3 «Что читать при старте» устаревшее «текущие расширения §13–16» → «§13–20». ARCHITECTURE.md: строки main.go (четыре группы snapshot) и accepted.go (проверка нормативной цитаты).
  - Files: `docs/tool-spec.md`, `AGENTS.md`, `.ai-factory/ARCHITECTURE.md`
  - Verify: `mdq tree docs/tool-spec.md` показывает §20 с REQ-SA-041 и 20.1; `git diff codex/bootstrap -- docs/tool-spec.md` — только шапка, одна фраза §19, §20; `shasum -a 256 -c acceptance/*.sha256 acceptance/legacy/baseline.sha256` OK; `rg -n 'smsplace|SMSPlace|FinanceSystem' docs/tool-spec.md` — без новых вхождений (пилот только в adoption-документе); `go test ./tool -short` зелёный.
  - Logging: документ; фиксирует уровни: `reconcile` — INFO «reconcile: справочные источники» {reference_files}; guard — ошибка с ID кандидата без текста цитат; advisory — WARN «scope содержит много норм» {scope, requirements, files, advisory}.
  - REQ: §6, §9, REQ-SA-003, REQ-SA-006, REQ-SA-010, REQ-SA-016, REQ-SA-025, accepted-index п.6/п.8

- [x] Task 2: группа `references` в CONFIG, snapshot и manifest
  - Deliverable: `tool/main.go`: `Config.References *Sources` (`yaml:"references,omitempty" json:"references,omitempty"`, комментарий как у `SDK`); `SourceFile.Reference bool json:"reference,omitempty"`; в `loadConfig` — до цикла групп: `References != nil` требует `IndexMode == "accepted"` (ошибка «references поддерживаются только при index_mode: accepted») и непустой `Paths` («references: нужен непустой paths»); затем `cfg.References` (при non-nil) добавляется в фиксированный цикл групп `[]*Sources{&cfg.Specs, &cfg.Code, &cfg.Tests}` (`main.go:359`) — сортировка Paths/Include/Exclude (стабильный `snapshot_id`), проверка glob, `localPath`, повторный путь, пересечение с reports_dir; `initConfig`/`defaultConfig` не меняются. `scanSnapshot`: группы описываются `{label, kind, sources, reference}` — `{"spec","spec",cfg.Specs,false}, {"reference","spec",*cfg.References,true}` (только при non-nil), code, tests; `seen[path]` хранит label (одинаковый label → пропуск, другой → «файл попал в несколько категорий»); пустая группа допустима только для `tests` — пустая `references` (нет файлов по include) отклоняется как `specs` («пустая группа источников: reference», отклонение от первоначальной формулировки плана ради единообразия §6); `SourceFile{path, kind, digest, len, reference}`; условие разбора declared остаётся `group.kind == "spec" && cfg.IndexMode == ""` (справочные файлы существуют только в accepted-режиме). Тесты `tool/accepted_test.go` `TestReferenceSources/config`: сабтесты на три отказа, на `snapshot_id` (с `references` ≠ без), на manifest без поля при nil (`!bytes.Contains(json, "references")`).
  - Files: `tool/main.go`, `tool/accepted_test.go`
  - Depends: 1
  - Verify: `go test ./tool -run 'TestReferenceSources|TestDeclaredHistoricalRun|TestContract|TestAcceptedHashVector' -count=1`; `go vet ./tool`; `./bin/spec-audit index acceptance/config.yaml` → без изменений в `files[]`.
  - Logging: DEBUG «snapshot: справочные файлы» {count} в конце `scanSnapshot` при наличии группы; ошибки конфигурации — существующий формат (stderr JSON).
  - REQ: REQ-SA-041, REQ-SA-002, REQ-SA-003, §6
<!-- Commit checkpoint: tasks 1-2 -->

### Phase 2: Приёмка, задание и отчёт
- [x] Task 3: `reconcile` — справочные файлы в view, guard нормативной цитаты
  - Deliverable: `tool/accepted.go`: `acceptedSources(cfg)` возвращает дополнительно `reference map[string]bool` (пути с флагом) — обновить два вызова в `reconcile` и тестовый helper `acceptedInputs` (`accepted_test.go:50`); view-тип `acceptedSource{Path, SHA256 string; Reference bool json:"reference,omitempty"}` в `acceptedView` (`legacySource` не меняется); функция `normativeAnchor(raw legacyRaw, decision AcceptedDecision, reference map[string]bool) error` — для операций accept/rebind/revise/split/merge каждый target по ID кандидата должен иметь хотя бы одну цитату с `!reference[path]`; неизвестный candidate ID пропускается (ошибку даёт `applyAccepted`, тексты и приоритет прежних отказов `TestAcceptedRejections` не меняются); при пустом `reference` guard тривиально проходит; ошибка `fmt.Errorf("кандидат %s цитирует только справочные источники (references); нужна цитата из нормативного файла", id)`; вызывается в `reconcile` после первого `legacyValidate` и до `applyAccepted` (повторная проверка перед записью не нужна — raw и CONFIG под lock не меняются между вызовами, но второй `legacyValidate` уже есть; guard детерминирован по тем же raw/decision). INFO «reconcile: справочные источники» {reference_files} при apply с непустым набором. Тесты `TestReferenceSources/reconcile` (fixture `acceptedFixture` + `clarification.md` из `TestAcceptedSourcesAndAudit`, CONFIG: `references: {paths: [clarification.md]}`): read-view с флагами; accept смешанного кандидата — OK и `accepted.citations` содержит обе; accept только-справочного — отказ, байты `._accepted-index.json` равны до/после; reject/defer только-справочного — OK; raw без справочного файла в `source_set` — прежний отказ; rebind/revise/split после приёмки — guard действует (один негативный сабтест на split с только-справочным target).
  - Files: `tool/accepted.go`, `tool/accepted_test.go`
  - Depends: 2
  - Verify: `go test ./tool -run 'TestReferenceSources|TestAccepted|TestReconcileApplyView|TestLegacy' -count=1`; `go vet ./tool`; `rg -n 'acceptedSources\(' tool/` — все вызовы с новой сигнатурой.
  - Logging: INFO «reconcile: справочные источники» {reference_files}; DEBUG «reconcile: guard нормативной цитаты» {candidates}; ошибка guard — ID кандидата, без цитат.
  - REQ: REQ-SA-041, REQ-SA-016, accepted-index п.1/п.2/п.6, legacy-extraction «цитаты из разных файлов»

- [x] Task 4: справочные файлы в задании ролей и отчёте
  - Deliverable: `tool/report.go`: `AuditSection.Reference bool json:"reference,omitempty"` (устанавливается из `file.Reference` при создании секции, report.go:246-248); `Navigation.Groups` получает `{Kind:"reference", Selection:*m.Config.References}` при non-nil (после `spec`); подсчёт файлов группы (report.go:367-373) относит файл с `Reference` к группе `reference`, не `spec`. `tool/report.html`: строка секции — `{{if .Reference}}<small class="metric">справочный источник — кандидаты не извлекались</small>{{end}}`; карточка файла — `data-reference="{{if .Reference}}yes{{else}}no{{end}}"` и `{{.Kind}}{{if .Reference}} · справочный{{end}}`; таблица «Какие источники выбраны» без изменений (новая группа попадает автоматически). Тесты `TestReferenceSources/roles` и `/report`: `prepare` → `Task.requirements[0].accepted.citations` содержит диапазон clarification.md, `TaskBatch.files` — clarification.md с `reference:true`; `submit`: `sampleResult` ставит `Spec = req.Source` (`main_test.go:158`), поэтому тест вручную заменяет `Assessments[i].Spec` на принятую цитату из clarification.md → OK; та же цитата со сдвигом строки вне диапазона → отказ; `report` JSON: `navigation.sections` для clarification.md `reference:true, total:0`, `groups` `reference.files==1`, `spec.files==1`; HTML содержит «справочный источник — кандидаты не извлекались» и `data-reference="yes"`; дополнительно in-memory `buildNavigation` на синтетическом `Manifest` (паттерн `report_test.go:196-199`) закрепляет группу `reference` и `AuditSection.Reference` без полного run. `TestReferenceSources/stale`: (а) правка clarification.md → `index` и `prepare` отказ «принятый индекс отсутствует или stale», `reconcile` read `freshness:"stale"`, `status` прежнего run работает; (б) путь пилота: индекс принят без `references`, затем группа добавлена в CONFIG → `acceptedFresh` stale по длине source_set (`accepted.go:285`): `reconcile` read `freshness:"stale"` и новый файл в `source_set` с `reference:true`, `index` отказ.
  - Files: `tool/report.go`, `tool/report.html`, `tool/accepted_test.go`
  - Depends: 3
  - Verify: `go test ./tool -run 'TestReferenceSources|TestReportProjection|TestNavigation|TestHTML' -count=1`; `go vet ./tool`; UI-check из README «Первый запуск» на fixture-отчёте, если Playwright уже установлен (иначе — ручной просмотр HTML, зафиксировать в отчёте задачи).
  - Logging: DEBUG «карта отчёта: справочные секции» {count} в `buildNavigation`; без новых INFO.
  - REQ: REQ-SA-041, REQ-SA-006, REQ-SA-010, REQ-SA-025, REQ-SA-028, 15.1
<!-- Commit checkpoint: tasks 3-4 -->

### Phase 3: Деление большого набора и launcher
- [x] Task 5: `scopeAdvisories` — предупреждение о большом scope в `index`/`prepare`
  - Deliverable: `tool/specs.go`: `const scopeAdvisoryRequirements = 24` с комментарием-основанием (18 норм/50 файлов — комфортный A/B; 49 × 2509 — 27–36 мин и 0,5–0,6 M токенов на роль; 64 — предел §6); `func scopeAdvisories(m Manifest) []string` — по `m.Config.Scopes` после `assignRequirements` (включая авто-`all`): `fmt.Sprintf("scope %s: %d норм, %d файлов; §6: слишком большой scope требует осознанного деления (рекомендация ≤ %d норм)", …)`. `indexConfig`: при непустом списке — ключ `advisories` в ответе и `slog.Warn` на каждую строку («scope содержит много норм» {scope, requirements, files, advisory}); `execute` prepare-ветка после `snapshot(cfg)`: те же WARN, `TaskBatch` без изменений. Тесты `tool/specs_test.go` (или `main_test.go`) `TestScopeAdvisories`: fixture-корпус даёт 5 норм — тест генерирует `rules.md` в профиле §4 (блоки `### REQ-DEMO-0NN — …` + Условие/Требование/Проверка по формату `acceptance/corpus/rules.md`): 25 норм при `scopes: []` → `index` содержит `advisories` из одной строки с «all», «25 норм»; 24 нормы → ключа нет; явные scope 13+12 → нет; один явный scope из 25 → одна строка; 65 норм при `scopes: []` → прежняя ошибка; через процесс (`TestNativeCLI`-паттерн, `LOG_LEVEL=warn`) — stderr содержит «scope содержит много норм», stdout — один JSON; `TestNativeCLI` с `LOG_LEVEL=error` на 5-нормном корпусе остаётся с пустым stderr (`main_test.go:493`).
  - Files: `tool/specs.go`, `tool/main.go`, `tool/specs_test.go` (новый) или `tool/main_test.go`
  - Depends: 1
  - Verify: `go test ./tool -run 'TestScopeAdvisories|TestContract|TestNativeCLI' -count=1`; `go vet ./tool`; `./bin/spec-audit index acceptance/config.yaml 2>/dev/null | jq 'has("advisories")'` → `false`.
  - Logging: WARN «scope содержит много норм» {scope, requirements, files, advisory} в `index` и `prepare`; никаких изменений stdout `prepare`.
  - REQ: 20.1, §6 (лимиты и «осознанное деление»), REQ-SA-014

- [x] Task 6: launcher SKILL.md — справочные источники и правило деления; документация
  - Deliverable: `skills/spec-audit/SKILL.md` (английский, без путей пилота): §конфигурация scope (стр. 14) — «…the necessary normative spec files, `references: {paths: [...]}` for meaning-defining documents that this scope does not audit (hashed and citable, never a source of candidates)…»; §accepted (стр. 16) — extractor receives the `source_set` with `reference` flags: extract candidates only from normative files, read and cite references for definitions; the binary rejects a candidate cited only from references; шаг 1 (стр. 20) — «If `index` returns `advisories` or the active set exceeds 24 norms, define `scopes` in the scope CONFIG before `prepare`: group active IDs by their specification subsection (order of the first accepted citation), 16–24 norms per scope, `focus` naming the subsections, every active ID exactly once (the binary rejects a missing or duplicate ID); update `scopes` after any decision that adds or retires IDs. `scopes: []` is for small sets. Do not narrow a role's file list to a scope: hidden files become `unknown`, not evidence»; шаг 2 (стр. 21) — files with `reference: true` are context sources; шаг 3 (стр. 23) — dispatch per scope in parallel within available slots; `review` stays one DECISION for the whole run. `protocol.txt`/`host-decision.md` не меняются. Ограничения `TestSkillFiles` (`skill_test.go:64-80`): в SKILL.md запрещены токены `#<цифры>` (regex `(^|\s)#\d+\b` — писать «§20», не «#20»), `smsplace`, `/Users/`, ссылки `](../` вне копии; ссылки на docs — `../../docs/...` (переписываются при материализации). README: «Быстрый старт»/accepted-абзац (стр. 193) — пример `references`, `advisories`; «Приёмка и ограничения» — строка §20 (что реализовано, что не проверено: пилот с `references` ещё не пройден, 24 — эвристика); ROADMAP: пункт «отложенные пункты» → `[x]` с решением 2026-09-17 и свидетельствами, строка таблицы этапов; `.ai-factory/DESCRIPTION.md` — без изменений (стек прежний); `docs/smsplace-adoption.md` «Обычный рабочий цикл» — рекомендация для следующего цикла `02-finance`: `references: {paths: [TZ/FinanceSystem.md]}`, явные `scopes` по подразделам 1.7.x/1.8.x, отметка, что это делает текущий индекс stale и требует новой приёмки по разрешению владельца.
  - Files: `skills/spec-audit/SKILL.md`, `README.md`, `.ai-factory/ROADMAP.md`, `docs/smsplace-adoption.md`
  - Depends: 4, 5
  - Verify: `go test ./tool -run 'TestSkillFiles|TestHostDecisionMirror|TestSkill' -count=1`; `rg -n 'references|advisories' skills/spec-audit/SKILL.md README.md`; `rg -n '/Users/|SMSPlace' skills/` — пусто; `shasum -a 256 -c acceptance/*.sha256` OK; полный `go test ./tool`, `go vet ./tool`, `CGO_ENABLED=0 go build -o bin/spec-audit ./tool`.
  - Logging: нет нового.
  - REQ: REQ-SA-037 (skill — поставляемый файл), REQ-SA-041, 20.1
<!-- Commit checkpoint: tasks 5-6 -->

### Phase 4: Поставка
- [ ] Task 7: релиз `v0.1.5`, обновление хоста, пилота и синтетического проекта
  - Deliverable: merge ветки в `codex/bootstrap` (по запросу владельца), тег `v0.1.5`, `release.yml` (approve deployment `release` через `gh api … pending_deployments`), `spec-audit update` на хосте → 0.1.5; `skill update --host both` в `spec-audit-synthetic` и в пилоте (только tracked-копия skill; scope-конфиг и ledger пилота не трогаются), коммиты tracked-копий — по запросу. README «Приёмка и ограничения»: строка о релизе 0.1.5 и что не проверено (реальный `references`-прогон на пилоте — отдельное решение).
  - Files: `README.md`; (пилот и синтетический проект — вне репозитория инструмента)
  - Depends: 6
  - Verify: `gh release view v0.1.5` — 5 ассетов; `spec-audit version` → `{"version":"0.1.5","release":true}`; `spec-audit update` → `updated:false`; `git -C <pilot> status --porcelain` пуст после коммита; `shasum -a 256 <pilot>/.spec-audit/scopes/02-finance/reports/._accepted-index.json` не изменился.
  - Logging: нет нового.
  - REQ: REQ-SA-035, REQ-SA-037, REQ-SA-038
<!-- Commit checkpoint: task 7 -->

## Открытые вопросы (не блокируют T1–T7)
1. До T5: порог 24 — эвристика по двум пилотным замерам; владелец может задать другое число до реализации (константа одна).
2. До T6: включать ли в `index` для accepted-режима подсказку группировки (`records[].requirement.accepted.citations[0]` уже даёт порядок) — план оставляет группировку хосту; отдельный `suggested_scopes` — только при доказанной потребности.
3. После T7: повторная приёмка `02-finance` с `references: [TZ/FinanceSystem.md]` и явными `scopes` в чистой сессии — отдельное разрешение владельца (текущий индекс станет stale, run `finance-2026-09-16-01` остаётся историей). Ожидаемый эффект: 5 ambiguous-норм получают определяющие цитаты; время на роль — по scope.

## Риски
- Изменение справочного файла делает stale весь пакет scope (контракт accepted-index п.6): для «живого» документа-источника истины это означает повторную приёмку с rebind всех норм; облегчение (rebind без нового raw) требует редакции frozen-контракта и не входит в план.
- Guard нормативной цитаты действует только при apply; журналы, принятые до §20, не перепроверяются при replay (нет CONFIG) — принятые ранее нормы остаются active.
- `snapshot_id` меняется при добавлении `references` — прежние run того же scope становятся историей; это ожидаемо (REQ-SA-003) и должно быть названо в launcher/README.
- Порог 24 не измерялся систематически; WARN — совет, не отказ; хост может его проигнорировать. Правило launcher — проза, проверяется только синтетически (`TestSkillFiles`) и следующим пилотным прогоном.
- Группа `reference` в `Navigation.Groups` и поле `reference` в секциях — новые ключи JSON отчёта; HTML прежних run без них рендерится как раньше (поля `omitempty`).
