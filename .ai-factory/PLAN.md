# План: контроль извлечения из legacy Markdown

Branch: codex/bootstrap
Created: 2026-09-15

## Original Request

сделай комит и пуш, потом к следующему шагу

## Settings

- Testing: yes — frozen corpus, негативные проверки формата/цитат, два независимых извлечения и отдельная смысловая проверка.
- Logging: вывод стандартного `go test -v`: количества и причины отказа; без исходников и сырых ответов в диагностике.
- Docs: yes — отдельный короткий контракт эксперимента и ссылка из README; рабочую спецификацию v0.1 не расширять обещанием готового legacy CLI.
- Working root: /Users/sh/PhpstormProjects/spec-audit. SMSPlace read-only; конфиги, skills и артефакты туда не устанавливаются.
- Autonomous: настройки продолжают разрешённую пользователем последовательность. Публикация завершённого среза выполнена до этого шага; новый эксперимент не коммитить автоматически.

## Requirements Reconciliation

Authority: решения владельца → `docs/tool-spec.md` (§2, §10, §11) → контракт этого контрольного эксперимента → независимые ожидания → план. Ожидания оценивают только приёмочные случаи, не задают нормы бизнес-проекта.

| Сочетание | Основание | Проверка |
| --- | --- | --- |
| Обычный Markdown / явные REQ-блоки | Legacy-извлечение ещё не принято; существующий индекс учитывает объявленные нормы | Корпус без REQ; не заменять текущий CLI и не менять его baseline |
| Точная цитата / сохранённый смысл | Цитата сама не доказывает интерпретацию | Машинные guards отдельно от независимых semantic mappings |
| Общие условия / исключения / единицы / приоритет / неоднозначность | Выявленные риски потери норм при атомизации | Независимые синтетические случаи и один неизменённый фрагмент собственной спецификации |
| Полный список кандидатов / полнота обязательств | Семантический знаменатель не возникает из ответа модели | Gold фиксируется до извлечения; отсутствие/лишние нормы отмечаются отдельно |
| Смысловые агенты / детерминированный бинарник | Решение владельца | Агенты текущей сессии; только test-only валидатор, без модельного API и новых зависимостей |

Предыдущий срез и завершённый план сохранены в `ebc389b` (`feat: add spec-first audit workflow`) и отправлены в приватный StenHigh/spec-audit. Оба репозитория перед новым шагом чисты.

## Tasks

- [x] Task 1: Зафиксировать `docs/legacy-extraction.md`, corpus, независимые semantic facets/MUST-NOT и SHA-256 до нового кода/извлечения. Шесть синтетических случаев плюс реальная проза из собственной спецификации. Определить candidate/review JSON и границы доказательства; не превращать число Markdown-узлов в знаменатель норм. Документы не исполняют команды и не ведут логи.
- [x] Task 2: Добавить минимальный test-only контроль в `tool/legacy_test.go`, переиспользуя strictJSON, requiredJSON, lineQuote и digest. Проверять baseline, ID, источник, точную цитату и полный учёт reviewer mappings. Негативные случаи: malformed/unknown/duplicate/null, stale hash, неверный диапазон/текст/путь, пропущенная/лишняя оценка. `go test -v` сообщает только границы и счётчики. Новых CLI-команд, библиотек и semantic word-matching нет. Зависит от 1.
- [x] Task 3: Два свежих агента получают только corpus и контракт извлечения, без gold, кода и старых ответов. Сохранить ответы без правки в `.local/legacy-control/`; проверить машинно, затем отдельный reviewer сопоставляет все gold facets и кандидаты. Механически посчитать его оценки; разногласия/ошибки не исправлять под видом исходного результата. Зависит от 2.
- [x] Task 4: Выполнить полный `go test`/`go vet`, проверить документацию/ссылки и отсутствие изменений SMSPlace. В README кратко указать реальный результат и ограничение; этот checklist завершить по фактам. Не создавать wrap-up markdown, не объявлять готовность массовой миграции ТЗ по одному проходу. Зависит от 3.

## Проверка завершения

На host macOS ARM64: `go test ./tool -count=1`, `go vet ./tool`, документированная сборка `go build -o bin/spec-audit ./tool`, gofmt и frozen SHA-256 — успешно. Начальная общая команда `go build ./...` встретила каталог `tool` вместо выходного бинарника; применена существующая команда из README, код ради этого не менялся.

Оба исходных извлечения технически приняты без правки. Отдельный reviewer составил свою инвентаризацию до доступа к gold/ответам; затем оба review прошли полный машинный учёт: preserved 14/14, supported 12/12 и 11/11 соответственно; partial/missing/overstated/unsupported/duplicate/gold_objections — нули. Сырые данные и транскрипция счётчиков — `.local/legacy-control/`. Семантический результат относится только к этому набору; обычный `go test` без переменных не запускает агентов и явно пропускает проверки внешних ответов.

Рабочий CLI, зависимости, skills и baseline v0.1 не менялись. Ссылки и документация сверены; SMSPlace `git status`, рабочий и staged diff чисты. Новый эксперимент остаётся незакоммиченным. Следующая граница для проектирования: как хост принимает кандидатов в устойчивый адресуемый индекс и как сохраняется происхождение при правках ТЗ; не объявлять кандидаты полным индексом автоматически.
