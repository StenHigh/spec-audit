# План: принятый индекс и ограниченный бизнес-пилот

Branch: codex/bootstrap
Created: 2026-09-15

## Original Request

Ок, давай разберемся со спецификацией как ты говоришь по поводу ISO мы должны писать так что бы потом было просто сделать нашу спецификацию соответствующей ISO. Как разберемся с документацией продолжай разработку дальше по плану как ты писал ранее. Задавай вопросы если нужен мой явный ответ, но не останавливайся с отчетом в ожидании. Продолжай самостоятельно. Задач у нас много а времени мало и просто ждать меня когда есть задачи ненужно.

Предыдущий запрос этого плана: «отлично, переходи к следующим этапам и проведи их самостоятельно, в конце такой же отчет».

## Settings

- Testing: yes — независимые сценарии до кода, lifecycle/negative/concurrency/compatibility, реальное самоприменение через существующий pipeline.
- Docs: yes — контракт принятия, минимальная актуализация README, основного контракта и launcher/protocol. Frozen legacy baseline не менять.
- Logging: существующий LOG_LEVEL/JSON stderr; только операция/причина отказа, без raw/цитат/секретов.
- Working root: /Users/sh/PhpstormProjects/spec-audit. SMSPlace строго read-only; никаких установок skills, конфигов, артефактов или issues там.
- Autonomous: продолжить этапы 9–12 по запросу «хорошо давай дальше так же самостоятельно» без повторного запроса режима; предыдущие изменения сохранить. Не коммитить и не отправлять изменения без отдельного запроса. Task 7 остаётся открытым до отдельного сопоставимого контроля старого процесса.

## Requirements Reconciliation

Authority: решения владельца → docs/tool-spec.md и новый явно ограниченный контракт принятого индекса → независимые сценарии → реализация. Старые baseline сохранены в Git; завершённый legacy-контроль — 940a261.

| Сочетание | Решение и проверка |
| --- | --- |
| Declared / accepted | Необязательный accepted-режим; старый профиль и сериализация snapshot без него неизменны. Прежние run читаются. |
| Candidate / requirement | Хост явно принимает/отклоняет/откладывает каждый кандидат; двоичный валидатор не принимает смысловых решений. |
| Источник / редакция | Адрес и snapshot отдельны от постоянного ID/редакции. Перенос требует явной перепривязки; байтовое изменение принятого содержания — новая редакция. |
| Split / merge / retire | Явная атомарная операция, новые ID потомкам, предшественники и причина сохраняются; исчезновение текста не отменяет норму. |
| Provenance / semantic truth | Все точные фрагменты, исключения/вопросы сохраняются. Полнота цитат по смыслу и правильность решения проверяются хостом. |
| Runtime / evidence reuse | Та же цепочка scopes/tasks/assertions/report; никакого автоматического переноса assessment/receipt в новый run. |
| Небольшой срез / будущий масштаб | Один ограниченный JSON-журнал в reports_dir, stdlib/имеющиеся helpers, без БД/кэша/нового audit engine. Полный пакет до 64 кандидатов; частичная инкрементальная приёмка больших корпусов вне этого среза. |
| Усиление проверок / изменение нормы | docs/accepted-index.md остаётся frozen; точные bytes, head, поля и границы проверяются дополнительными assertions в tool/accepted_test.go. Исторический snapshot берётся из сохранённого старого run, не пересчитывается как ожидание текущим кодом. |
| Прикладной пилот / продуктовая миграция | docs/smsplace-adoption.md REQ-SMS-001…006: один небольшой scope, исходное /TZ — авторитет, код/тесты read-only. Ни issues, ни установки skill, ни изменения CI/trace. |
| Независимость / доступность контекста | Независимая инвентаризация норм до просмотра извлечения; mapper/redteam получают свой TaskBatch и очищенную копию источников, не старый trace. Legacy-сравнение только после фиксации нового результата и с той же редакцией источников. Ограничения свежести старых CELL явно сохраняются. |
| Связь с тестом / исполнение | В пилоте runtime=none: реальные assertions читаются, PHP/Artisan и тесты общей БД не запускаются. not_recorded не означает failed. |
| Затраты / неподтверждённая экономия | Измеряем wall time, объём выбранных файлов/пакетов и сырых ответов; токены, стоимость и ускорение относительно старого подхода не выдумываем. По одному scope не делаем статистического вывода. |
| Норма / контекст / вопрос | Новый отдельный acceptance/context-control.json CC-01…03. Старый raw не меняется, notes — sidecar; два независимых знаменателя. Неопределённая и человеческая обязанность остаётся нормативной. Старый отрицательный gold не правится. |
| Поиск / передача / утверждение | CC-04…07: selector ищет по дереву без trace, complete Task сжимается только lossless quote pool; Result/submit прежние. Семантическая достаточность не следует из точного диапазона. |
| Новый A/B / исторический re-audit | CC-08 сохраняет REQ-SMS-003: одинаковый snapshot новых вариантов достижим, исторический audit snapshot не установлен. Не подменяем одно сравнение другим. |

Два независимых explorer-прохода выполнены по aif-plan: точки интеграции и 12 приёмочных сценариев. Ожидания подготовлены без чтения кода/gold.

## Tasks

- [x] Task 1: До кода закрепить контракт в docs/accepted-index.md и 12 независимых сценариев в acceptance/accepted-index.json, зафиксировать SHA-256. Определить CLI/YAML, полный учёт кандидатов и прежних active ID, ID/revision/lineage, несколько источников, stale/replay/CAS и предел доказательств. Документы не исполняют команды. Основной контракт сохраняет совместимость v0.1.
- [x] Task 2: Перенести уже проверенный legacy-validator в production; добавить минимальную файловую приёмку и подключить активные нормы к существующим snapshot/assign/submit/report. Новые зависимости не нужны. Строгая схема, лимиты до публикации, atomic write и один lock на индекс; LOG_LEVEL остаётся общей точкой диагностики. Проверить lifecycle, отказ без частичной записи, разные редакции/источники, точные цитаты, replay и конкурентные решения. Зависит от 1.
- [x] Task 3: Пройти самоприменение на спецификации инструмента: независимое извлечение, явное решение хоста, задания mapper/redteam, неизменённые raw, фактический запуск разрешённых Go-тестов и JSON/HTML. Данные только в .local; не подменять semantic-review зелёным unit test. Актуализировать launcher/protocol через skill-creator, без установки в SMSPlace. Зависит от 2.
- [x] Task 4: aif-verify: полный go test/vet/build, race для конкурентного индекса, frozen baseline, совместимость прежних run, документация/ссылки, чистота SMSPlace. README отражает реальный результат и ограничения, checklist закрывается по фактам. Без wrap-up Markdown, автокоммита/push и обещаний готовой миграции всего /TZ. Зависит от 3.
- [x] Task 5: Усилить tool/accepted_test.go: точное сохранение raw/decision с пробелами/CRLF, всех semantic fields и причин; отдельная verification в HTML; независимые seed/head и исторический declared snapshot; достижение 128 commits и 32 MiB при append с отказом без изменения журнала; явный stale CAS при пустом active. Переиспользовать helpers, без новых runtime dependencies. Диагностика t.Fatal/t.Log, без вывода payload. Источники — frozen accepted contract и host-reconciliation прошлого этапа.
- [x] Task 6: Зафиксировать один небольшой scope SMSPlace и чистую локальную копию выбранных ТЗ/кода/тестов в .local/sms-pilot; exact origin/clean hashes и line-preserving redaction trace-подсказок. Независимый эталон норм и независимое извлечение; полный host review до reconcile. CONFIG и все выходы только в spec-audit, LOG_LEVEL остаётся общим. Зависит от 5. Выполнено: 29 кандидатов, 28 приняты, одно пояснение отклонено как отдельная обязанность реализации. Формальный контроль не пройден из-за смешения нормативных и контекстных строк в gold; исходный отрицательный результат и три возражения сохранены, не заменены новым знаменателем.
- [ ] Task 7: Через существующий accepted pipeline выполнить новый mapper/redteam по принятому scope, сохранить raw без правки, валидацию и JSON/HTML. Использовать существующие legacyValidate/legacyCount через явно запускаемый TestExternalExtractionReview в tool/legacy_test.go, не новый production CLI и не изменение frozen baseline. После фиксации ответов сравнить с независимой инвентаризацией и имеющимся trace на той же редакции; различать confirmed defect / weak assertion / unknown и не создавать бизнес-задачи. Runtime none; wall time/bytes считаются командами, недоступные usage/cost помечаются unavailable. Зависит от 6. Частично выполнено: два raw приняты без правки, отчёты и согласование хоста сохранены, wall time/bytes измерены. Равное A/B-сравнение не выполнено: старая ячейка другой редакции, точный audit snapshot не установлен. REQ-SMS-003 не объявляется пройденным; оставшаяся часть требует нового сопоставимого контрольного запуска, не переписывания старого результата.
- [x] Task 8: Проверить go test/vet/race/build, frozen baseline, документацию, отсутствие изменений SMSPlace. Через aif-docs обновить README и существующую спецификацию применения фактическим результатом; ограничить следующий шаг по данным пилота, не реализовывать масштабирование заранее. Закрыть checklist по фактам, выдать лаконичный человеческий отчёт. Зависит от результатов 7. Выполнено с warn: тесты механики проходят, отрицательный смысловой контроль и незавершённое равное сравнение сохранены как ограничения приёмки.

## Продолжение: типизация и контекст

- [x] Task 9: Зафиксировать до кода acceptance/context-control.json и SHA-256, восемь независимо предложенных сценариев. В .local/context-control подготовить неизменяемые источники/протокол, независимый spec-only gold и поиск адресного контекста без старых выводов. Диагностика подготовки — counts/hashes/ограничения, без payload в общих логах. Источники: CC-01…04 и требования применения. Выполнено: пин контракта, 2462 неизменяемые проекции/исходника, gold до extraction, первый selector и синтетический поиск вне стартового файла. Ограничения очистки истории зафиксированы; уточнение selector сохраняется отдельным результатом.
- [x] Task 10: В одном tool/context_control_test.go переиспользовать legacyValidate/legacyCount/lineQuote/strictJSON и существующий submit-validator: проверить notes sidecar, раздельный учёт нового gold, lossless Task quote pool, отрицательные/граничные сценарии и прежние свидетельства. До реализации закрепить ожидания; никакого нового production CLI/SDK/dependency. Диагностика t.Log/t.Fatal, без секретов/полных raw. Зависит от 9. Реализовано test-only: unit/integration guards, отдельные opt-in review/packet drivers. Полный Go test/vet и адресный race прошли; внешний смысловой контроль честно отказал (11/12 normative, один notes gold objection).
- [x] Task 11: Провести свежее извлечение и независимый review нового типизированного gold; сохранить raw и отрицательные исходы. Выполнить новый full/compact контроль на одном snapshot и одинаковом принятом наборе со свежими mapper/redteam, явно согласовать результаты после фиксации всех raw. Измерить bytes/wall time, не подменять исторический A/B; все артефакты в .local/context-control. Runtime none, никакого PHP/продуктовых issues. Зависит от 9–10. Контроль выполнен, не объявлен semantic PASS: 11/12 normative, один notes gold objection; 4/4 raw приняты без ремонта. Full/compact implementation 36/36, assertion 31/36; Task меньше на 30,3%. Host-comparison сохраняет все строки и отдельно снимает уверенность в одной общей находке после дополнительного чтения ТЗ.
- [x] Task 12: Проверить go test/vet и адресный race, frozen baseline/историю/свежесть и сохранность SMSPlace; через aif-docs обновить существующие README и docs/smsplace-adoption.md фактическими выводами и следующим ограниченным шагом. aif-verify завершает с честным gate; незавершённые критерии не закрывать. Зависит от результатов 11. Выполнено на macOS ARM64: полный Go test/vet, адресный race, native build и запуск бинарника без PATH; четыре frozen baseline сохранены. Оба run complete/fresh, сверены 2462 пары исходников, 13 входов, четыре raw и три дополнительные цитаты; расхождений нет. SMSPlace чист, PHP не запускался. README и спецификация применения обновлены; смысловой контроль остаётся отрицательным, Task 7 / REQ-SMS-003 открыт. Коммит/push не выполнялись.

## Commit Plan (продолжение)

## Продолжение: спецификация выпуска и согласование аудита

Mode: fast, существующая ветка и PLAN.md, без нового worktree. Testing: yes. Docs: yes через aif-docs. Logging: существующий slog/LOG_LEVEL, только операции/IDs/counts/hash/отказы, без сырых payload. Старые незакоммиченные изменения сохраняются; коммит/push только отдельно. Пользователь разрешил непрерывное выполнение и вопросы параллельно, поэтому типовые остановки skills после документации/плана пропускаются; новые внешние полномочия не предполагаются.

### Requirements Reconciliation — выпуск

Authority: текущие решения владельца → docs/tool-spec.md §12–14 → неизменённый accepted-index и baseline v0.1 → frozen acceptance → код. Исторические отрицательные контроли не меняются. Конкретный новый scope не подменяет Task 7 / A5.

| Сочетание | Поведение / источник | Проверка |
| --- | --- | --- |
| spec-first / ISO | SA-016…021 и §11–14 до кода; ISO-подготовка без заявления conformance | Ссылки, ID, источники, границы и A1…A6; frozen новый контракт |
| новый raw / исторический canonical | §14: точные первые байты, optional hash; legacy явно отмечен | CRLF/semantic duplicate/orphan/tamper/старый fixture |
| host review / pending / fresh / outdated | SA-019…021: полный набор, CAS state+previous review, история | positive/negative/replay/concurrency/retry/receipt/stale |
| применимость / составной assertion / неоднозначная обязанность | SA-016…018; без новых Result labels и автоматического классификатора | Небольшие независимые пары и существующие validators |
| инструмент / SMSPlace | Старый CI сохраняется; без бизнес-записей и PHP | Git status до/после, все выходы только в spec-audit/.local |

- [x] Task 13: Через aif-docs завершить единый вход в спецификацию и границу ближайшего выпуска; сохранить frozen документы, разделить требования/эксперименты/приёмку, уточнить переход с сохранением CI. Файлы docs/tool-spec.md, docs/smsplace-adoption.md, README.md. Диагностика — найденные пробелы и проверка ссылок, без отчёта-дубликата. Источник: текущий запрос владельца, официальные ISO/NASA, §12. Выполнено: цели G1…G4, SA-016…021, A1…A6, открытые решения и точный §14 до кода; ссылки проверены. Навигационные правки frozen документов отложены ради сохранения baseline, ISO-conformance не заявлено.
- [x] Task 14: До реализации закрепить acceptance/release-control.json и SHA, парные синтетические источники/ожидания и механические сценарии §14; в tool/context_control_test.go и tool/release_control_test.go переиспользовать проверку происхождения и полный учёт без изменения старых gold. Различать корректную дополнительную note и потерю нормы; новые эксперименты не меняют legacyCount. Диагностика t.Log/counts/hash, без исходных payload. Зависит от 13. Выполнено: пин a707645d…, парные corpus/expected, отдельный учёт дополнительных notes; адресные Go-проверки прошли до production-расширения. Смысловой запуск остаётся Task 16.
- [x] Task 15: Реализовать §14 в tool/review.go и точках tool/main.go: exact первые raw, optional provenance для совместимости, команда review, отдельный bounded журнал, CAS и report projection. Тесты tool/review_test.go используют существующий corpus/fixtures и проверяют negative/concurrency/history/limits. Зависимости не добавлять; slog Debug/Info операции, stdout JSON. Зависит от 14. Выполнено: новый review, exact raw/legacy provenance и HTML; прежние Go-тесты прошли, отдельные проверки review (включая ровно 32 MiB, 64 записи, CAS и actual Go receipt) прошли. Полная финальная проверка остаётся Task 17.
- [x] Task 16: Через skill-creator обновить только skills/spec-audit/SKILL.md и references/protocol.txt; встроить SA-016…018 и review в один workflow. Свежие ограниченные роли на парных примерах и самоприменении, без gold/чужих ответов; raw/JSON/HTML только .local/release-control. Ни установки skill, ни бизнес-команд. Измерить bytes/wall time; неизвестные usage/cost не выдумывать. Зависит от 15. Выполнено: launcher/protocol обновлены и валидны; четыре свежие роли дали 12/12 заданных оценок, извлечение/reviewer сохранили 3/3 нормы и допустимую дополнительную note. Self-v1 независимо выявил три группы слабых assertions; после адресных регрессий self-v2 дал supported/relevant обеих ролей. Все восемь raw приняты без ремонта, согласования и HTML записаны через CLI. Перепроверка не называется слепой; полный итог/метрики — .local/release-control/study.json.
- [x] Task 17: aif-verify: Go test/vet/race/build, историческая совместимость, frozen hashes, валидация skill и docs links; явно оценить A1…A6 и обновить фактический статус через aif-docs / aif-implement. Не закрывать A5 без одинаковой редакции и OQ-01 без ответа. Зависит от 16. Коммит/push не входят. Выполнено с warn: полный Go test/vet, review race и отдельный race новых регрессий; пять frozen baseline, ссылки и skill validation; четыре сборки. Пустой PATH проверен на macOS ARM64 и Linux AMD64 через Docker-эмуляцию без сети/SMSPlace. Постоянная сборка bin/spec-audit; бизнес HEAD f4ea779… и чистый status сохранены. A1–A4 приняты для заданного среза; A5 / Task 7 и A6 открыты. Два вопроса владельцу заданы параллельно работе; старый процесс не запускался и внедрение не объявлено без новых полномочий.

Группа фиксации после 13–14: docs/spec и frozen acceptance; после 15–17: feat/review и проверенный launcher. Это логические checkpoint, не разрешение на коммит. После общих задач продолжить доступную часть A5; если нужны новые полномочия, запросить их параллельно независимой работе.

После этапа 12 — единая проверенная группа контроля. Коммит/push только по отдельному запросу, автоматических checkpoint-коммитов нет.

## Продолжение: карта проекта, 2026-09-15

### Original Request — карта

Отлично, закрепи в спецификации а потом приступай к реализации. Простой дорого обходится так что сам доведи уту часть до конца.

Mode: fast; текущая ветка. Testing: yes. Docs: yes, существующие README и tool-spec. Logging: slog Debug начала/конца проекции с числами, общий LOG_LEVEL; без payload. Автономно завершить 18–21, без коммита/push. Старые открытые критерии вне этого среза не закрывать.

### Requirements Reconciliation — карта

Authority: запрос владельца → docs/tool-spec.md §15, SA-022…026 → неизменённые §1–10/accepted-index → реализация.

| Сочетание / источник | Проверка |
| --- | --- |
| Declared/accepted × pending/доставка; SA-022…024 | Все manifest-файлы и нормы, multiple citations/scopes, unlinked и вопросы accepted до ответов |
| Current/missing/outdated × fresh/stale; SA-025 | Хост — основа только current, история отдельно, ссылка не скрывает отсутствие текущей связи |
| Requirements/code/tests × переход в обе стороны; SA-023 | Точные диапазоны, уникальные якоря, цитата только хоста, test_id/receipt отдельно |
| Поиск/фильтр/прямая ссылка/без JS; SA-024…026 | Комбинации и пустой результат, раскрытие скрытой цели, клавиатура/узкое окно |
| Недоверенный текст/полный инвентарь; SA-022,026 | Экранирование, собственные якоря вместо URL из путей, большой набор без новых зависимостей |
| Реальный сохранённый run / исторические контрольные файлы | Повторная генерация в отдельной .local-копии без изменения старых raw/отчётов; SMSPlace чист |

Два read-only explorer-прохода проверили точки интеграции и граничные сценарии. Карта строится после HostReview; состояние/форматы аудита не меняются. Недостаточность данных показывается явно, автоматический поиск всех конфликтов и неописанного поведения не заявляется.

- [x] Task 18: До кода закрепить SA-022…026 и приёмку в docs/tool-spec.md, текущий план. Без создания документа-дубликата; диагностика — сверка областей и ограничений.
- [x] Task 19: Добавить производную карту после присоединения HostReview: весь inventory, deduplicated citations, обратные связи с происхождением/актуальностью, flags по текущему основанию. Файлы tool/main.go, tool/report.go; snapshot/state/Result/ReviewDecision не менять. Переиспользовать расчёт последнего receipt, slog Debug с числами. Проверки в tool/report_test.go до завершения задачи. Выполнено: адресные Go-регрессии прошли на хосте macOS; 25000 файлов, pending accepted-вопрос, host-only current/outdated/stale, shared fragment и сохранённые цитаты.
- [x] Task 20: Сделать автономный HTML с обзором, фильтрами, карточками/якорями, историей и точными фрагментами; вынести прежний template в tool/report.html для читаемости, embed в бинарник. Stdlib/native controls, без CDN/Node/server. Обновить лишь устаревший запрет любого script в существующем тесте, сохранив проверку XSS. Проверить текущий/устаревший/неполный отчёт и все внутренние ссылки. Зависит от 19. Выполнено: автономный template, экранирование, разрешение всех якорей для roles/current/outdated/stale, host/role disagreement и историческая совместимость прошли в полном Go-прогоне. Браузерная функциональная проверка остаётся Task 21.
- [x] Task 21: Реальный render через CLI в отдельной .local-копии; браузерный поиск/фильтры/ссылки/узкий экран, Go test/vet/race и macOS/Linux сборки. aif-docs — короткий сценарий в README; aif-verify — фактическая проверка SA-022…026, frozen baseline и чистоты SMSPlace. Не изменять исторические результаты и не создавать бизнес-артефакты. Зависит от 20. Выполнено: полный Go test/vet, адресный race, четыре сборки; нативная генерация с пустым PATH на macOS ARM64. Linux/Intel в этом этапе только собраны. Реальный HTML в .local/navigation-control/reports/same-edition: 2506 файлов, 16 норм, fresh/current, ни одного битого/повторного якоря. Собственная спецификация: 26 норм и 19 файлов, pending без выдуманных оценок. Исходные raw/state/manifest/review и 13 хэшей исторических артефактов сохранены; frozen contracts/skills/dependencies и SMSPlace не изменены. README/ссылки проверены; синтаксис JS проверен уже установленным Node только как dev-check, бинарнику он не нужен. Браузерная часть принята владельцем после ручного просмотра HTML: «я посмотрел все хорошо с html». Предыдущий блокер приёмки снят этим подтверждением. Автоматический браузерный прогон не выполнялся из-за запрета file URL; отдельные сценарии клавиатуры/узкого экрана не выдаются за проверенные агентом. Коммит/push не выполнялись.

## Результат accepted-index — предыдущий срез, 2026-09-15

- Контракт и 12 независимых сценариев остались побайтно равны пинам до реализации. Старые baseline не менялись; новых зависимостей нет.
- Самоприменение: 40 принятых требований, два ответа на каждое, свежий `self-v2`, девять фактически прошедших тестов в receipt. JSON/HTML и согласование хоста — `.local/accepted-index/reports/`.
- Две роли нашли общий дефект атомарной записи: ошибка могла возвращаться после публикации. Реальный контрпример воспроизведён до исправления; общая функция исправлена и защищена двумя регрессиями. Старые raw и stale-run сохранены.
- Полный Go test/vet, адресный race и сборки четырёх OS/arch прошли. Реальное исполнение без runtime: macOS/Linux ARM64; x86_64 только собран. Проверки Go выполнялись на хосте, Linux binary — в отдельном read-only контейнере без сети. SMSPlace не монтировался и остаётся чистым.
- Реальный прежний declared-run читается с исходным snapshot. Отдельный CLI-контроль stale CAS на пустом active-наборе отказал без изменения журнала.
- Verify: warn, без блокеров реализации этого среза. Шесть требований имеют unknown хотя бы одной роли, 15 — weak/missing assertion; это не 15 подтверждённых дефектов. Первый self-run раскрыл ролям часть accepted history; launcher ограничен TaskBatch, повторный проход не объявляется слепым.
- По результатам назначены этапы 5–8 выше. Частичная приёмка больших корпусов вне реализации; предел полного пакета — 64 кандидата. Никаких задач или артефактов в SMSPlace не создано; коммит/push не выполнялись.

## Результат усиления проверок и бизнес-пилота, 2026-09-15

- Точные bytes/history/hash и реальные границы append защищены тестами; три намеренные мутации пойманы. Полный go test/vet и адресный race прошли на macOS ARM64; четыре сборки прошли, текущий native binary работает без PATH. Linux в этом этапе только пересобран, прежнее исполнение не выдаётся за новый запуск.
- Независимое извлечение: 29 кандидатов, 28 приняты хостом. Формальный внешний контроль красный из-за классификации эталона; исходный результат не подменён скорректированным знаменателем.
- Mapper/redteam: 2/2 ответов без ремонта raw, восемь разногласий согласованы явно. Один общий статический контрпример, девять unknown по реализации; PHP и продуктовые тесты не исполнялись. JSON/HTML и полный итог — в .local/sms-pilot/reports/.
- Позднее сравнение со старым trace обнаружило два пропущенных при сборке контекста тестовых файла и несопоставимость редакций. Missing не превращён в бизнес-задачи; равный A/B остаётся открытой частью Task 7. Экономия денег/токенов и преимущество точности не заявлены.
- Verify: warn, без блокеров механики этого среза, но приёмка для массового применения не пройдена. Frozen baseline и все 26 исходных файлов сохранены; SMSPlace чист. Новых зависимостей, бизнес-артефактов, issues, коммита или push нет.
- Следующий срез: небольшой spec-first контроль разделения норм/контекста/вопросов, достаточности и компактности адресного контекста, затем повтор на одинаковом snapshot. Масштабирование и миграция пока не реализуются.
