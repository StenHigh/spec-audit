# План: spec-first вертикальный срез spec-audit v0.1

Branch: codex/bootstrap
Created: 2026-09-15

## Original Request

Да иди самостоятельно по всем этим пунктам.

Убедись только что эти скилы не оставят артефакты в бизнес проекте smsplace

## Settings

- Testing: yes — автоматические проверки контракта, независимые семантические примеры и сквозной прогон.
- Logging: verbose через LOG_LEVEL; структурный stderr, без содержимого исходников, командных секретов и ответов агентов.
- Docs: yes — обновить README, создать спецификацию инструмента и отдельную спецификацию применения к SMSPlace.
- Working root: /Users/sh/PhpstormProjects/spec-audit. Любые артефакты — только здесь; SMSPlace read-only.
- Autonomous: пользователь разрешил пройти все этапы; промежуточные вопросы о режиме, документации и продолжении не требуются. Новые внешние публикации/релизы не выполняются.

## Requirements Reconciliation

Authority: запросы владельца → спецификация инструмента, написанная до реализации → контракт приёмочных примеров → план. Прототипы и исторические отчёты — технические свидетельства, не нормативный источник.

| Решение / сочетание | Основание | Проверка |
| --- | --- | --- |
| Go-бинарник; AI вызывает текущая сессия; PHP только project Docker | Согласованная модель владельца; README | Сквозной запуск CLI без модельного API; проверка container mount |
| Норма / пример; статический assertion / реальный запуск; завершение заданий / полнота требований | Выводы пилотов и запрос на улучшение spec-first | Независимые примеры до реализации; явные состояния отчёта |
| Новый каталог / тот же источник; изменение источника / устаревший run | Проверенный недостаток prototype snapshot identity | Переносимость, stale/retry и неправильный snapshot |
| Спецификация инструмента отдельно от внедрения в SMSPlace | Явное решение владельца | Раздельные документы; исходный бизнес-проект без изменений |

Пилоты доступны локально в `.local/research/`; они не копируются в нормативные требования и не загружаются в Git. Полный текст ISO 29148 не получен: используем ISO-ориентированную структуру и открытые рекомендации NASA/INCOSE, без заявления о формальном соответствии.

## Tasks

- [x] Task 1: До реализации создать `docs/tool-spec.md` с назначением, требованиями, контрактом доказательств, интерфейсами, ограничениями и приёмкой; `docs/smsplace-adoption.md` — отдельные требования безопасного применения. Подготовить независимый от новой реализации синтетический corpus и зафиксировать его ожидания. Документы не выполняют команды и не ведут логи; обоснования отделены от норм.
- [x] Task 2: Собрать один CLI из переиспользуемых механизмов прототипов: Markdown → адресуемые требования → scope × role → проверенный ответ → состояние доказательств. Добавить переносимый YAML/snapshot, точный учёт требований, SDK/receipt границу и JSON/HTML. Файлы реализации и тестов — только в этом репозитории. Логи границ операций DEBUG/INFO/ERROR в stderr, включаемые LOG_LEVEL; никаких исходников и секретов в логах. Зависит от 1.
- [x] Task 3: Автоматизировать frozen acceptance cases и негативные проверки; выполнить реальный ограниченный проход независимыми ролями и самоприменение к спецификации инструмента. Обязательные случаи: зелёный слабый тест, зелёный тест дефектного поведения, пропущенная обязанность, неоднозначная норма, stale evidence, retry/неполный run. Результаты исполнения — только `.local/`; summary в CLI без выдуманных PASS/usage. Зависит от 2.
- [x] Task 4: Прогнать build/test/vet и переносимость macOS/Linux в доступной среде; проверить README/ссылки/границы доверия; исправить найденные дефекты и сверить итог с требованиями. Проверить, что SMSPlace не изменён и туда не созданы артефакты. Обновить только существующий README и этот checklist; не создавать отдельный wrap-up markdown. Зависит от 3.

## Контроль границ

Приёмка зафиксирована до изменения CLI: `acceptance/baseline.sha256`, пять независимых случаев. Начальный SHA-256 спецификации инструмента: `ea0f54f1363a44f08595e801cf12cbf1de264e85c8f70d85609028468a444c95`; изменения после этой точки требуют объяснения, не подгонки ожиданий.

Уточнения спецификации после baseline: REQ-SA-015 явно говорит о host-записях, согласуя норму с уже описанным контейнерным /tmp для JUnit; правило однострочных полей объясняет отделение поясняющей прозы пустой строкой. Приёмочные исходники и gold не изменены.

Самопроверка self-v1 выявила неполный workdir у служебных PHP-вызовов, раздувание отчёта повторными summaries и слабые проверки полей Markdown/нативного CLI. Исправления проверяются отдельными регрессиями; исходные ответы self-v1 сохранены без изменения. Уточнение §7 задаёт компактную проекцию последнего исполнения без потери общей истории. В §8 добавлена защитная сериализация PHPUnit одного reports_dir; она дополняет, но не заменяет уже назначенную хосту координацию общей БД. Не заявляем межконфигурационную изоляцию.

В повторном self-v2 redteam нашёл коллизию имён per-run и общего PHP-lock при RUN_ID=phpunit. Процессная регрессия воспроизвела зависание до runtime. Общий lock получил зарезервированное имя, невозможное для per-run lock; тот же тест проверяет исправление. Это исправление реализации, норма не ослабляется; исходный self-v2 сохраняется как история.

Начальное состояние SMSPlace: `git status --porcelain=v1 --untracked-files=all`, `git diff --exit-code`, `git diff --cached --exit-code` — чисто. Не вызывать skill-скрипты с рабочим каталогом SMSPlace; его инструкции можно только читать по абсолютным путям. Исторические прототипы не удалять: использовать как референс или явно переносить с сохранением истории.

## Проверка завершения

- `go test -count=1 ./tool`, `go vet ./tool`, skill validator и frozen SHA-256 — успешно на host macOS ARM64. Итоговый тест содержит воспроизведённый ранее RUN_ID=phpunit.
- blind-v1: 10/10 assessments совпали с пятью независимыми ожиданиями; self-v1/self-v2 сохранили исходные findings, их исправления проверены отдельно. Это не сертификат и не оценка точности на реальном корпусе.
- Все автономные команды реально выполнены с пустым PATH на macOS/Linux ARM64; Darwin/Linux amd64 — только cross-build. Реальные SDK, PHPUnit filter/empty/timeout — в собственном read-only, no-network контейнере; контейнер удалён, артефакты сохранены локально.
- HTML проверен генерацией и escaping assertions; визуальный просмотр ограничен политикой браузера, обход не выполнялся. SMSPlace Git status/diff остались чистыми; установка skill, коммит, push и изменение GitLab не выполнялись.
