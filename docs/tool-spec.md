[README](../README.md) · [Принятый индекс →](accepted-index.md)

# Спецификация spec-audit

Редакция документа: 1.43, 2026-09-19. Владелец продукта — StenHigh. Разделы 1–10 сохраняют контракт реализованного v0.1; разделы 12–16 задают рабочий выпуск, статус приёмки — §12.4. Редакция 0.6 восстановила план PHP/Laravel SDK (§17); 0.7 добавила §18 — поставку через подписанный релиз; 0.8 добавила §19 — уроки чистой сессии (`validate`, журнал версий бинарника run, ответ `reconcile` после apply, протокол роли) по решению владельца 2026-09-16; 0.9 добавила §20 — справочные источники ТЗ и деление большого набора по решению владельца 2026-09-17; 1.0 добавила §21 — уроки второго прогона (`check` без записи, подсказки сопоставления, изоляция роли) по решению владельца 2026-09-17; 1.1 добавила §22 — уроки третьего прогона (`reanchor`, `unique_shared`, диагностика `scopes`, признак `base_index_current`) по решению владельца 2026-09-17; 1.2 добавила §23 — облегчённую форму host DECISION (version 2) и список ролей в `review` по решению владельца 2026-09-17; 1.3 добавила §24 — уроки четвёртого прогона (таблица расхождений `outcomes[]`, черновик решения `draft`, материализация `dispatch/`) по решению владельца 2026-09-17; 1.4 уточнила подпись свидетельств хоста (§23, §24.3) и добавила §24.4 синонимы `help`/`version` и §24.5 семантику `--dir` у `skill install|update` по решению владельца 2026-09-17; 1.5 добавила §25 — уроки пятого прогона (решение version 3 с цитатами хоста, limitations ролей и прошлый вердикт хоста в `outcomes[]`) по решению владельца 2026-09-18; 1.6 добавила §26 — мелочи пятого прогона (`freshness` в `index`, подсказка о якорях ТЗ вне snapshot, общий `dispatch/files.json`) по решению владельца 2026-09-18; 1.7 добавила §27 — уроки шестого прогона (проверка решения хоста без записи, statement прошлого решения и разность цитат ролей в `outcomes[]`) по решению владельца 2026-09-18; 1.8 добавила §28 — мелочи шестого прогона (списки всегда присутствуют, протокол mapper по веткам, пустой statement при совпадении с ролями — уточнение §23) по решению владельца 2026-09-18; 1.9 добавила §29 — уроки седьмого прогона (чтение решения по одной норме, симметричный ответ `review … DECISION`) и правило assertion при relevant и contradicts в §14 по решению владельца 2026-09-18; 1.10 добавила §30 — мелочи седьмого прогона (промпт роли и протокол от `prepare`, индекс цитат `review … citations`, счётчики доставки в `prepare`/`tasks`) по решению владельца 2026-09-18; 1.11 добавила §31 — точную цитату `cite`, журнал `validate` роли в промпте и политику постороннего симлинка (пункты 8, 11 четвёртого и 6 шестого прогона; очередь ведётся автономно по поручению владельца 2026-09-18); 1.12 добавила §32 — сводки `index CONFIG summary` и `review CONFIG RUN_ID summary` (пункт 7 четвёртого прогона); 1.13 добавила §33 — уроки восьмого прогона (индекс якорей `anchors`, фильтры индекса цитат, предупреждение о смене версии в run, текстовая форма `review … REQ-ID text`); 1.14 добавила §34 — обзор нескольких scope `overview CONFIG...` (первый шаг этапа «несколько разделов и обзор корпуса»); 1.15 добавила §35 — уроки девятого прогона (уточнения промпта роли, краткая форма `review … REQ-ID brief`); 1.16 добавила §36 — уроки чистого синтетического прогона 0.1.22 (состояния вердикта хоста в навигации отчёта, unknown вместо missing при выносе за срез, список contradicted в `overview`); 1.17 добавила §37 — уроки десятого прогона (вид и вложенные ссылки определений в `anchors`, `index … summary` без журнала приёмки, `brief` без запусков); 1.18 добавила §38 — уроки одиннадцатого прогона (остаток пакета в ответах, определения вне snapshot и вложенные якоря в `anchors`, `oversize_reason` у scope); 1.19 добавила §39 — второй пакет на том же scope: операция `keep` (REQ-SA-048) и диапазоны якорей в `anchors`; 1.20 добавила §40 — уроки двенадцатого прогона (`previous_host` по содержанию нормы, `pending_ids` в `tasks`, VALIDATE только заданной командой, шум `matches` во втором пакете); 1.21 добавила §41 — уроки тринадцатого прогона (имена полей в отказе формы, `ambiguous_over_clear` в `outcomes[]`, `likely_duplicate` в `matches`); 1.22 добавила §42 — уроки четырнадцатого прогона (подсказка дубликата по словам, кросс-scope память `contradicted_code` в `overview`, допустимые диапазоны в отказе цитаты); 1.23 добавила §43 — уроки пятнадцатого прогона (`related` в CONFIG и `contradicted_elsewhere` под нормой); 1.24 добавила §44 — уроки шестнадцатого прогона (память только по противоречащим цитатам, свёртка в `brief`, предел словесной меры, имя поля в отказе типа, `prepare` в заранее созданный каталог); 1.25 добавила §45 — сводная HTML-карта корпуса `corpus OUT_HTML CONFIG...` по решению владельца 2026-09-18; 1.26 добавила §45.1 — счётчик GAP по классам (решение владельца 2026-09-18: добавить, не менять); 1.27 добавляет §46 — DECISION приёмки version 2 с сужением statement (REQ-SA-049) по решению владельца 2026-09-18; там же зафиксировано решение оставить `keep` (§39) вместо multi-raw пакета. 1.28 добавила §47 — динамика GAP между решёнными run (`delta`, `baseline_run`) на карте корпуса по решению владельца 2026-09-18; 1.29 добавляет §48 — разбор внешнего review 2026-09-18 (полнота свидетельств для version 2, права до commit point `update`, отмена дочерних процессов по сигналу, мелочи) с перечнем отложенного. 1.30 добавляет §49 — публикация статического сайта для заказчика `publish OUT_DIR CONFIG...` и история run на карте корпуса по решению владельца 2026-09-18. 1.31 добавила §50 — инкрементальный run `prepare … since PREV_RUN` (REQ-SA-050) по решению владельца 2026-09-19; 1.32 уточнила §40.1 — `previous_host` сохраняется при изменившихся источниках с признаком `same_files: false` (урок перепрогона после фикса); 1.33 добавляет §51 — точность инкрементального run по уроку первого живого перепрогона (`same_files` по файлам нормы, перенос spec-gap, `changed` по цитируемым строкам, регруппировка заданий, `plan`, причины в outcomes и промпте). 1.34 добавляет §52 — перенос известных дефектов и детализация причин (урок инкремента finance: критерий ≤ ⅓ выполнен). 1.35 добавляет §53 — ухудшения внутри GAP в динамике и предупреждения перед решением (урок инкрементов пяти scope). 1.36 добавляет §54 — `rerun RUN_ID CONFIG...`: инкрементальные run по всем scope одной командой (milestone «Перепрогон затронутых scope одной командой»). 1.37 добавляет §55 — журнал индекса version 2 с классификацией источников: перенос файла между `specs` и `references` делает индекс stale (review 2.1, milestone P1.3). 1.38 уточняет §18 через §56 — релизная квалификация полным набором тестов, Actions по SHA, строгий `update` в квалификации, литеральный вектор `counts` (review 2.5). 1.39 уточняет §18 через §57 — свежая копия skill собирается в staging-каталоге и публикуется одним rename: прерванная установка не оставляет каталога без receipt (review 2.3). 1.40 добавляет §58 — фильтр фактов экспортёра по множеству `files` и контейнерная приёмка трейта вне запрошенного множества (review 2.7). 1.41 добавляет §59 и уточняет §16.1 — владелец нормы в производной сводке отчёта — первая нормативная цитата, справочный файл норм не считает (review 2.6). 1.42 добавляет §60 — бенчмарки большого run и кэш проверенных источников в пределах команды (review 4). 1.43 добавляет §61 — состав контекста роли по артефактам пилота и `dispatch/files.txt` вместо чтения `files.json` ролью (milestone «Токены ролей»). Текущие форматы, §6 и `init` не меняются. Это не заявление о полной приёмке или ISO-conformance. Изменение требований требует явного обоснования; зелёный тест не разрешает подгонять норму под реализацию.

Точка входа в нормативный комплект — этот документ и явно связанные контракты. [Принятый индекс](accepted-index.md) сохраняет свой frozen baseline. [Применение к SMSPlace](smsplace-adoption.md) задаёт только ограничения применения, не нормы инструмента или продукта. Эксперименты, README и план — свидетельства и навигация, не новые требования. При конфликте сначала применяется явное решение владельца; неразрешённый конфликт документов нельзя молча разрешить кодом.

Расширение [принятого индекса](accepted-index.md) добавляет отдельный `index_mode: accepted` и `reconcile`. Без этого поля действует прежний declared-профиль и совместимость его run; правила ниже не требуют переписывать legacy-ТЗ в REQ-блоки.

## 1. Зачем нужен инструмент

Менеджеру недостаточно знать, что рядом с требованием найдены код и тест. Тест может проверять другую ветвь, использовать слабое сравнение или закреплять дефект. Нужен отчёт, который показывает, какая обязанность рассмотрена, на чём основан вывод и чего для уверенности не хватает.

Первый выпуск проверяет этот подход на небольшом разделе, а не обещает автоматически сертифицировать весь продукт. Пользователь задаёт источники и ограничения в YAML, запускает бинарник вручную или через skill. Смысловой анализ выполняют независимые агенты существующей сессии; бинарник проверяет происхождение данных, ведёт состояние и формирует JSON/HTML.

Полезное отличие от списка trace-связей: отдельные состояния требования, реализации, релевантности assertion и фактического исполнения. Недостаток тестов не равен дефекту продукта.

## 2. Область и исключения

В срез входят: локальный конфиг; структурное чтение Markdown; объявленные адресуемые требования; scope × mapper/redteam; точные цитаты; собственные receipts запуска тестов; PHP syntax-only SDK в Docker; возобновление/устаревание; один отчёт из общей модели данных.

Не входят: модельные API и ключи в бинарнике; автоматическое создание issues; изменение проверяемого кода или ТЗ; массовая миграция старого ТЗ; сервер/БД; граф зависимостей; универсальная система плагинов; автоматический сертификат соответствия; доказательство экономии токенов.

Обычный Markdown без профиля можно структурно прочитать, но нельзя объявить автоматически полным индексом обязательств. В baseline v0.1 семантическая атомизация отсутствует; ограниченная приёмка кандидатов теперь задаётся отдельным контрактом расширения. v0.1 испытывается на явно размеченной новой спецификации и независимых примерах. Это не требование переписать SMSPlace.

## 3. Термины и ответственность

| Термин | Значение |
| --- | --- |
| requirement | Объявленная норма с постоянным ID, условием, требуемым результатом и способом проверки |
| scope | Ограниченное множество requirement IDs, назначенное каждой из двух ролей |
| snapshot | Относительные пути и хэши выбранных источников плюс версия протокола/профиль; не копия файлов и не ОС-песочница |
| assessment | Суждение агента об одной норме с цитатами и ограничениями; не машинно доказанная истинность |
| assertion | Конкретная проверка ожидаемого результата в тесте; имя теста, комментарий и @tz её не заменяют |
| receipt | Запись фактически запущенного бинарником тестового процесса, привязанная к snapshot и runtime |
| stale | Источники или смысловая конфигурация отличаются от snapshot; прежние выводы доступны только как история |

Автор спецификации определяет нормы. Хост выбирает scope, разрешает запуск SDK/тестов и согласует противоречия. Mapper систематически сопоставляет нормы, код и assertions; redteam независимо ищет опровержения. В v0.1 окончательное согласование остаётся вне автоматического вердикта CLI.

## 4. Профиль Markdown

Это локальный формат проекта, не синтаксис, предписанный ISO. Норма — top-level заголовок третьего уровня `### REQ-<NAMESPACE>-<NUMBER> — <название>`, где namespace состоит из заглавных латинских букв/цифр, number содержит не меньше трёх цифр. Под ним ровно по одной непустой однострочной записи `Условие:`, `Требование:`, `Проверка:`. Заголовки в code fence или blockquote — примеры, не нормы. Следующий top-level заголовок уровня 1–3 завершает блок. Остальная проза — пояснение, не дополнительный ручной индекс.

`Требование:` описывает один наблюдаемый результат. Условия, исключения и границы должны быть явными; измеримые критерии предпочтительнее слов «быстро» и «удобно». Смысловую атомарность и проверяемость оценивает человек/агент: структурная валидация этого не доказывает. ID не меняется от перемещения строки; изменение нормы меняет её content hash. Один ID не может принадлежать двум scope данного run.

## 5. Требования первого среза

Поля могут идти подряд в одном Markdown-абзаце. Пояснение отделяется пустой строкой: продолжение поля на следующей строке не принимается как однострочная запись профиля.

### REQ-SA-001 — Безопасная инициализация

Условие: пользователь вызывает `init CONFIG`.

Требование: инструмент должен создать шаблон YAML только по отсутствующему пути, не перезаписывая существующий файл или символическую ссылку.

Проверка: повторный init отклонён, байты исходного конфига не изменились.

### REQ-SA-002 — Однозначная конфигурация

Условие: операция читает YAML-конфиг.

Требование: инструмент должен отклонить неизвестные/повторные поля, несколько YAML-документов и недопустимые источники согласно контракту раздела 6.

Проверка: негативные варианты конфигурации не создают готовый run и не изменяют прежние результаты.

### REQ-SA-003 — Переносимая идентичность

Условие: одинаковые выбранные файлы, профиль и назначения scope перенесены вместе в другой каталог.

Требование: инструмент должен сохранить snapshot ID независимо от абсолютного корня проекта и физического каталога отчётов.

Проверка: два одинаковых дерева в разных каталогах имеют один ID; изменение входного файла, профиля или назначения scope меняет ID.

### REQ-SA-004 — Адресуемый индекс

Условие: index читает спецификацию в профиле раздела 4.

Требование: инструмент должен вернуть каждую объявленную норму ровно один раз с её условием, формулировкой, способом проверки, content hash и точной ссылкой на исходный блок.

Проверка: Unicode/CRLF, перестановка строк, code fence, blockquote, дубликаты ID и неполный блок; отсутствие объявленных норм не выдаётся за доказательство отсутствия обязательств.

### REQ-SA-005 — Полный учёт назначенных норм

Условие: submit принимает ответ роли на scope.

Требование: инструмент должен принять ответ только при точном равенстве множества assessment IDs назначенному множеству requirement IDs.

Проверка: отсутствующий, повторный или чужой ID отклонён; невозможность оценки выражается явным unknown/ambiguous, а не пропуском строки.

### REQ-SA-006 — Привязка цитат

Условие: агент представил assessment в declared-профиле; для accepted-профиля допустимые диапазоны заданы контрактом расширения.

Требование: инструмент должен отклонить цитату, не совпадающую с исходными строками своего snapshot и категории, а цитату спецификации — также находящуюся вне блока назначенного требования.

Проверка: чужое требование с реальной цитатой, сдвинутый диапазон, выдуманный текст, traversal, symlink и код вместо теста отклонены.

### REQ-SA-007 — Раздельная сила свидетельств

Условие: отчёт содержит assessment нормы.

Требование: инструмент должен представлять качество нормы, поддержку реализацией, релевантность assertion и выполнение теста отдельными состояниями по разделу 7.

Проверка: корректный код со слабым зелёным тестом не превращается в product gap; зелёный тест дефектного поведения не превращается в соответствие.

### REQ-SA-008 — Исполнение подтверждается процессом

Условие: хост явно вызывает test для разрешённого runtime.

Требование: инструмент должен создать receipt из наблюдаемого запуска согласно разделу 8, не принимая высказывание агента или импортированный JSON за факт собственного исполнения.

Проверка: успешный, упавший, пустой, прерванный запуск и изменение snapshot до/после дают разные состояния; фильтр без реально выполненных тестов не даёт passed.

### REQ-SA-009 — PHP только в проверенном контейнере

Условие: хост явно запрашивает PHP facts или PHPUnit.

Требование: инструмент должен выполнять PHP только в проверенном project container по правилам раздела 8, без fallback на PHP хоста.

Проверка: отсутствующий контейнер, чужой mount, некорректный SDK-envelope или недоступный parser дают отказ; исходники и цитаты SDK сверены до использования.

### REQ-SA-010 — Устаревание не скрывается

Условие: источники изменились после prepare.

Требование: инструмент должен показывать сохранённый run как stale в status/report и запрещать добавление новых assessments или исполнений к прежнему snapshot.

Проверка: HTML/JSON обновляются с явным stale; история не теряется, свежий вывод не выдаётся, нужен новый run.

### REQ-SA-011 — Повторная попытка

Условие: хост повторяет submit или вызывает retry задания.

Требование: инструмент должен применять протокол попыток раздела 9 без потери ранее принятых ответов.

Проверка: одинаковый submit идемпотентен, другой ответ той же попытки отклонён, retry увеличивает attempt, поздний ответ старой попытки отклонён.

### REQ-SA-012 — Целостность состояния

Условие: процессы одновременно изменяют один run.

Требование: инструмент должен сохранять каждое успешное изменение без потерянных обновлений, используя ограниченные по размеру атомарные записи.

Проверка: конкурентный submit разных ролей сохраняет оба ответа; превышение размера отклоняется до публикации непрочитываемого state.

### REQ-SA-013 — Честный отчёт

Условие: пользователь вызывает report для любого существующего run.

Требование: JSON и HTML должны показывать каждую объявленную норму, обе роли, pending/stale и раздельные свидетельства, не выдавая автоматический PASS или сертификат.

Проверка: незавершённый run сохраняет строки ожидающих норм; HTML экранирует пользовательский текст; доставка всех ответов не снимает необходимость согласования.

### REQ-SA-014 — Детерминированное автономное ядро

Условие: вызываются init/index/prepare/tasks/submit/status/retry/report.

Требование: эти операции должны работать нативным бинарником на macOS/Linux без Node, Python, PHP, Go toolchain и модельного API во время исполнения.

Проверка: готовый бинарник проходит детерминированный сценарий с пустым PATH; SDK/тестовые команды отдельно требуют runtime проверяемого проекта.

### REQ-SA-015 — Изоляция служебных записей

Условие: инструмент выполняет любую операцию.

Требование: инструмент должен записывать служебное состояние на хосте только в явный CONFIG при init или в reports_dir, не изменяя выбранные исходники, Git, задачи и установленную конфигурацию агента.

Проверка: сравнение входных байтов до/после; stdout содержит JSON, диагностический stderr управляется LOG_LEVEL и не содержит цитат/секретов/сырых ответов.

## 6. YAML и CLI

```yaml
version: 1
project_root: .
specs:
  paths: [specs]
  include: ['*.md']
code:
  paths: [src]
  exclude: ['*_test.go']
tests:
  paths: [tests]
reports_dir: .spec-audit/reports
runtime:
  kind: none
  service: ''
  paths: []
  tests: []
  timeout_seconds: 30
scopes: []
```

CONFIG — доверенный ввод владельца, а не инструкция из анализируемых файлов. project_root и reports_dir разрешаются относительно каталога CONFIG; абсолютные пути допустимы. paths каждой группы — существующие файлы/каталоги относительно project_root. include/exclude — glob только имени файла (`filepath.Match`), не язык рекурсивных путей; отсутствующий include означает все имена. Файл не может одновременно попасть в разные категории. Пустая группа tests допустима. specs/code непусты. Символические ссылки и специальные файлы не входят в снимок. reports_dir не должен пересекаться с выбранными входными деревьями; внутри project_root отдельный служебный каталог допустим.

scopes: список `{id, focus, requirements: [REQ-...]}` снабжает каждую норму ровно одним владельцем. Пустой scopes назначает все объявленные нормы одному scope `all`. Неизвестные, повторные или нераспределённые IDs отклоняются. Лимиты первого среза: 16 scope, 64 нормы на scope, 512 норм суммарно, 25000 входных файлов/512 MiB, файл 32 MiB, CONFIG 1 MiB, ответ роли 4 MiB, state 32 MiB. Нормы не разрезаются по числу токенов; слишком большой scope требует осознанного деления.

CLI: `init CONFIG`; `index CONFIG`; `prepare CONFIG RUN_ID`; `tasks CONFIG RUN_ID`; `submit CONFIG RUN_ID TASK_ID RESULT_PATH`; `retry CONFIG RUN_ID TASK_ID`; `status CONFIG RUN_ID`; `report CONFIG RUN_ID`; `test CONFIG RUN_ID EXECUTION_ID`; `php-facts CONFIG RUN_ID FILE...`. prepare не перезаписывает run. RUN_ID, TASK_ID, EXECUTION_ID — локальные slug, не пути. Ошибка команды — non-zero exit и JSON error в stderr; логирование использует LOG_LEVEL=debug/info/warn/error, по умолчанию info. Успешная команда — один JSON-документ в stdout.

## 7. Контракт ответа и отчёта

Ответ роли содержит `task_id, attempt, snapshot_id, scope, role, summary, assessments, limitations`. Все поля обязательны, unknown/duplicate/case-changed поля и null отклоняются. Каждая assessment содержит `requirement_id, specification, implementation, assertion, statement, spec, code, tests, limitations`.

| Поле | Допустимые значения / смысл |
| --- | --- |
| specification | clear / ambiguous — оценка определённости нормы агентом |
| implementation | supported / contradicted / unknown — статическая оценка, не runtime verdict |
| assertion | relevant / weak / contradicts / missing / unknown — суждение о тестовой проверке |
| spec, code | Массив полнострочных цитат `{path,line_start,line_end,quote}`; spec непустой, supported/contradicted требуют code |
| tests | Массив `{test_id,citation}`; relevant/weak/contradicts требуют конкретный тестовый источник; test_id связывает с receipt |
| limitations | Что не доказано; unknown/ambiguous не скрывается отсутствием строки |

Цитата не доказывает интерпретацию. test_id и принадлежность assertion соответствующему тесту остаются проверяемым суждением агента/хоста; совпадение имени в receipt не доказывает эту смысловую связь. ID исполнения Go — `import/path::TestName`, PHPUnit — `ClassName::methodName` (с суффиксом dataset, если он есть в JUnit). Неизвестный test_id отображается как not_recorded. Receipt другого snapshot не засчитывается. Противоречащие роли показываются рядом и требуют согласования, а не усредняются.

Отчёт содержит `delivery_complete`, `freshness`, `requirements_total`, pending tasks, все строки норм, список executions и `host_reconciliation_required: true`. Итоги и ограничения роли представлены один раз на task; строка нормы показывает последнюю запись для каждого test_id, а общий список executions сохраняет полную историю, включая предыдущие неудачи. База полноты — **объявленные требования данного профиля**, не все возможные обязательства естественного языка. Итогов automatic PASS/compliant нет. Позднейшие изменения источников после генерации статического HTML не могут обновить его сами: перед решением нужен свежий вызов status/report, а на странице указаны snapshot и время генерации.

## 8. Runtime и PHP SDK

kind=none отключает исполнение. kind=go разрешает фиксированный запуск `go test -json -count=1` внутри project_root; paths задаёт локальные пакеты, tests — точные имена функций для фильтра, не shell-команду. Go нужен лишь как runtime проверяемого Go-проекта. kind=docker-php использует Docker Compose service (по умолчанию app), локальные PHPUnit paths/tests и таймаут 1–120 секунд. Произвольный исполняемый файл или shell из конфига не принимается.

PHP-процесс запускается через проверенный container ID: фактический mount project_root → `/var/www/html`, workdir тот же, ровно один работающий контейнер. Внутри контейнера действует отдельный timeout; убийство docker CLI само по себе не считается остановкой PHP. PHPUnit не пишет result cache/coverage; JUnit — в уникальном контейнерном /tmp. Сам бинарник не пишет в бизнес checkout. Тесты и Composer autoload — доверенный исполняемый код проекта, а не песочница: оператор отвечает за изолированную тестовую среду, поэтому launcher не запускает тесты без разрешения хоста и не допускает параллельные тесты общей БД.

Дополнительная защита CLI сериализует PHPUnit для разных run одного reports_dir и повторно сверяет snapshot после ожидания. Это не блокировка общей БД между разными конфигами/reports_dir или внешними test runners: их согласует хост. Таймаут теста не расходуется на проверку контейнера и чтение/удаление JUnit; эти служебные стадии ограничены отдельно.

Receipt фиксирует ID, snapshot, время, runtime/version, команду без секретов, exit code, фактически завершённые test IDs/statuses, хэши/размер вывода и состояние passed/failed/incomplete/no_tests/stale. passed возможен только при успешном процессе, корректном отчёте, хотя бы одном passed-тесте, отсутствии failed и совпадении входного snapshot до/после. Это доказательство запуска в записанной среде, не полноты и не корректности продукта. Повторный EXECUTION_ID не перезаписывает receipt. Повторный запуск требует нового ID.

PHP facts — syntax_only. SDK разбирает данные, не подключает выбранные source files; Composer autoload при этом выполняется. Envelope содержит точные байтовые диапазоны/хэши, фактическую версию/reference php-parser, хэш composer.lock и SDK. Бинарник проверяет схему, точное множество запрошенных файлов, текущие исходники и цитаты перед записью. SDK не гарантирует полноту динамических вызовов и не доказывает силу PHPUnit assertions. Отдельная PHPStan-проба остаётся исследовательским материалом.

## 9. Состояние и безопасность

Один run имеет неизменяемый manifest и задания scope × role. Ответ хранится по task/attempt, submit одинаковых данных идемпотентен. retry отзывает текущий ответ задания и увеличивает attempt, сохраняя предыдущий файл как историю. Производные отчёты инвалидируются после изменения state. Файловая блокировка сериализует изменение одного run; запись через temp + rename. Полное восстановление после power loss не заявлено.

Переносимый snapshot включает относительные источники/хэши, распределение требований, конфиг анализа и версию протокола/парсера; физические корни исключаются. Безопасное переиспользование семантических ответов между разными run в v0.1 не реализуется. Изменение runtime-версии не превращает исторический receipt в подтверждение новой среды: версия записана в нём явно.

Источник, assessment и SDK output считаются недоверенными данными. Никакие инструкции в них не запускаются. Пути не выходят из назначенных корней; HTML использует экранирование; проверки размеров выполняются до публикации state. Доверенные локальные пользователи могут изменить state — криптографическая подпись и защита от администратора машины не обещаны.

## 10. Приёмка и предел утверждений

До реализации подготавливаются синтетические случаи с независимыми ожидаемыми выводами: корректная реализация/сильная проверка; корректный код/слабая проверка; неверный код/зелёный закрепляющий тест; отсутствие достаточного свидетельства; неоднозначная норма. Дополнительно проверяются чужая цитата, пропуск ID, stale, retry, неполный run, пустой и неуспешный runtime.

Автоматический контрактный тест проверяет механизмы, а не интеллект агента. Отдельный свежий проход mapper/redteam не получает ожидаемые ответы. Его результаты сравниваются с зафиксированными примерами; ошибки не исправляются хостом незаметно. Один успешный проход не доказывает статистического повышения точности. Самоприменение к этой спецификации дополняет независимые примеры, но не заменяет их.

Технологическое решение среза: Go, stdlib для процесса/JSON/HTML/файлов; уже испытанные Goldmark и YAML; существующий PHP parser проекта через SDK. Одна поддерживаемая сборка, исторические прототипы не становятся вторым источником норм. Для macOS/Linux проверять нативное исполнение в доступной архитектуре и отдельно обозначать только кросс-компиляцию остальных; не приравнивать сборку к исполнению.

## 11. Стандарты и открытые решения

Структура ориентирована на requirements engineering из [публичного описания ISO/IEC/IEEE 29148:2018](https://www.iso.org/standard/72089.html). Полный платный текст не читался; формальное соответствие не установлено. Практические критерии адресуемости, явных допущений и проверяемости сверены с [NASA Appendix C](https://www.nasa.gov/reference/appendix-c-how-to-write-a-good-requirement/) и [публичной памяткой INCOSE](https://www.incose.org/docs/default-source/working-groups/requirements-wg/guidetowritingrequirements/incose_rwg_gtwr_v4_summary_sheet.pdf). Наш синтаксис, состояния отчёта и границы первого среза — решения этого проекта, не цитаты стандартов.

На 2026-09-15 [официальная карточка ISO](https://www.iso.org/standard/72089.html) обозначает 29148:2018 опубликованным стандартом, подлежащим пересмотру; DIS 29148 ещё разрабатывается. Основа нашей подготовки — опубликованная редакция 2018, не проект DIS. [Открытый ISO OBP](https://www.iso.org/obp/ui?_escaped_fragment_=iso:std:iso-iec-ieee:29148:ed-2:v1:en) различает соответствие процессам, содержанию информационных объектов и адаптированное соответствие. Эта карта — подготовка к будущей проверке, не постатейная матрица соответствия.

| Область подготовки | Где ведём сейчас | Что ещё нужно для заявления о соответствии |
| --- | --- | --- |
| Назначение, заинтересованные стороны, условия применения | §1–3, §12.1 | Сверка полноты с выбранными информационными объектами полного стандарта |
| Однозначные адресуемые требования и атрибуты | §4–5, §12.2–13 | Независимая проверка качества всего принятого комплекта |
| Управление изменениями и основанием решения | accepted-index; §12.2; §14 | Определить заявляемый вид соответствия и проверить процессы по полному тексту |
| Подтверждение выполнения и пригодности | §10, §12.4 | Отделить проверку реализации от подтверждения полезности владельцем |
| Адаптация под малый проект | Явные исключения §2 и §12.3 | После доступа к стандарту согласовать и обосновать каждую допустимую адаптацию |

Полный текст стандарта не приобретался и целиком не проверялся. До такой проверки нельзя писать «соответствует ISO» или «сертифицирован». Будущий аудит сначала фиксирует редакцию и область заявления, затем сопоставляет каждый применимый пункт с документом/свидетельством и закрывает пробелы. NASA используется как открытая практическая памятка, не замена ISO. Зафиксированные ниже ID, источники, обоснования, приёмка и история уменьшают объём будущей переработки; гарантии лёгкой сертификации нет.

## 12. Контракт ближайшего выпуска

### 12.1 Цели и сценарий использования

Основной пользователь — владелец или команда проекта, работающие в существующей AI-сессии, например Codex/Claude Code. Менеджер потребляет итог, разработчик проверяет цитаты и контрпримеры. Агент-хост организует работу, но не считается независимым владельцем бизнес-норм. ТЗ и код остаются источниками; трекер задач и workflow разработки выбирает проект. spec-audit только аудирует и отдаёт обоснованные findings: не исправляет код, не синхронизирует задачи и не запускает инструменты разработки автоматически.

Инструмент предназначен для разных проектов. Ядро и общий launcher не должны содержать названий/путей конкретного проекта, бизнес-норм, ID задач или исключений под пилот. Эти знания поступают из CONFIG, выбранных источников и отдельного договора применения. Особенности языка/фреймворка отделяются в ограниченные SDK/runtime; текущий PHP SDK даёт только syntax_only. Независимость от проекта требуется сейчас, поддержка всех языков и универсальная система плагинов — нет. Пилот служит проверкой полезности и источником независимых регрессий, не доказательством пригодности для любого проекта. Исторические упоминания в frozen baseline и свидетельствах не задают defaults рабочего инструмента.

| Цель | Пользовательский результат | Покрывающие требования |
| --- | --- | --- |
| G1 — вынести обслуживание аудита из продукта | Самостоятельный бинарник; CONFIG/состояние/отчёты в разрешённой изолированной области — внешней либо локальной игнорируемой `.spec-audit/`; PHP только при разрешённом контейнерном запуске | SA-001…003, SA-009, SA-014…015 |
| G2 — не проверять выдуманную или потерянную обязанность | Сохранены условия/исключения; применимость и неизвестность видны | SA-004…006, SA-016…018; accepted-index |
| G3 — получить объяснимый результат | Отдельны код, assertions и исполнение; хост обосновывает итог рядом с исходными ролями | SA-007…008, SA-013, SA-019…021 |
| G4 — продолжать работу без потери истории | Попытки и решения сохраняются; устаревший вывод не становится текущим | SA-010…012, SA-020 |
| G5 — видеть состояние проекта и находить основание решения | Карта выбранных источников, проблемных норм и переходы ТЗ ↔ код ↔ assertions | SA-022…026 |

Нормальный сценарий: владелец задаёт CONFIG и раздел ТЗ → хост извлекает кандидатов и проверяет контекст → явно принимает индекс → готовит задания → независимые mapper/redteam → проверяет и сохраняет raw → при разрешении запускает тесты → записывает обоснованное согласование → показывает JSON/HTML. Новый контекст, меняющий смысл, требует нового принятия/prepare; в старый snapshot он не подмешивается.

Без ИИ бинарник выполняет механические команды. Он не вызывает модель и не объявляет семантическую полноту. Изменения кода после отчёта требуют нового run. Безопасность исполнения тестов зависит также от изоляции проекта; snapshot не является sandbox.

### 12.2 Правила ведения требований

Стабильный ID и поля §4 сохраняются. Для каждого нового требования ниже отдельно указаны источник и обоснование. Общие атрибуты: владелец StenHigh; приоритет Must для согласованного выпуска; стадия — задано для реализации, а состояние фактической приёмки ведётся в §12.4. Изменение смысла оформляется новой редакцией с причиной и влиянием на тесты/интерфейсы; старые экспериментальные gold и хэши не переписываются. Существующие SA-001…015 остаются baseline v0.1, не получают фиктивную историю утверждения задним числом.

Обязанность отделяется от обоснования и примера. Предпочтителен один наблюдаемый результат; контекст, исключения и зависимости могут иметь несколько источников. Если обязанность известна, неизвестный порог или человеческий исполнитель не отменяет её. Нерешённый вопрос хранится с владельцем, влиянием и событием закрытия; план не может закрыть его галочкой вместо решения.

### 12.3 Границы и открытые решения

Аудит не меняет существующие CI-проверки, прежние инструменты, отчёты и маркеры проверяемого проекта. Их перенос или удаление требуют отдельного решения владельца проекта. Graph/cache, автоматические issues, универсальная система плагинов и доказательство абсолютной безошибочности не входят в выпуск; это не отменяет независимость ядра и launcher от бизнес-проекта (§12.1). Настройка и установка требуют отдельного разрешения: `init` создаёт только YAML; установка и обновление skill — отдельные явные команды `skill install|update` по [§18](#18-поставка-версия-обновление-и-установка-skill), реализованы и приняты на релизе `v0.1.0` (README «Приёмка и ограничения»).

| Вопрос | Владелец / событие закрытия | Влияние до ответа |
| --- | --- | --- |
| OQ-01: аудит выбранных разделов или весь корпус одной командой | Закрыт StenHigh 2026-09-15: «закрепляем и продолжай самостоятельно выполнять» после предложения ограниченного применения | Принят аудит выбранных разделов. Не единый индекс всего продукта; предел 64 active-норм сохраняется |
| OQ-02: вид будущего ISO-conformance и доступ к полному стандарту | StenHigh; перед формальной оценкой соответствия | Не блокирует разработку; ISO-conformance не заявляется |
| OQ-03: разрешения конкретного проекта | Владелец проверяемого проекта; до установки или исполнения проектного кода | Аудит не даёт разрешения на бизнес-записи, установку или тесты. Для текущего пилота действуют отдельно зафиксированные [условия применения](smsplace-adoption.md) |

Для ограниченного аудита можно использовать отдельные CONFIG/reports_dir по разделам; это не единый индекс всего продукта и не способ скрыть пропущенные межраздельные зависимости. Лимиты §6 и accepted-index остаются действующими, превышение требует явного выбора границы, не усечения.

### 12.4 Конечная приёмка и статус

| Критерий | Свидетельство / актуальный статус 2026-09-15 |
| --- | --- |
| A1 — прежние автономные команды и frozen контракты сохранены | Пройдено для этого среза: полный Go test/vet, адресный race, пять frozen baseline, исторический declared-run; четыре сборки, review без PATH на macOS ARM64 и Linux AMD64 в эмуляции |
| A2 — три смысловые границы различаются на заранее заданных парах | Малый [frozen-контроль](../acceptance/release-control.json) пройден: четыре свежие роли, 12/12 оценок; независимое извлечение/review сохранило 3/3 нормы, обязательную context note и допустимый дополнительный вопрос. Это не статистическая точность |
| A3 — итог хоста записан без ремонта исходных ролей | Пройдено: review по §14, exact raw и bounded history, negative/concurrency/stale/compatibility тесты; оба контрольных JSON/HTML имеют current-согласование и не объявляют полноту ТЗ |
| A4 — пользователь получает полный сквозной результат | Пройдено для SA-019…021 на фиксированных копиях: self-v1 нашёл три группы слабых assertions; после адресных регрессий self-v2 получил supported/relevant обеих ролей. Raw, отдельное согласование и JSON/HTML сохранены. Перепроверка не объявляется новым слепым экспериментом; runtime этих run — none |
| A5 — основание перехода со старого AI-аудита | Выполнен с ограничениями same-edition контроль `.local/comparison-20260915/study.json`: одинаковая редакция общих норм и 2499 PHP-файлов; оба подхода нашли два одинаковых статических контрпримера. Контекстные бюджеты различались, PHP-тесты не исполнялись, точность/скорость/цена нового подхода не доказаны. Исторические отрицательные контроли не переписаны |
| A6 — граница внедрения принята владельцем | Принята 2026-09-15 для текущего пилота: выбранные разделы, сохранение прежних проверок; последующее разрешение на локальную рабочую область и подключение существующего skill записано в [спецификации применения](smsplace-adoption.md). Внешние прогоны остаются историей. Это не массовая миграция и не разрешение менять другие проекты |
| A7 — показатели и настройки HTML по SA-027…030 | Технические проверки выполнены: Go test/vet/race, четыре сборки, автономный synthetic UI-check. Daily v2 перевыпущен без изменения исходных результатов: 7 weak / 1 missing / 0 relevant. Владелец принял HTML ручным просмотром 2026-09-15. Это приёмка интерфейса в его среде; отдельная проверка каждого IDE/ОС и всех нативных клавиатурных действий не заявляется |

Основание A1…A6 принято только для описанной ограниченной границы; пригодность обычного рабочего цикла проверяется отдельно от исследовательского A/B. Отрицательная оценка бизнес-кода сама по себе не провал инструмента; потеря нормы, ложная уверенность или необъяснённое снятие разногласия — провал контроля. Измеряются wall time, размеры пакетов, число повторов/ручных исправлений и непринятых результатов; неизвестные токены/стоимость отмечаются unavailable. Первый рабочий бюджет продолжения — 8 часов по предложению хоста, не обещание качества или срока; при выходе за бюджет нужно показать остаток, а не молча расширять исследование.

## 13. Требования расширения

### REQ-SA-016 — Сохранение обязанности

Условие: хост принимает кандидатов из обычного Markdown.

Требование: процесс принятия должен сохранять каждую обнаруженную обязанность вместе с её условиями, исключениями и неопределённостью, не заменяя её ненормативной заметкой.

Проверка: парные случаи обязательства/пояснения, человеческой обязанности и неизвестного порога; источник не усиливается и не теряется, решение хоста проверяется отдельно от схемы JSON.

Источник: G2; запрос владельца о spec-first; отрицательный контроль извлечения 2026-09-15. Обоснование: правильная проверка неверно извлечённой нормы не отвечает на вопрос продукта. Зависимость: accepted-index.

### REQ-SA-017 — Граница применимости

Условие: роль оценивает реализацию по принятому набору источников.

Требование: роль должна обозначить реализацию как unknown, если применимость нормы к рассматриваемому участнику, направлению взаимодействия или условию не установлена.

Проверка: одинаковый код обработки неизвестного идентификатора при норме для client request и при явно заданной норме для supplier response; соседние определения включены до prepare, одинаковые слова не заменяют проверку области действия.

Источник: G2; согласование контекстного контроля 2026-09-15. Обоснование: соглашение ролей не делает перенос правила между протоколами корректным. Зависимость: SA-006, SA-016. Это правило роли/хоста, не автоматический смысловой классификатор.

### REQ-SA-018 — Полнота утверждения теста

Условие: роль присваивает assertion значение relevant.

Требование: роль должна обосновать проверку всех существенных результатов назначенной нормы на её применимых условиях; проверка только части составной обязанности должна оставаться weak.

Проверка: пары полного и частичного assertion, зелёный тест ошибочного поведения; факт исполнения берётся только из receipt и не меняет силу assertion.

Источник: G3; запрос владельца о сопоставлении требований с assertions. Обоснование: проверка одного поля не подтверждает все инварианты. Зависимость: SA-007…008.

### REQ-SA-019 — Явное согласование аудита

Условие: все назначенные ответы ролей получены и хост отправляет решение по §14.

Требование: бинарник должен принять только полное обоснованное решение для каждой нормы, привязанное к текущему основанию аудита, и сохранить точные входные байты решения без изменения исходных ролей.

Проверка: неполный/повторный/чужой ID, неточная цитата, чужой run, изменённое основание, пустое обоснование и попытка до завершения ролей отклоняются без изменения журнала.

Источник: G3; требование владельца получать понятный итог. Обоснование: результат не должен оставаться только в ручном файле хоста. Зависимость: SA-005…006, SA-012.

### REQ-SA-020 — Актуальность согласования

Условие: после записанного решения изменились источники, попытки ролей или записи исполнения.

Требование: бинарник должен показывать прежнее согласование как outdated, сохраняя его историю и требуя нового явного решения для текущего аудита.

Проверка: retry, новый receipt, изменение источника и конфликт параллельных решений не дают прежнему решению статус current.

Источник: G4; SA-010…012. Обоснование: согласование относится к конкретным свидетельствам, а не только ID нормы.

### REQ-SA-021 — Итог рядом со свидетельствами

Условие: формируется JSON/HTML run с историей согласования.

Требование: отчёт должен показывать последнее решение хоста и его актуальность отдельно от неизменённых исходных ролей, не объявляя автоматического соответствия или доказанной полноты ТЗ.

Проверка: обе роли, их разногласия, host-обоснование, unknown/weak/not_recorded, устаревшая история и экранирование HTML видны в одном отчёте.

Источник: G3; SA-013. Обоснование: менеджер должен видеть вывод и его предел, разработчик — проверяемое основание.

## 14. Контракт согласования аудита

Для новых submit бинарник сохраняет первые принятые байты роли в прежнем results-файле и необязательный raw_sha256 в Entry. Семантически равная повторная отправка остаётся duplicate, но не заменяет первые байты; оставшийся после прерванной публикации results-файл используется только при равном Result. Read проверяет hash и соответствие state. Старые Entry без hash остаются legacy_canonical: их исходное форматирование неизвестно и не восстанавливается. Согласование старого run допустимо с явной отметкой этого ограничения; нельзя объявлять его exact raw. Retry снимает hash текущей попытки, сохраняя прежний файл. Новый бинарник читает старые run; чтение расширенного state старым бинарником не обещается.

Новая команда `review CONFIG RUN_ID [DECISION]` не заменяет `reconcile`: reconcile принимает требования, review принимает итог аудита. Без DECISION возвращается контекст согласования: run_id, snapshot_id, basis_sha256, delivery_complete, freshness, требования, entries, executions и история решений. Этот режим доступен и для stale-истории; запись требует fresh и полной доставки. Сам вызов CLI не доказывает личность автора: reviewer — подпись оператора, не криптографическая идентификация.

DECISION — строгий UTF-8 JSON до 4 MiB со всеми полями: `version:1, review_id, run_id, snapshot_id, basis_sha256, reviewer, summary, assessments, limitations`. review_id — slug; reviewer и summary непусты. assessments содержит ровно одну обычную Assessment §7 на каждую норму, с теми же правилами цитат/неоднозначности. statement объясняет выбор хоста с учётом разногласий. Если по норме одновременно есть relevant-тесты штатного пути и contradicts-тест достижимой ветки, assertion нормы — contradicts: зелёный тест, закрепляющий противоречащее норме поведение, важнее подтверждающих тестов; weak и missing contradicts не отменяют (§29). Нельзя добавлять execution status или новые нормы. Если нужен ранее отсутствовавший нормативный контекст — новый принятый индекс и run, а не цитата вне snapshot.

Основание: SHA-256 от Go `json.Marshal` объекта `{snapshot_id, state, review_head}` в указанном порядке. state — текущий State с entries/executions без производных отчётов; review_head — SHA-256 точных байтов последнего принятого DECISION либо пустая строка. Оно меняется также после нового решения хоста, чтобы параллельные решения не теряли друг друга. Это compare-and-swap, не доказательство правильности оценки.

Под общим lock run хранится `host-reviews.json`: `{version:1, records:[<точная UTF-8 строка DECISION>,...]}`, до 64 решений и 32 MiB целиком. Массив append-only; один и тот же review_id с теми же байтами — duplicate без записи, с другими — конфликт. Исторический duplicate не делает решение current. Проверки размеров/схем/цитат, CAS и повторная сверка snapshot происходят до атомарной публикации. Производные отчёты инвалидируются до изменения журнала; отказ не меняет журнал или роли.

Актуальность последнего решения проверяется относительно state/snapshot и review_head его предшественника. Состояния: missing / current / outdated. Read/report валидируют журнал и не скрывают повреждение как отсутствие решения. Старый run без журнала остаётся доступен без миграции. JSON/HTML сохраняют историю и исходные роли; `host_reconciliation_required` становится false только при current, но `semantic_completeness_proven` остаётся false. Отсутствие автоматического PASS сохраняется; это явное суждение хоста, а не сертификат бинарника.

## 15. Карта проекта для человека

Менеджеру нужна обозримая картина доработок, разработчику — переход от проблемы к конкретному месту ТЗ, кода и теста. Карта дополняет существующий отчёт, а не вводит второй аудит. Ручные `@tz` не требуются: связи берутся из проверенных цитат принятого индекса и assessments. Наличие маркера или ссылки само по себе не свидетельствует о соответствии.

### REQ-SA-022 — Полный видимый инвентарь

Условие: report строится для существующего run, включая незавершённый или stale.

Требование: JSON/HTML должны содержать каждый выбранный файл manifest с категорией, относительным путём и хэшем, отдельно от множества норм данного run и их состояния аудита.

Проверка: нецитированный файл кода, документ без принятых норм, пустая группа tests и ожидающая норма остаются видимыми; счётчики совпадают с manifest, исключённые конфигом файлы не включаются.

Источник: G5; запрос владельца о картине проекта. Обоснование: просмотр только найденных связей скрывал непроверенные области. Зависимость: SA-003, SA-013.

### REQ-SA-023 — Двусторонняя навигация

Условие: у нормы или assessment есть сохранённая цитата.

Требование: HTML должен позволять перейти от нормы к точному сохранённому диапазону ТЗ, кода или assertion и обратно к связанным нормам, показывая автора связи и test_id для тестового свидетельства.

Проверка: несколько норм на один файл/диапазон, отдельная цитата хоста, Unicode в пути и повторные цитаты; каждый внутренний якорь разрешается, цитата доступна без исходного checkout.

Источник: G5; прежний сценарий ТЗ ↔ код и человекочитаемый trace HTML. Обоснование: путь к файлу без диапазона и перехода неудобен для проверки вывода. Зависимость: SA-006, SA-021.

### REQ-SA-024 — Поиск областей внимания

Условие: человек просматривает карту.

Требование: HTML должен предоставлять поиск по ID, тексту и путям, отбор по scope и отдельные фильтры проблем реализации, слабых/отсутствующих assertions, вопросов к ТЗ, unknown, pending, разногласий ролей и отсутствия текущего согласования; файлы должны отбираться по категории и наличию текущих связей.

Проверка: фильтры комбинируются, показывают число результатов и пустое состояние; принятая неоднозначность/открытый вопрос видны до ответа ролей; переход по прямому якорю открывает скрытую фильтром цель.

Источник: G5; запрос владельца о местах доработок. Обоснование: последовательное чтение полного журнала не даёт обзора. Зависимость: SA-022…023.

### REQ-SA-025 — Честное основание карты

Условие: карта сводит сохранённые оценки и связи.

Требование: карта должна явно различать текущее решение хоста, предварительные оценки ролей и историю, не превращая отсутствие связи в дефект, разногласие ролей в противоречие ТЗ или ссылку на тест в подтверждённое исполнение.

Проверка: current/outdated/missing review × fresh/stale × pending/доставленные роли; устаревшая host-only связь не скрывает отсутствие текущих связей; relevant без receipt остаётся not_recorded, а разные мнения видны и после согласования.

Источник: G3…G5; ограничения прежнего trace. Обоснование: удобная визуализация не должна усиливать достоверность исходных данных. Зависимость: SA-007…008, SA-020…021.

### REQ-SA-026 — Автономная безопасная страница

Условие: отчёт открывают в браузере вне рабочей AI-сессии.

Требование: навигация, цитаты и фильтры должны работать в одном HTML без сервера, CDN, сети и доступа к checkout, с экранированием недоверенного текста и доступными с клавиатуры штатными элементами управления.

Проверка: автономный браузерный просмотр, узкое окно, поиск/фильтры/обратный переход, строка с HTML/JavaScript отображается как текст; без JavaScript все свидетельства доступны через штатные раскрывающиеся блоки и ссылки.

Источник: G1, G5; запрос владельца о переносимом бинарнике. Обоснование: для чтения результата не нужна ещё одна система. Зависимость: SA-014…015.

### 15.1 Правила представления и границы

- Единственный вход — прежний `report CONFIG RUN_ID`; добавляется производная `navigation` в JSON. Manifest, Result, ReviewDecision, snapshot и receipts не меняются. Существующая история остаётся доступной.
- Инвентарь — все файлы **выбранного snapshot**, не весь диск или исключённые конфигом части проекта. Карта одного run показывает все его scope; объединение разных run и полная семантическая индексация всего проекта этим не заявляются. OQ-01 и A6 закрыты отдельным решением владельца, не самим наличием карты.
- Текущий host review задаёт основу фильтров оценок; без него показываются доступные роли без усреднения. Pending учитывается отдельно. Разногласие — различие состояний specification/implementation/assertion между доставленными ролями; это не автоматическое обнаружение противоречий между документами.
- «Вопросы к ТЗ» включает ambiguous и принятые unresolved; содержание проблемы и цитаты читаются в карточке. Для сопоставления конфликтующих норм можно открыть обе карточки. Автоматический поиск всех междокументных конфликтов не входит в этот срез.
- «Нет текущих связей» означает отсутствие сохранённой привязки, пригодной для текущего snapshot. Это может быть технический код, ещё не разобранная функциональность или кандидат на незаданное поведение. Дефект и отсутствие нормы требуют смыслового разбора, а не подсчёта ссылок. Исторические связи отображаются отдельно и не считаются текущими.
- Основная ссылка ведёт на встроенный сохранённый фрагмент с точным path:line и происхождением. Полные исходники не копируются; сервер исходников не нужен. §16 добавляет необязательную ссылку на локальный редактор, не заменяя цитату. Нецитированные методы специально не выдумываются из имени файла. Семантический поиск по всему коду не обещается.
- У исполнения сохраняются собственные статусы/receipt и отдельная история; ни единого «процента соответствия», ни зелёного общего PASS. Snapshot, время генерации, fresh/stale и статус review всегда показаны. Статическая страница не обнаруживает изменения после генерации.

### 15.2 Приёмка среза

До реализации заданы проверки SA-022…026: pending/без tests; общий диапазон нескольких норм; отдельное host-свидетельство; current → retry/outdated → stale; фильтры и прямые ссылки; Unicode/HTML-injection; прежний declared-run и accepted-run. Проверяются Go-регрессии, браузерный просмотр и повторная генерация из копии реального сохранённого run без правки его исходных raw. Результат фиксируется в существующем плане; пользовательская полезность не выдаётся за статистическую точность аудита. Бизнес-репозиторий остаётся read-only.

## 16. Достаточность тестов и настройки HTML

Причина редакции 0.4 — запрос владельца 2026-09-15: слабые проверки трудно заметить, не хватает обзора по ТЗ, переходов в редактор и тёмной темы. Это расширение представления, не повторный смысловой аудит. Старые оценки, журналы и snapshot не изменяются.

### REQ-SA-027 — Видимая неполнота проверок

Условие: HTML показывает оценку assertion.

Требование: отчёт должен явно различать наличие тестового свидетельства, частичную или слабую проверку (weak), достаточность по оценке автора (relevant), противоречащую проверку, отсутствие подходящей проверки и неизвестность, независимо от факта исполнения.

Проверка: supported + weak + passed остаётся частичной проверкой; relevant без receipt не означает запуск; missing/unknown с цитатой не теряют признак наличия свидетельства. Weak виден в закрытой карточке текущего решения и отдельном фильтре.

Источник: G3, G5; запрос владельца. Обоснование: наличие теста не означает проверки всех существенных результатов нормы. Зависимость: SA-007…008, SA-018, SA-021.

### REQ-SA-028 — Сводка по источникам ТЗ

Условие: report строится для любого сохранённого run.

Требование: JSON/HTML должны показывать общую сводку и графические доли по файлам ТЗ, отдельно считая поддержку кодом, требования с противоречием реализации, наличие тестовых свидетельств и достаточность assertions по текущему согласованию хоста; знаменатели, неизвестность и отсутствие текущей оценки должны быть явными.

Проверка: одна норма с несколькими источниками не удваивает общий итог; файл без норм имеет неопределённую долю, а не 100%; pending/outdated/stale не дают текущих положительных числителей; клик по строке файла отбирает его нормы.

Источник: G5; запрос владельца об обзоре как в trace-matrix. Обоснование: доля ссылок на тесты скрывала слабость проверок. Зависимость: SA-022…026.

### REQ-SA-029 — Открытие локального источника

Условие: пользователь выбрал в HTML локальные каталоги и редактор.

Требование: HTML должен строить безопасные ссылки на файл и начальную строку для Zed, PhpStorm, Cursor и VS Code, предварительно заполняя каталоги из конфигурации генерации и сохраняя встроенную цитату как независимый источник истории.

Проверка: разные каталоги кода/ТЗ, перенос проекта, пробелы/Unicode/URL-разделители и неверный путь; смена выбора меняет все ссылки, не записывает CONFIG или исходники и не запускает редактор без клика. Недоступность browser storage не ломает навигацию.

Источник: G1, G5; запрос владельца. Обоснование: для проверки вывода нужен удобный переход в рабочую копию. Зависимость: SA-015, SA-023, SA-026.

### REQ-SA-030 — Системная и выбранная тема

Условие: человек читает HTML в браузере.

Требование: отчёт должен следовать теме системы по умолчанию и позволять выбрать светлую или тёмную тему, сохраняя читаемость свидетельств, графиков и элементов управления без внешних ресурсов.

Проверка: system реагирует на изменение prefers-color-scheme; явная тема его перекрывает; выбор восстанавливается при доступном storage, при недоступном страница продолжает работать; проверены узкое окно и клавиатура.

Источник: G5; запрос владельца. Обоснование: длительное чтение отчёта должно соответствовать рабочей среде пользователя. Зависимость: SA-026.

### 16.1 Подсчёт и границы

- Расширяется только производная `navigation`: сводка, строки файлов ТЗ и defaults локальных ссылок. Исторические manifest/Result/review/receipts не мигрируют. Сводка использует только `fresh` + `current` host review; предварительные роли и история остаются в карточках и прежних фильтрах, но не становятся текущей готовностью.
- Знаменатель — все нормы выбранного run, включая несогласованные и unknown. Норма относится ровно к одному основному файлу ТЗ — первой нормативной цитате принятого требования (§59: справочный файл группы `references` норму не считает, даже если процитирован первым; `Source` пакета остаётся первой цитатой); дополнительные источники не увеличивают счёт. В старом snapshot нет дерева заголовков, поэтому «раздел» здесь — файл ТЗ, а не восстановленный заголовок из изменившегося checkout. Выбранные файлы без основных норм остаются видимыми. Объединения run или полноты всего /TZ нет.
- «Код + достаточные тесты» — число норм с clear, supported, relevant и без принятых открытых вопросов. Это статическая оценка хоста, не автоматический PASS, не готовность к релизу и не успешный запуск. Счётчики GAP означают требования с contradicted, не число уникальных дефектов/задач трекера. Test presence — непустой массив Tests; он независим от relevant/weak/missing/unknown. Проценты не являются line/branch coverage или долей проверенных ветвей внутри одной нормы.
- Для кода/тестов исходная база — актуальный CONFIG.project_root. Для ТЗ база — этот корень плюс общий каталог выбранных spec-файлов manifest; его точный относительный префикс показан в UI и снимается один раз. Пользователь меняет локальные базы, не пути snapshot. При нескольких несмежных каталогах общий каталог может быть корнем проекта. Ввод — абсолютные POSIX-каталоги macOS/Linux до 4096 символов; `~`, URL, traversal и управляющие символы отклоняются.
- Редактор выбирается из закрытого списка URI-схем, путь кодируется как данные. Браузер/ОС должны разрешать и иметь зарегистрированный обработчик; установленность и открытие файла HTML проверить не может. Локальный файл и номера строк могли измениться после snapshot. Ссылки не устанавливают плагины и не вызывают shell. «Capilot» требует уточнения названия; произвольная URI-схема не добавляется.
- Тема сохраняется как browser preference; каталоги/редактор — в ключе по исходному корню проекта. Нельзя считать localStorage безопасным хранилищем: значения проверяются перед применением; при отказе сохранения остаётся работающий выбор текущей страницы. Нет server/CDN/телеметрии. Без JS графики, числовые таблицы и цитаты доступны, настройки/фильтры — нет.

Форматы переходов сверены с [VS Code URLs](https://code.visualstudio.com/docs/configure/command-line#_opening-vs-code-with-urls), [обработчиком Zed](https://github.com/zed-industries/zed/blob/main/crates/zed/src/zed/open_listener.rs) и [JetBrains URL navigation](https://youtrack.jetbrains.com/articles/SUPPORT-A-2159/How-to-open-a-source-file-with-line-and-column-specified-from-an-URL). Cursor использует профиль file-навигации семейства VS Code; реальный обработчик каждого редактора требует пользовательской проверки в его среде. Эти ссылки — справочная основа адаптеров, не новые нормы продукта.

## 17. PHP и Laravel SDK

Статус: п.1–3 [§17.2](#172-порядок-реализации-и-приёмка) реализованы и приняты 2026-09-16 на независимых синтетических PHP- и Laravel-примерах по [контракту расширения](php-sdk-contract.md) (команда `php-typed`, формат `sdk/3`, блок CONFIG `sdk` с собственным таймаутом; §8 и `php-facts` не изменены). Порядок п.2 изменён решением владельца 2026-09-16: пилотный проект служил средой отладки, приёмка — только синтетика. Известное расхождение с контролем: Larastan разрешает `app(Contract::class)` в привязанную реализацию через загруженный контейнер. Сравнение п.4 на пилоте выполнено один раз по разрешению владельца 2026-09-16 с нейтральным результатом (небольшой прирост найденных мест реализации, без изменения полноты тестов, точности и времени); доказанного улучшения нет, числа — в README. Источник — уточнение владельца 2026-09-16: сохранить интеграцию PHPStan/Larastan для Laravel-проектов, не переносить бизнес-знания пилота в ядро. A1…A7 и frozen §1–10 не свидетельствуют о выполнении SA-031…034; свидетельства — регрессии и внешние тесты, описанные в README.

### 17.1 Зачем и что уже есть

Цель для менеджера — реже пропускать реализацию и связанные тесты, когда они спрятаны за интерфейсом, общим сервисом или возможностями фреймворка. SDK должен помогать находить конкретные методы и места использования; оценка выполнения бизнес-требования и достаточности assertions остаётся за аудитом. Это ожидаемая польза, которую нужно измерить, а не доказанное улучшение точности.

Сейчас [sdk.php](../tool/sdk.php) через php-parser находит объявления методов и обычные/статические вызовы, возвращая имена, точные диапазоны и цитаты. [Go-граница](../tool/php.go) проверяет контейнер, схему `sdk/2`, исходники, composer.lock и хэш SDK. `php-facts` сохраняет отдельный JSON; он ещё не обогащает автоматически TaskBatch, связи тестов и HTML. PHPStan/Larastan в этом пути не вызываются.

[PublicCallProbeRule.php](../prototypes/core/PublicCallProbeRule.php) исследует тип получателя, возвращаемый тип и доступную цель вызова через публичные API PHPStan. Он выводит факты как диагностические сообщения, оставляет `framework_origin` пустым и не подключён к рабочему CLI. Его наличие не означает готового экспортёра или полной карты вызовов.

Планируемое разделение:

| Компонент | Ответственность |
| --- | --- |
| PHP SDK + php-parser | Адресуемые PHP-символы и точные исходные фрагменты |
| PHPStan | Типы выражений и доступные статически разрешённые цели вызовов |
| Larastan — профиль PHP SDK | Laravel-типы и отражение поддерживаемых механизмов фреймворка; без правил конкретного бизнеса |
| Go-ядро | Проверка происхождения, snapshot, хранение и передача ограниченного набора фактов |
| AI-хост и роли | Подбор контекста, связь требования с кодом/assertions и обоснованный итог; HTML показывает происхождение связей |

Техническая опора: публичные [Scope API](https://phpstan.org/developing-extensions/scope) и [Collectors](https://phpstan.org/developing-extensions/collectors), а не чтение внутреннего кеша PHPStan. У [Larastan](https://github.com/larastan/larastan) есть Laravel-aware анализ; [документация возможностей](https://github.com/larastan/larastan/blob/3.x/docs/features.md) описывает, в частности, Eloquent-типы и отношения. Это основание проектного решения, не гарантия разрешения любого фасада, binding или динамического вызова. Поддерживаемые случаи и версии SDK предстоит закрепить на контрольных примерах.

### REQ-SA-031 — Типизированные связи без выдуманных целей

Условие: пользователь явно выбрал PHP SDK и, для Laravel, профиль с Larastan.

Требование: SDK должен возвращать адресуемые объявления и места вызовов с доступной типовой информацией и происхождением; отличать синтаксис от вывода анализатора. Неразрешённый, неоднозначный, виртуальный или внешний target должен оставаться явно ограниченным свидетельством: декларация интерфейса не объявляется единственной runtime-реализацией, а виртуальному методу не выдумывается файл. PHP-профиль без Laravel не требует Larastan.

Проверка: одноимённые методы разных классов, наследование, интерфейс без известной реализации, динамическое имя, внешний/виртуальный метод; отдельно — заранее выбранные Laravel-примеры Eloquent, facade и container binding с ожидаемыми ограничениями.

Источник: §17.1, G2…G3. Обоснование: поиск по имени теряет типовой контекст; ложная точная ссылка опаснее честной неизвестности. Зависимость: SA-006, SA-009.

### REQ-SA-032 — Факты доходят до аудита и человека

Условие: для snapshot доступны проверенные SDK-факты.

Требование: выбранные факты должны помогать собирать ограниченный контекст кода и кандидатов тестов для Task и отображаться в навигации с явным происхождением. Они не принимают нормы автоматически и не заменяют чтение assertions. Вызов нужного метода в тесте — кандидат связи, не доказательство relevant; отсутствие ребра не доказывает missing. Смысловая связь ТЗ ↔ код/тест возникает из цитат и оценки роли/хоста, а не из PHPStan. Ручные trace-маркеры не требуются.

Проверка: тест с подходящим вызовом, но слабым assertion остаётся weak; одноимённый посторонний метод не подменяет цель; общий helper не теряет значимый контекст. Подсказка SDK и подтверждённая аудитором связь различимы в Task/HTML. Без SDK прежний аудит работает с явным ограничением, без ложного заявления полноты.

Источник: §17.1, G3, G5. Обоснование: отдельный JSON фактов сам по себе не улучшает рабочий цикл. Зависимость: SA-018, SA-021…025, SA-031.

### REQ-SA-033 — Проверяемое происхождение и совместимость

Условие: SDK-факты импортируются или повторно используются.

Требование: версия формата, выбранные файлы/диапазоны, версии SDK/анализаторов и основание анализа должны проверяться Go-ядром. В основание входят composer.lock, используемые конфиги/расширения анализатора и значимые источники типизации, включая объявленные bootstrap/stubs/модели/миграции. Изменение основания не позволяет переиспользовать прежние факты как текущие или сохранять прежнее согласование текущим. Данные вне выбранной области должны иметь явную границу происхождения, не становиться незаметно доступными цитатами. `sdk/2`, прежние run и raw остаются совместимой историей, без ретроспективного обогащения.

Проверка: подмена пути/цитаты/lock/config, смена SDK или контекста типизации, неполный/оборванный экспорт, чужой snapshot и старый syntax-only run; старые выводы не «зеленеют» после обновления фактов. Ошибки PHPStan и факты экспортёра учитываются отдельно: lint-диагностика не становится GAP ТЗ автоматически.

Источник: §17.1, G4. Обоснование: корректный тип зависит не только от текста одного метода. Зависимость: SA-010…012, SA-020, SA-031…032.

### REQ-SA-034 — Явный безопасный запуск адаптера

Условие: требуется запуск PHPStan/Larastan через SDK.

Требование: запуск разрешает хост, только в проверенном Docker runtime выбранного проекта, с фиксируемыми версиями, ограничениями времени/вывода и выделенной рабочей областью. Установка/обновление Composer-зависимостей, изменение проектного CI/config и host-PHP fallback не выполняются автоматически. Отсутствие зависимости, несовместимость или ошибка bootstrap дают явный отказ/неполный результат, не успешный пустой граф. Кеши и временные конфиги не пишутся в бизнес-исходники; изоляция должна исключать обращения к рабочим БД, очередям и внешним сервисам.

Larastan поднимает контейнер приложения, поэтому его запуск нельзя считать простым чтением исходников; это отдельно отмечено в [официальном описании](https://github.com/larastan/larastan). Разрешение syntax-only SDK или тестов не означает автоматического разрешения framework bootstrap. Невозможность обеспечить нужную изоляцию блокирует этот запуск, но не обычный аудит по исходникам.

Проверка: неправильный mount, отсутствующий анализатор, bootstrap с попыткой записи/сетевого доступа, недоступная среда; нет скрытой установки и записи в бизнес-проект. Версии и параметры контейнера задаются проектом, названий и путей пилота в общем SDK нет.

Источник: §17.1, G1. Обоснование: bootstrap способен выполнять проектный код, несмотря на название «статический анализ». Зависимость: SA-009, SA-014…015, SA-033.

### 17.2 Порядок реализации и приёмка

1. Зафиксировать точные JSON/CONFIG/Task-контракты, набор поддерживаемых конструкций, лимиты, совместимость версий и отрицательные ожидания до изменения кода. Не придумывать уже доступные CLI-флаги: текущий `php-facts` остаётся syntax_only до отдельного расширения.
2. Реализовать минимальный экспорт/валидацию на независимом PHP-примере, затем Laravel-профиль на синтетическом приложении. PHP остаётся внутри контейнера, Go — ядром. Наличие Larastan в другом проекте не закрывает эту приёмку.
3. Довести проверенные факты до контекста ролей и навигации; пройти SA-031…034, включая ложных кандидатов тестов, unknown и stale. Одного успешного PHPStan/экспорта недостаточно.
4. После отдельного разрешения сравнить аудит с SDK и без него на одной редакции ограниченного пилотного scope: найденные/пропущенные связи и assertions, ложные findings, время, размер контекста и накладные расходы bootstrap. Эталон не подстраивать под экспорт; отрицательный результат сохранить. Установка SDK не означает доказанного улучшения.

Следующий агент начинает с пункта 1, а не с переноса старого trace или запуска полного бизнес-корпуса. Точный контейнерный профиль и разрешения пилота — в [спецификации применения](smsplace-adoption.md#обкатка-phplaravel-sdk).

## 18. Поставка: версия, обновление и установка skill

Причина редакции 0.7 — решение владельца 2026-09-16: инструмент поставляется как подписанный релиз GitHub с явными командами `version`, `update`, `skill install|update` и скриптом первичной установки `install.sh`. Статус: реализовано и принято 2026-09-16 на релизе `v0.1.0` — нативная квалификация обеих платформ в workflow, установка на хосте владельца, замена ручной копии в пилоте; свидетельства и ограничения (Linux только на runner) — в README «Приёмка и ограничения». Сквозной цикл из чистой AI-сессии через установленный launcher — отдельный этап. Frozen §1–10 и §6 не меняются; `init CONFIG` по-прежнему создаёт только YAML (REQ-SA-001, §12.3) — установка skill остаётся отдельной явной командой, не флагом `init`. Образец дизайна — открытый инструмент Pri-Fly (`install.sh` + manifest с sha256, `update` с проверкой Ed25519); это образец, не норма.

Отношение к REQ-SA-015: `update` пишет только собственный исполняемый файл, `skill install|update` — только каталог `DIR/.spec-audit/skill/` и две относительные host-ссылки. Это отдельные явно вызванные операции владельца, не служебные записи аудита; во время аудита они не выполняются. Встроенные в бинарник документы launcher (`SKILL.md`, протокол роли, зеркало §14, контракты принятого индекса, legacy-извлечения и PHP SDK) — собственные контракты инструмента, не бизнес-нормы проверяемого проекта. Известное решение владельца: контракты принятого индекса и legacy-извлечения содержат навигационные ссылки на README/спецификацию checkout и на документ применения пилота как внешний указатель; файлы заморожены хэшами и при встраивании не редактируются, поэтому в установленной копии эти ссылки вне копии не разрешаются. Линт самодостаточности (нет `](../`, все относительные ссылки существуют, нет абсолютных путей и ID задач) применяется к собственным текстам launcher; к встроенным документам — только отсутствие абсолютных путей и ID задач.

### REQ-SA-035 — Подписанный релиз и явное обновление

Условие: пользователь вызывает `version` или `update` без аргументов.

Требование: `version` возвращает `{"version","release","os","arch"}`, где `release` истинно только в сборке с версией вида `X.Y.Z` и встроенным публичным ключом Ed25519. `update` выполняет проверки строго в порядке: манифест `release-manifest.json` (≤ 1 MiB, лимит `maxConfig`) → подпись `release-manifest.sig` (ровно 64 байта) → проверка Ed25519 над точными байтами манифеста → строгий JSON `{"schema_version":"spec-audit-release/1","version":"X.Y.Z","assets":[{"os","arch","file","sha256"}]}` без лишних/повторных полей → версия строго больше текущей по трём числам, иначе успешный no-op `{"updated":false}` без записи → ассет с точным совпадением `os`/`arch` текущей сборки и именем `spec-audit-<os>-<arch>` → файл ассета (≤ 32 MiB, лимит `maxFile`) → SHA-256 файла равен `sha256` манифеста → атомарная замена собственного исполняемого файла (temp + rename в каталоге бинарника, права 0755, общий `atomicWrite`). Сборка из исходников (`version` = `dev` или без ключа) и исполняемый файл с именем, отличным от `spec-audit`, отказывают до любого обращения к сети. Один общий таймаут HTTP-клиента на запрос, достаточный для скачивания ассета на медленном канале (в редакции 0.7 — 15 минут; 120 с в v0.1.0/v0.1.1 обрывали загрузку); статус ответа вне 2xx — отказ. Сообщения об ошибках и журнал не содержат URL, байтов ответа и содержимого файлов (практика `main.go`: входные данные не попадают в логи через диагностику декодера). Любой отказ на любом шаге оставляет исполняемый файл байт-в-байт прежним.

Ключи: приватный ключ Ed25519 хранится только секретом окружения `release` GitHub Actions и используется единственным публикующим job; публичный ключ встраивается в бинарник при сборке через `-ldflags -X`. Подпись — 64 сырых байта над файлом манифеста, совместимая с `openssl pkeyutl -sign -rawin` (OpenSSL 3) и `crypto/ed25519.Verify`. Бинарник не содержит модельных API и секретов (§12.1).

Проверка: dev-сборка и переименованный бинарник — отказ без сетевого вызова; неверная подпись, повторные/лишние поля манифеста, версия не `X.Y.Z` — отказ до скачивания ассета; равная и меньшая версия — `updated:false`; отсутствие ассета платформы, несовпадение sha256, превышение лимита, HTTP 404 — отказ с неизменённым бинарником; успешный путь — байты заменены, права 0755, следующий `version` показывает новую версию; тексты ошибок без URL и байтов ответа; подпись OpenSSL 3 принимается Go.

Источник: решение владельца 2026-09-16, ROADMAP «Воспроизводимый выпуск macOS/Linux». Обоснование: без подписи и sha256 обновление доверяет любому ответу сети; без явной команды обновление стало бы скрытой служебной записью. Зависимость: SA-014, SA-015, SA-038.

### REQ-SA-036 — Явная непривилегированная первичная установка

Условие: пользователь выполняет `install.sh` из релиза.

Требование: скрипт — POSIX `sh` с `set -eu`, без `sudo`, без правки shell-профилей и системных каталогов; параметры `SPEC_AUDIT_RELEASE_BASE` (по умолчанию GitHub `releases/latest/download`) и `SPEC_AUDIT_INSTALL_DIR` (по умолчанию `$HOME/.local/bin`). Платформа определяется `uname` и должна быть `darwin/arm64` или `linux/amd64`, иначе отказ до обращения к сети. Скрипт скачивает манифест и ассет одного и того же релиза, извлекает `sha256` ассета из манифеста без внешних JSON-инструментов, проверяет его `shasum -a 256` или `sha256sum` (нет ни одного — отказ), затем публикует файл staged-переименованием с правами 0755 и печатает подсказку о PATH и следующем шаге (`spec-audit skill install --dir <проект> --host codex|claude|both`). При любом отказе временные файлы удаляются и бинарник не установлен. Скрипт не подменяет установку сборкой из исходников и не устанавливает языковой runtime.

Граница доверия первой установки — HTTPS до GitHub Releases: подпись манифеста при первой установке не проверяется, это заявлено явно, а не скрыто. Дальнейшие обновления идут только через подписанный `update` (REQ-SA-035).

Проверка: неподдерживаемая платформа — отказ без сети; `sha256` ассета не совпадает с манифестом — отказ, staged-файл удалён, в `SPEC_AUDIT_INSTALL_DIR` ничего нет; успешная установка через `SPEC_AUDIT_RELEASE_BASE=file://…` без сети; `sh -n` и, при наличии, `shellcheck` без замечаний.

Источник: решение владельца 2026-09-16, образец Pri-Fly. Обоснование: одна команда установки без привилегий снимает зависимость пилота от checkout инструмента, не расширяя права скрипта. Зависимость: SA-035, SA-038.

### REQ-SA-037 — Установка skill только по явной команде

Условие: пользователь вызывает `skill install` или `skill update` с флагами `--dir DIR` (по умолчанию текущий каталог), обязательным `--host codex|claude|both` и необязательным `--replace`.

Требование: skill встроен в бинарник; версия skill равна версии бинарника, сеть и checkout не нужны. Раскладка: реальный каталог `DIR/.spec-audit/skill/` с `SKILL.md`, `references/protocol.txt`, `references/host-decision.md` (зеркало §14), `references/docs/*.md` и receipt `.spec-audit-skill.json` = `{"schema_version":"spec-audit-skill/1","version":"X.Y.Z|dev","files":{"<rel>":"<sha256>"}}` (ключи отсортированы, сам receipt в `files` не входит); относительные host-ссылки `DIR/.agents/skills/spec-audit` и/или `DIR/.claude/skills/spec-audit` → `../../.spec-audit/skill` по выбранному `--host` (каталоги `.agents/skills`/`.claude/skills` создаются только при явном выборе host). Все проверки состояния (включая существующих предков host-ссылок) выполняются до первой записи; отказ по состоянию оставляет дерево `DIR` байт-в-байт. Ошибка ввода-вывода во время записи возвращается как отказ; свежая копия публикуется целиком через staging-каталог (§57), поэтому повторный `skill install` устанавливает заново, а прерванное обновление нашего каталога завершается следующим `update`; чужие файлы при этом не затрагиваются. Все операции с путями — внутри `os.Root(DIR)`; выход symlink-ом за `DIR` отвергается. Команда не читает и не меняет `.gitignore`, Git, CONFIG и чужие файлы; лишние файлы в каталоге skill не учитываются и не удаляются. Ответ: `{"dir","skill","version","updated","links":{"<rel>":"created|kept|relinked"}}`.

| Состояние `.spec-audit/skill` | `install` | `install --replace` | `update` | `update --replace` |
| --- | --- | --- | --- | --- |
| отсутствует | создать | создать | отказ «сначала skill install» | отказ |
| наш каталог, digest равны | `updated:false` | `updated:false` | `updated:false`, без записи | `updated:false` |
| наш каталог, drift управляемого файла | отказ со списком путей | переписать только управляемые файлы | отказ со списком путей | переписать только управляемые файлы |
| symlink (прежняя ручная установка) | отказ | снять ссылку (цель не трогать), создать каталог | отказ | отказ |
| чужой каталог без receipt | отказ | отказ | отказ | отказ |

| Состояние host-ссылки | без `--replace` | с `--replace` |
| --- | --- | --- |
| отсутствует | создать (`created`) | создать |
| наша ссылка на `../../.spec-audit/skill` | оставить (`kept`) | оставить |
| чужая ссылка | отказ | перелинковать (`relinked`), цель не трогать |
| реальный файл/каталог | отказ | отказ |

Проверка: свежая установка для `codex`, `claude`, `both`; отсутствие `--host`, неверный host, `DIR` не каталог — JSON-отказ без записи и без usage-текста в stderr (§6); symlink на месте skill без и с `--replace` (цель ссылки нетронута); чужой каталог, чужая host-ссылка, реальный путь; `update` без изменений — байты и mtime неизменны; drift без и с `--replace` при сохранении чужих файлов; `update` без receipt; symlink `.spec-audit` наружу `DIR`; для каждого отказа обход `DIR` до и после идентичен.

Источник: решение владельца 2026-09-16 (замена symlink в пилоте разрешена только явным `--replace`), ROADMAP «Безопасная установка audit-skill». Обоснование: единственный источник skill — бинарник, поэтому обновление детерминировано и без дублей, а чужие настройки агента сохраняются. Зависимость: SA-001, SA-015, §12.1, §12.3.

### REQ-SA-038 — Матрица платформ и квалификация

Условие: публикуется релиз или заявляется поддержка платформы.

Требование: матрица релиза — только `darwin/arm64` и `linux/amd64`. Ассет платформы включается в манифест только после нативного исполнения собранного бинарника на runner той же платформы: `version` → `release:true`; `index` независимого синтетического CONFIG; `skill install`, затем `skill update` → `updated:false`; `update` релизной сборки не изменяет собранный бинарник (отказ или `updated:false`), а dev-сборка отказывает в `update` до обращения к сети. macOS собирается на native arm64 runner с проверкой `uname -m`; cross-build без исполнения не является свидетельством поддержки (RULES «Честный статус»). Иные платформы отказывают до скачивания и в `install.sh`, и в `update`. Публикация — GitHub Actions по тегу `vX.Y.Z` с одним публикующим job в окружении `release`; версия бинарника берётся из тега. Публикуются: `install.sh`, `release-manifest.json`, `release-manifest.sig`, `spec-audit-darwin-arm64`, `spec-audit-linux-amd64`.

Проверка: workflow отказывает без публичного ключа и при `uname -m` ≠ `arm64` на macOS-job; манифест содержит ровно два ассета с именами по шаблону; `release-manifest.sig` = 64 байта; установленный бинарник на хосте владельца отвечает `release:true` и `update` → `updated:false` сразу после релиза. Linux квалифицируется только job GitHub Actions — нативного Linux-хоста у владельца нет, это фиксируется в README.

Источник: решение владельца 2026-09-16. Обоснование: заявленная платформа без исполнения — необоснованное утверждение. Зависимость: SA-035, SA-036.

### 18.1 Границы

- Управляемая установка бинарника не имеет receipt: сборка с ключом и имя файла `spec-audit` — достаточные условия `update`; ручная копия релизного бинарника обновляется так же.
- Первый релиз — `v0.1.0`; прежние сборки считаются dev и `update` не поддерживают.
- `skill update` не удаляет чужие и устаревшие файлы; переименование управляемых файлов в будущих версиях потребует явной миграции.
- `skill uninstall` не вводится до потребности; удаление описывается в README вручную.
- Игнорировать или трекать установленную копию skill, host-ссылки и CONFIG решает владелец проекта; инструмент `.gitignore` не читает. В изолированных worktree-сессиях агентов доступны только tracked-файлы, поэтому игнорируемая установка там невидима; при tracked-варианте `skill update` меняет tracked-файлы и требует коммита.
- Windows и другие платформы не поддерживаются; замена запущенного бинарника переименованием безопасна на darwin/linux.

## 19. Уроки чистой сессии: проверка ответа роли, провенанс версии, ответ reconcile

Причина редакции 0.8 — прогон из чистой AI-сессии 2026-09-16 (пилот и синтетический проект): хосту пришлось писать собственный валидатор формы ответа роли; версия бинарника, которой выполнялись команды run, нигде не фиксируется; ответ `reconcile` после применения решения не содержит `freshness`; протокол роли предписывал Codex-специфичный инструмент записи. Раздел вводит две нормы и два уточнения расширениями: frozen §1–10 (в том числе список CLI §6, контракт ответа §7 и неизменяемый manifest §9) и контракт [принятого индекса](accepted-index.md) не редактируются. Пункты о reference-only файлах ТЗ вне лимита 64 кандидатов и о разбиении больших accepted-наборов решены владельцем 2026-09-17 в §20.

### REQ-SA-039 — Проверка ответа роли без записи

Условие: хост вызывает `validate CONFIG RUN_ID TASK_ID RESULT_PATH` для существующего run.

Требование: команда выполняет ровно те проверки, что `submit` до первой записи: лимит 4 MiB и строгий UTF-8 JSON (§6, §7), полный набор полей без null и лишних ключей, допустимые значения состояний, полный учёт назначенных ID (REQ-SA-005), привязка цитат к текущему snapshot с чтением источников (REQ-SA-006), канонический размер. Она не создаёт results-файл, не меняет entry/attempt, state, производные отчёты и журналы; дерево run остаётся байт-в-байт прежним. Устаревший snapshot и неизвестный TASK_ID отклоняются так же, как у `submit`; `project_root` обязателен. Успех возвращает `{"valid":true,"task_id","attempt","already_submitted"}` и не гарантирует последующий `submit`: источники, состояние и дубликаты проверяются им заново. Ошибка формы для конкретной оценки называет ID нормы; содержимое цитат и ответа в текст ошибки и журнал не попадает (REQ-SA-015).

Проверка: валидный ответ → `valid:true`; каждая мутация формы отклоняется тем же текстом, что у `submit`; байты `state.json`, `results/` и `report.*` до/после идентичны, новых записей в каталоге run нет; stale-run — отказ; вызов через процесс отдаёт JSON в stdout, отказ — JSON в stderr.

Источник: прогон чистой сессии 2026-09-16 (хост писал валидатор вручную), ROADMAP «Уроки чистой сессии». Обоснование: роли проходили `submit` с первой попытки только благодаря локальной предпроверке; дублировать её вне бинарника — источник расхождений. Зависимость: SA-005, SA-006, SA-012, SA-014, SA-015.

### REQ-SA-040 — Журнал версий бинарника run

Условие: пишущая команда (`prepare`, `submit`, `retry`, `test`, `php-typed`, `review` с DECISION) успешно публикует state или журнал согласования run.

Требование: в каталоге run ведётся append-only файл `tool-versions.json` = `{"version":1,"records":[{"command":"<команда>","tool_version":"<версия бинарника>","recorded_at":"<RFC 3339 UTC>"}]}` до 32 MiB. До публикации state/host-reviews команда читает и проверяет журнал (строгий JSON, полный набор полей), формирует новую запись и проверяет размер — любой отказ на этом шаге оставляет run нетронутым, повреждённый или переросший журнал отклоняет пишущую команду. После успешной публикации записываются только уже проверенные байты; сбой этой записи — предупреждение в журнале диагностики, не отказ (публикация state — точка фиксации, §9). Команды без публикации state (`status`, `report`, `review` без DECISION, `validate`, `tasks`, `php-facts`) записей не добавляют и не предупреждают о смене версии, но проверяют журнал и не скрывают повреждение как отсутствие (как `host-reviews.json`, §14). Старые run без файла читаются без миграции; `snapshot_id`, основание согласования §14, байты `manifest.json` и `state.json` от журнала не зависят. Запись с версией, отличной от предыдущей, допускается с предупреждением: смена версии после `update` легитимна, но провенанс смешанного run должен быть виден. `report` показывает список версий (`tool_versions`); версия `dev` фиксируется как есть и не является уникальной идентичностью сборки.

Проверка: prepare → два submit → review дают четыре записи в порядке команд; отклонённый submit не создаёт и не меняет файл; read-only команды не меняют байты журнала; копия исторического run без файла обслуживается, `report` без `tool_versions`; повреждённый журнал отклоняет submit до записи state и отклоняет `report`; сбой записи после публикации — предупреждение при успешной команде; смена версии — предупреждение и обе версии в отчёте; `snapshot_id`, основание review и байты manifest неизменны.

Источник: инцидент 2026-09-16 — во время run пилота бинарник обновлялся 0.1.0→0.1.3, провенанс восстановить нельзя. Обоснование: поле в manifest меняло бы переносимую идентичность (REQ-SA-003), поле в state — основание §14 и читаемость run старым бинарником; отдельный журнал по образцу §14 ничего из этого не затрагивает. Зависимость: SA-003, SA-012, SA-013, §9, §14.

### 19.1 Ответ reconcile после применения решения

`reconcile CONFIG RAW DECISION` при первом применении возвращает те же поля, что читающий вызов (`base_index`, `source_set`, `records`, `history`, `journal`, `freshness`, `semantic_completeness_proven:false`), плюс `accepted:true` и `duplicate:false`; `freshness` измеряется повторным сканом источников под удерживаемой блокировкой, а не предполагается: если ТЗ изменилось между проверкой и записью, ответ честно показывает `stale`. Точный повтор решения по-прежнему возвращает `duplicate:true` и `freshness_checked:false` без `freshness` — исторический duplicate не подтверждает свежесть. Каждая запись `records[]` дополнительно несёт `revision` и `clarity`, равные `requirement.accepted.revision/clarity`; сама структура `requirement` не меняется, потому что она хэшируется в `snapshot_id` и передаётся ролям. Контракт принятого индекса не редактируется — это дополнение ответа.

### 19.2 Протокол роли и запись ответа

Протокол роли не называет инструмент конкретного хоста: роль записывает только свой OUTPUT_PATH любым инструментом записи файлов, который даёт хост, и может собирать большой ответ в этом файле по частям; итог — один JSON-документ до 4 MiB (§6), формат цитат §7 неизменен. Бинарник видит только итоговый файл. Компактный формат цитат не вводится — это отдельное решение владельца по frozen §7 и REQ-SA-006.

## 20. Справочные источники ТЗ и деление большого набора

Причина редакции 0.9 — прогон пилота 2026-09-16: документ, определяющий смысл терминов раздела, пришлось исключить из scope из-за лимита 64 кандидатов на пакет, и пять норм остались ambiguous с нерешённым «см. документ-источник»; при `scopes: []` одна роль получила 49 норм и 2509 файлов (27–36 минут и 0,5–0,6 M токенов на роль). Решение владельца 2026-09-17: справочные источники вводятся отдельной группой CONFIG, лимиты и frozen-контракты не меняются; деление большого набора остаётся осознанным решением хоста (§6), бинарник лишь предупреждает. §1–10, контракт [принятого индекса](accepted-index.md) и [формат извлечения](legacy-extraction.md) не редактируются.

### REQ-SA-041 — Справочный источник без кандидатов

Условие: CONFIG с `index_mode: accepted` содержит группу `references: {paths, include, exclude}`.

Требование: файлы группы — источники ТЗ (`kind: spec`) с признаком `reference: true` в snapshot, manifest, `TaskBatch.files` и ответе `reconcile` (`source_set[].reference`); они хэшируются, входят в source_set извлечения и в проверку свежести наравне с нормативными файлами (accepted-index п.6, REQ-SA-010) и могут цитироваться кандидатами и ролями по прежним правилам (REQ-SA-006, accepted-index п.8). Они не порождают кандидатов: `reconcile` отклоняет accept/rebind/revise/split/merge, если у target нет ни одной цитаты из нормативного файла; reject/defer/retire не проверяются. Лимиты 64 кандидатов, 64 active-норм и 4 MiB не меняются — справочные файлы их не расходуют. Группа допустима только при `index_mode: accepted`, требует непустой `paths` и подчиняется правилам §6 для групп (существующие пути, glob только имени файла, файл не может попасть в две категории, reports_dir не пересекается с входным деревом). Отсутствие группы сохраняет прежние байты manifest и `snapshot_id`; её наличие и состав входят в идентичность snapshot (REQ-SA-003). Отчёт показывает справочный файл отдельной строкой без норм, не выдавая отсутствие кандидатов за отсутствие обязанностей (REQ-SA-025, REQ-SA-028). Изменение справочного файла или самой группы делает принятый индекс stale до новой полной приёмки; облегчённого rebind без нового raw нет (контракт п.4–6).

Проверка: группа без `index_mode`, с пустым `paths` или с файлом из `specs` — отказ; читающий `reconcile` показывает справочный файл с `reference:true`; кандидат с нормативной и справочной цитатами принимается с обеими цитатами, кандидат только со справочными — отказ с ID кандидата без записи в журнал, тот же кандидат как reject/defer принимается; raw без справочного файла в source_set — прежний отказ; после приёмки `prepare` передаёт обе цитаты и файл с признаком, spec-цитата роли в принятом справочном диапазоне принимается, вне диапазона — отказ; правка справочного файла и добавление группы после приёмки дают `stale`; `report` показывает секцию с `reference:true` и нулём норм и группу `reference`; manifest прежних run без группы байт-в-байт неизменен.

Источник: прогон scope финансов 2026-09-16 (пять ambiguous из-за исключённого документа-источника); решение владельца 2026-09-17. Обоснование: определяющие смысл фрагменты нужны для точной нормы (REQ-SA-016, SA-017), но извлечение из большого справочного документа исчерпывает пакет; расширение лимита или многопакетный срез меняли бы frozen-контракт. Зависимость: SA-002, SA-003, SA-006, SA-010, SA-016, SA-025, SA-028; accepted-index п.6, п.8.

### 20.1 Деление большого набора: предупреждение бинарника и правило launcher

`scopes: []` по-прежнему назначает все нормы одному scope `all`; бинарник не режет нормы по числу токенов и не создаёт scope автоматически (§6: «слишком большой scope требует осознанного деления»). Рекомендуемый размер scope — не более 24 норм (`scopeAdvisoryRequirements`): 18 норм на 50 файлов прошли сравнительный run без затруднений, 49 норм на 2509 файлов заняли 27–36 минут и 0,5–0,6 M токенов на роль; 64 остаётся жёстким пределом. `index` возвращает `advisories` — список строк по каждому scope с числом норм выше рекомендации (ключ отсутствует, когда список пуст); `index` и `prepare` дублируют каждую строку предупреждением в журнале диагностики; stdout `prepare` (TaskBatch) не меняется. Порог — эвристика по двум замерам, не измеренная граница качества.

Правило launcher: если `index` вернул `advisories` или active-набор больше 24 норм, хост до `prepare` задаёт `scopes` в scope-конфиге — группирует active ID по подразделам ТЗ в порядке первой принятой цитаты, по 16–24 нормы, `focus` называет подразделы, каждый active ID назначен ровно один раз (пропуск и повтор отклоняются, §6); после решения, добавляющего или снимающего ID, `scopes` обновляются. Список файлов роли не сужается по scope: скрытый файл даёт `unknown`, а не свидетельство. Роли разных scope выполняются параллельно в пределах доступных слотов; согласование хоста остаётся одним DECISION на run (§14). Проверка: 25 норм при `scopes: []` — одна строка `advisories`; 24 — ключа нет; явные scope 13+12 — нет; 65 при `scopes: []` — прежний отказ; корпус из пяти норм не пишет предупреждений.

## 21. Уроки второго прогона: проверка приёмки без записи, подсказки сопоставления, изоляция ролей

Причина редакции 1.0 — второй чистый прогон 2026-09-17 (scope со справочным источником и тремя scope): хост снова писал зеркало проверок raw и вычислял content hash, чтобы выбрать rebind или revise до записи; после смены source_set свежее извлечение переформулировало все кандидаты, сопоставление с прежними ID делалось вручную по пересечению цитат, rebind оказался недостижим (0 из 49) и revision выросла у всего набора без смены смысла; две параллельные роли делили общий рабочий каталог, и одна выполнила одноимённый скрипт другой. Решение владельца 2026-09-17: команда `check` по образцу `validate` (§19), подсказки сопоставления как часть её ответа, изоляция роли в собственном каталоге. §1–10 (в том числе список CLI §6), §14, контракт [принятого индекса](accepted-index.md) и [формат извлечения](legacy-extraction.md) не редактируются; облегчённая форма host DECISION (§14) отложена до следующего прогона.

### REQ-SA-042 — Проверка приёмки без записи

Условие: хост вызывает `check CONFIG RAW` или `check CONFIG RAW DECISION` при `index_mode: accepted`.

Требование: команда выполняет ровно те проверки, что применяющий `reconcile` до записи, и в том же порядке: чтение журнала (повреждённый или переросший журнал — отказ, как у apply), скан источников и чтение выбранных файлов, схема raw и лимиты, полнота и хэши source_set, точные цитаты, признак нормативной цитаты (REQ-SA-041); с DECISION дополнительно — строгий разбор решения (до чтения журнала, как у apply), точный повтор decision_id (ответ `duplicate:true` без чтения источников — исторический duplicate не подтверждает свежесть), повтор decision_id с другими байтами (отказ), лимит 128 пакетов, guard нормативной цитаты, полный прогон операций по контракту принятого индекса (base_index, raw_sha256, учёт всех кандидатов и прежних active ID, rebind/revise по content hash, выдача ID) и размер будущего журнала. Ответ без DECISION: `{"valid":true,"base_index","freshness","source_set":[{path,sha256,reference}],"candidates":[{"id","clarity","normative","matches":[…]}]}`; с DECISION дополнительно `"duplicate"`, `"next_head"`, `"assignments":[{"candidate","requirement_id","action","revision","previous":[…]}]` и `"retired":[…]` — ровно те ID и редакции, которые получил бы apply тех же байтов при неизменных источниках и журнале. Команда ничего не создаёт и не меняет: ни каталог reports_dir, ни lock, ни журнал; блокировка не берётся. Успех не гарантирует последующий apply: источники, журнал и base_index проверяются им заново под блокировкой. Тексты ошибок — те же, что у apply (первая ошибка); содержимое цитат в ошибку и журнал диагностики не попадает (REQ-SA-015).

Проверка: declared-профиль — отказ; без reports_dir — `valid:true`, `freshness:"uninitialized"`, каталог не появляется; каждая мутация raw и решения даёт тот же текст, что apply; повреждённый журнал — отказ; accept/rebind/revise/split/merge/retire/reject/defer дают предсказанные ID и редакции, последующий apply тех же файлов даёт `base_index`, равный предсказанному `next_head`, и те же assignments; точный повтор — `duplicate:true` без `freshness`; байты журнала и список файлов reports_dir до/после неизменны; вызов через процесс — один JSON в stdout, отказ — JSON в stderr.

Источник: отчёт второго чистого прогона 2026-09-17 (≈20 минут на приёмку, зеркало проверок и хэша на хосте). Обоснование: как и для ответов ролей (REQ-SA-039), проверка вне бинарника — источник расхождений; операции контракта уже реализованы чистой функцией, и предпросмотр не требует второй реализации. Зависимость: SA-012, SA-014, SA-015, SA-039, SA-041; accepted-index п.1, п.3–4, п.6–7.

### 21.1 Подсказки сопоставления

Только `check CONFIG RAW` дополняет каждого кандидата списком `matches` — до трёх active-записей принятого индекса, ранжированных по пересечению точных строк цитат: `overlap` — коэффициент Жаккара множеств строк (`quote`, разбитая по переводам строк, без нормализации) кандидата и `accepted.citations` записи, `shared_lines` — число общих строк, `revision` записи и `fields_equal` — равны ли condition, statement, exceptions, clarity и unresolved кандидата полям записи (тогда rebind с прежними title и verification даст тот же content hash). Порог включения `overlap ≥ 0,1`. Основание: замер на реальном переходе пилота между двумя приёмками (49 кандидатов с прежним ID, истина — `previous` в решении хоста) — ранжирование по Жаккару даёт верную первую подсказку в 48 случаях из 49, при пороге 0,1 истина попадает в три подсказки в 49 из 49, при пороге 0,5 — только в 28 из 49, потому что добавленные цитаты справочника размывают пересечение; метрика покрытия прежней записи ранжирует хуже (39 из 49). Пути и номера строк не участвуют, поэтому подсказки устойчивы к сдвигу текста; retired-записи не учитываются. Подсказка — не смысловая эквивалентность и не решение: бинарник по-прежнему не вычисляет эквивалентность (контракт п.3), а DECISION хоста остаётся полным и явным. Читающий `reconcile` не меняется: у него нет raw.

### 21.2 Изоляция роли и размер scope

Launcher выдаёт каждой роли собственный каталог `reports_dir/dispatch/<task_id>/` для OUTPUT_PATH и любых вспомогательных файлов и скриптов; параллельные роли не делят рабочий каталог и имена файлов. Протокол роли запрещает читать и выполнять файлы других ролей и создавать вспомогательные файлы вне каталога своего OUTPUT_PATH; это контекстное разделение, не ОС-песочница (§9). Рекомендация размера scope уточняется: около 16–24 норм, при этом подраздел ТЗ не разрезается ради нижней границы — граница подраздела важнее нижнего предела. Перед `reconcile CONFIG RAW DECISION` launcher выполняет `check CONFIG RAW` и `check CONFIG RAW DECISION`.

## 22. Уроки третьего прогона: перепривязка с сохранением содержания, подсказки, диагностика scopes

Причина редакции 1.1 — третий чистый прогон 2026-09-17 (переприёмка после правки ТЗ): свежее извлечение переформулировало все кандидаты, rebind не был доступен ни разу (0 из 50), после двух переизвлечений 33 нормы получили revision 3 без смены смысла, а ambiguous выросло с 8 до 12 из-за перефразированных unresolved; подсказки `check` дали 4 ложных первых места из 50 на строках, общих для многих кандидатов; `index` после приёмки отказал без имени scope и ID; dry-run `check` показал `freshness:"stale"` у пакета, который эту устарелость закрывает. Решение владельца 2026-09-17: новое действие `reanchor`, поле `unique_shared` и лимит четырёх подсказок, диагностика `scopes`, признак `base_index_current`. §1–10, §14, контракт [принятого индекса](accepted-index.md) не редактируются: `reanchor` реализует уже разрешённый п.3 путь «сохранение прежней принятой формулировки». Облегчённая форма host DECISION остаётся отдельным решением.

### REQ-SA-043 — Перепривязка с сохранением принятого содержания

Условие: DECISION содержит операцию `reanchor` с одним прежним active ID в `previous` и одним кандидатом в `targets`.

Требование: принятое содержание нормы — title, condition, statement, verification, exceptions, clarity, unresolved — вместе с ID, revision, content hash и parents сохраняется из прежней записи; цитаты и source берутся из кандидата. `title` и `verification` в target обязаны быть пустыми строками: хост ничего не переформулирует, а лишь утверждает, что кандидат — та же норма в новой редакции текста; за эту эквивалентность отвечает хост, бинарник её не вычисляет (контракт п.3). Кандидат учитывается ровно один раз, прежний ID получает ровно одно продолжение (п.1), guard нормативной цитаты действует (REQ-SA-041), формулировка кандидата остаётся в raw и в истории пакета. Операция не изменяет content hash и revision, поэтому счётчик редакций движется только при смысловой правке (`revise`); изменение clarity или unresolved требует `revise`. Прежние run после `reanchor` становятся историей, как после любой смены цитат (REQ-SA-010).

Проверка: после `reanchor` запись хранит прежние title/statement/verification/clarity/unresolved, content hash и revision, но новые citations и source; журнал воспроизводится детерминированно; непустой title или verification — отказ; previous не active или повторный — прежние отказы; кандидат только со справочными цитатами — отказ guard; `check CONFIG RAW DECISION` предсказывает `assignments[].action == "reanchor"` с прежней revision; после `prepare` роли получают новые цитаты, spec-цитата в старом диапазоне отклоняется.

Источник: отчёт третьего прогона 2026-09-17 (0 rebind из 50, 33 нормы на rev 3, ambiguous 8→12). Обоснование: п.3 контракта допускает сохранение прежней принятой формулировки, но без отдельной операции это возможно лишь при побайтовом совпадении текста из raw, чего свежий извлекатель не даёт; правка raw запрещена (п.1). Зависимость: SA-016, SA-041, SA-042; accepted-index п.1, п.3–4, п.8.

### 22.1 Подсказки сопоставления: `unique_shared` и четыре подсказки

Ранжирование подсказок по коэффициенту Жаккара строк цитат и порог 0,1 (§21.1) сохраняются: на двух переходах пилота (49 и 50 кандидатов с прежним ID) взвешивание строк по частоте или исключение общих строк ухудшило первый переход (верный top-1 48→43 из 49). Лимит подсказок — четыре (истина в четырёх подсказках: 49 из 49 и 50 из 50; в трёх — 49 и 49). Каждая подсказка дополняется `unique_shared` — числом общих строк цитат, которых нет ни у одной другой active-записи: по замеру бинарником на тех же двух переходах у верных первых подсказок оно больше нуля в 84 случаях из 92, у ложных — в 4 из 7; `unique_shared: 0` означает, что подсказка держится на строках, общих с другими нормами (заголовки таблиц, вводные), и требует проверки перед `reanchor`, а ненулевое значение верность не подтверждает. Подсказка по-прежнему не эквивалентность и не решение.

### 22.2 Диагностика `scopes`

Когда `scopes` в CONFIG расходятся с текущим набором норм (после приёмки, изменившей ID), `index`, `prepare` и любая команда, строящая snapshot, называют scope и ID: «scope <id>: неизвестные ID [...]; повторно назначенные [...]» для первого scope с расхождением и «не распределены по scope: [...]» для норм без владельца; списки отсортированы, не длиннее 64 элементов. Правила §6 не меняются: пустой `scopes` назначает все нормы одному scope `all`, лимиты прежние.

### 22.3 `check CONFIG RAW DECISION`: признак актуальности base_index

Успешный ответ dry-run с DECISION содержит `"base_index_current": true`: base_index решения равен текущему head, а операции применимы. Поле `freshness` в обоих режимах `check` описывает текущий принятый индекс до применения пакета (после apply source_set пакета совпадает с текущими источниками по построению); при `duplicate` и в режиме без DECISION признака нет.

## 23. Облегчённая форма host DECISION (version 2) и список ролей

Причина редакции 1.2 — третий чистый прогон 2026-09-17: host DECISION по §14 повторял цитаты обеих ролей (823 KB), хост тратил около четверти часа на чтение 102 оценок и сборку файла, а арифметическая ошибка в summary потребовала второй записи в append-only журнал. Решение владельца 2026-09-17: вторая, облегчённая форма решения — вердикт хоста со ссылкой на принятое свидетельство роли и машиносверяемые счётчики; полная форма §14 (`version: 1`) остаётся действительной и нужна, когда хост цитирует то, чего роли не приводили. §7, §14, основание CAS, журнал `host-reviews.json`, лимиты и состояния не меняются; журнал может содержать решения обеих форм.

### REQ-SA-044 — Вердикт хоста по свидетельствам роли

Условие: хост подаёт DECISION с `version: 2`.

Требование: документ — строгий UTF-8 JSON до 4 MiB со всеми полями: `version:2, review_id, run_id, snapshot_id, basis_sha256, reviewer, summary, verdicts, counts, limitations`; идентичность, review_id, reviewer, summary, основание, CAS, полнота доставки, дубликаты и конфликты review_id, лимиты журнала — по §14. `verdicts` содержит ровно одну запись на каждую норму run: `requirement_id, specification, implementation, assertion, concur, statement, limitations` с теми же допустимыми состояниями и правилом принятой неоднозначности, что у Assessment §7, но без цитат. `statement` может быть пустым только когда `concur: both` и вердикт совпадает с оценками обеих ролей по specification/implementation/assertion — тогда отчёт показывает стандартный текст «Совпадает с оценками обеих ролей.»; при любом расхождении или ином `concur` statement обязателен (§28.3). `concur` — `mapper`, `redteam` или `both`: свидетельствами хоста по норме считаются цитаты (spec, code, tests) названных ролей из текущих результатов run, при `both` — объединение без повторов; собственных цитат форма не содержит — для них используется `version: 3` (§25) или полная форма `version: 1`. `counts` — `supported, contradicted, implementation_unknown, relevant, weak, contradicts, missing, assertion_unknown, ambiguous`; бинарник пересчитывает их по verdicts и отклоняет решение при первом расхождении, называя поле и оба числа. Запись возможна только при полной доставке, поэтому у названных ролей всегда есть результат; журнал хранит точные байты. Отчёт и `review` показывают решение version 2 в общей форме: вердикт хоста рядом с принятыми цитатами и пометкой, чьи это свидетельства; для outdated-решения принятые цитаты берутся из текущих результатов ролей и не выдаются за цитаты момента решения — состояние outdated уже названо (REQ-SA-020), а основание §14 привязывает решение к состоянию момента записи.

Проверка: version 2 с `concur: both` принимается, состояние current, журнал содержит точные байты; `concur: mapper` даёт в отчёте только цитаты mapper; расхождение любого счётчика — отказ с именем поля; недопустимый `concur`, пропущенная или повторная норма, пустой statement при расхождении с оценкой роли или при `concur` ≠ both, принятая ambiguous-норма с `specification: clear`, `version: 3`, stale-основание, незавершённая доставка, повтор review_id с другими байтами — отказы без записи; журнал с решениями version 1 и 2 читается целиком; после `retry` роли решение становится outdated; отчёт подписывает решение хоста «свидетельства mapper / свидетельства redteam / свидетельства обеих ролей».

Источник: отчёт третьего прогона 2026-09-17. Обоснование: узкое место согласования — не запись, а чтение оценок и копирование чужих цитат; счётчики, проверенные бинарником, ловят ошибку хоста до записи в append-only журнал. Зависимость: SA-019, SA-020, SA-021, §7, §14.

### 23.1 Список ролей в ответе `review`

`review CONFIG RUN_ID` без DECISION дополняет контекст полем `roles[]` — по одной записи на задание: `task_id, role, scope, attempt, submitted` и `raw_sha256` при наличии результата; порядок — порядок entries. Поле `entries` не меняется. Ответ также содержит `form` последнего решения (`assessments` для version 1, `verdicts` для version 2) и `concur` — соответствие requirement_id → concur для version 2.

## 24. Уроки четвёртого прогона: таблица расхождений, черновик решения, материализация dispatch

Причина редакции 1.3 — четвёртый чистый прогон 2026-09-17, первое живое решение version 2: хост собирал таблицу «норма × роль → состояния» вручную из ответа `review` размером более мегабайта (самая дорогая операция шага согласования), сам выбирал имя, место и `review_id` файла решения и вручную нарезал каталоги ролей `dispatch/<task_id>/` из ответа `prepare` (с порчей кириллицы в промптах и риском подсунуть роли чужое задание). Решение владельца 2026-09-17: бинарник даёт производную таблицу расхождений, печатает пустой черновик решения и сам создаёт каталоги ролей. §7, §14, §23, основание CAS, журнал и состояния не меняются; `entries` и прежние поля ответа `review` сохраняются.

### REQ-SA-045 — Черновик решения `draft`

Условие: хост вызывает `draft CONFIG RUN_ID` после полной доставки ответов ролей.

Требование: команда ничего не публикует — не пишет журнал решений, state и запись провенанса в `tool-versions.json`; на stale-снимке отказывает как публикующие команды («снимок stale; нужен новый run»); при неполной доставке отказывает («нужны все ответы ролей; перечитайте review»). Ответ — полный документ version 2 по §23 в форме, которую принимает `review CONFIG RUN_ID DECISION` после заполнения: `version: 2`; `review_id: host-review-NNN`, где NNN — число записей журнала плюс один, три цифры; `run_id`, `snapshot_id`; `basis_sha256` текущего контекста (§14, с учётом последней записи журнала); пустые `reviewer` и `summary`; `verdicts` — по одной записи на каждую норму run в порядке manifest с `requirement_id` и пустыми `specification`, `implementation`, `assertion`, `concur`, `statement`, пустым `limitations`; `counts` — все девять ключей §23 с нулями; пустой `limitations`. Состояния и `concur` не предзаполняются даже при согласии ролей: решение остаётся суждением хоста, а не производной ролей (REQ-SA-019); незаполненный черновик отклоняется правилами §23 без записи. Любое изменение состояния run после `draft` (retry, receipt) меняет основание — хост перечитывает `draft`; `review_id` черновика хост не заменяет своим, иначе повтор идентификатора с другими байтами отклоняется как конфликт §14.

Проверка: до полной доставки — отказ; после — сериализованный черновик проходит строгий разбор формы version 2, заполненный по результатам ролей принимается с `review_state: current`; при одной записи в журнале черновик получает `host-review-002` и новое основание; после `retry` роли — снова отказ; `tool-versions.json` после `draft` байт-в-байт прежний; правка исходника после prepare — отказ «снимок stale»; исторический run без `dispatch/` обслуживается без создания каталогов.

Источник: отчёт четвёртого прогона 2026-09-17. Обоснование: хосту нужны все `requirement_id`, основание и ключи `counts` в одном месте; позиционный контракт CLI §1–10 не допускает флаг у `review`. Зависимость: SA-019, SA-044, §14, §23.

### 24.1 Таблица расхождений `outcomes[]`

`review CONFIG RUN_ID` без DECISION дополняет контекст производным полем `outcomes[]` — по одной записи на норму run в порядке manifest: `requirement_id`, `scope` (scope задания, которому назначена норма), `roles` — объект по имени роли (`mapper`, `redteam`) с `task_id, attempt, specification, implementation, assertion` из текущего результата этой роли, и `agree` — истина, только когда результаты обеих ролей есть и все три состояния совпадают. Роль без текущего результата (pending, после `retry`) в `roles` отсутствует, `agree` — ложь. Цитаты и statement в таблицу не копируются — они остаются в `entries`; limitations ролей и прошлый вердикт хоста добавляет §25; `entries`, `roles[]`, `form`, `concur` и прочие поля не меняются. Таблица — подсказка для чтения, не оценка: совпадение ролей не делает норму доказанной (RULES «Свидетельства»).

### 24.2 Материализация каталогов ролей

`prepare` создаёт для каждого задания каталог `reports_dir/<run>/dispatch/<task_id>/` с файлом `task.json` (объект задания, равный `tasks[i]` ответа `prepare`); список файлов snapshot, равный `files` ответа `prepare`, пишется один раз в `reports_dir/<run>/dispatch/files.json` (§26.3); `retry` перезаписывает `task.json` новой попыткой и не трогает остальные файлы каталога. Каталоги — производные от state: state сохраняется раньше, отказ записи каталога возвращает ошибку, но run остаётся пригодным (`tasks`, повторный `retry`). Протокол роли, prompt и OUTPUT_PATH в этот же каталог кладёт launcher (§21.2); каталог хоста — `dispatch/host/`, бинарник его не создаёт и не проверяет. Исторические run без `dispatch/` обслуживаются как прежде: `review`, `draft`, `status` и `report` каталоги задним числом не создают; `retry` на таком run пишет `dispatch/<task_id>/task.json` новой попытки без `files.json`.

### 24.3 Подпись свидетельств хоста

В run `finance-2026-09-17-03` хост дал по одной норме вердикт weak с `concur: both`, тогда как одна из ролей оценила assertion как relevant; подпись «по обеим ролям» рядом с таким вердиктом читалась как согласие ролей. `concur` называет принятые свидетельства, не согласие роли с вердиктом: отчёт (`host_concur` в JSON и текст HTML) подписывает решение хоста «свидетельства mapper / свидетельства redteam / свидетельства обеих ролей» и говорит, что вердикт — хоста и может расходиться с оценкой роли; исходные оценки ролей остаются рядом (REQ-SA-021). Форма DECISION §23 не меняется.

### 24.4 Синонимы `help` и `version`

`help`, `--help` и `-h` печатают успешный JSON `{"usage": "<список команд>"}` — ту же строку, что и отказ при неверных аргументах; `--version` и `-V` равны `version`. Эти вызовы не читают CONFIG и ничего не записывают. Прочие неизвестные команды и флаги отклоняются как прежде.

### 24.5 `--dir` у `skill install|update`

`--dir` — корень проекта, содержащий `.spec-audit/skill`. Путь, чьё базовое имя — `.spec-audit`, или `.spec-audit/skill`, нормализуется к корню по именам каталогов без чтения файловой системы (проект, чей корень сам называется `.spec-audit`, или каталог `skill` вне `.spec-audit` не поддерживаются); ответ `dir`/`skill` называет корень. Отказ «не установлен» называет полный путь `DIR/.spec-audit/skill` и семантику `--dir`.

## 25. Уроки пятого прогона: цитаты хоста в решении (version 3), limitations и прошлый вердикт в `outcomes[]`

Причина редакции 1.5 — пятый чистый прогон 2026-09-17 на том же snapshot, что четвёртый: по одной норме обе роли дали supported/relevant, а вердикт хоста contradicted держался на строках, которых ни одна роль не цитирует; version 2 без собственных цитат этого выразить не может, и хост вернулся к полной форме version 1 на 726 KB ради трёх цитат. Ключевой сигнал по той же норме лежал в limitations роли при supported, а итог по всем нормам совпал с прошлым прогоном только потому, что хост помнил прошлое решение. Решение владельца 2026-09-18: форма version 3 с собственными цитатами хоста внутри вердикта; таблица `outcomes[]` показывает limitations ролей и прошлый вердикт хоста. §7, §14, §23, основание CAS, журнал и состояния не меняются; журнал может содержать решения трёх форм.

### REQ-SA-046 — Решение version 3: цитаты хоста в вердикте

Условие: хост подаёт DECISION с `version: 3`.

Требование: форма и правила — как у version 2 (§23), но каждая запись `verdicts` дополнительно содержит обязательные `spec`, `code` и `tests` — собственные цитаты хоста по правилам Assessment §7: группы `spec`/`code`/`tests` по виду файла snapshot, не более 128 цитат в группе, точные строки и quote, цитата `spec` внутри блока назначенной нормы, `test_id` у тестовой цитаты; пустые массивы допустимы и означают, что хост опирается только на свидетельства ролей. Свидетельствами хоста по норме считаются цитаты названных в `concur` ролей из текущих результатов run, объединённые с собственными без повторов; полнота §7 (spec обязательна; supported/contradicted требуют code; relevant/weak/contradicts требуют тестовый источник) проверяется на объединении при записи. При записи цитаты хоста сверяются с snapshot (sha и полные строки), как у роли; при чтении журнала проверяется форма. `concur` по-прежнему `mapper`, `redteam` или `both`: собственные цитаты дополняют свидетельства ролей, вердикт без свидетельств ролей не вводится. Отчёт и `review` показывают решение в общей форме и помечают нормы, где есть собственные цитаты хоста («… и хоста»). Version 1 и 2 остаются действительными; `draft` печатает version 3 с пустыми цитатами.

Проверка: version 3 с пустыми цитатами принимается и равна version 2 по свидетельствам; собственные `code`/`tests` по одной норме дают в отчёте объединение цитат ролей и хоста без повторов и подпись «… и хоста»; цитата с неверной quote, вне snapshot, вне группы или сверх лимита — отказ с именем нормы без записи; `null` вместо массива — отказ; журнал с решениями трёх форм читается целиком; после `retry` роли решение становится outdated.

Источник: отчёт пятого прогона 2026-09-17. Обоснование: три цитаты хоста по одной норме не должны стоить полной формы; правила цитат едины для ролей и хоста. Зависимость: SA-044, SA-045, §7, §14.

### 25.1 Limitations ролей в `outcomes[]`

Запись `roles.<role>` таблицы `outcomes[]` (§24.1) дополняется полем `limitations` — массивом limitations этой роли по норме из её текущего результата (пустой массив при отсутствии). Statement и цитаты по-прежнему остаются в `entries`.

### 25.2 Прошлый вердикт хоста

Запись `outcomes[]` получает необязательное поле `previous_host` — `{run_id, review_id, specification, implementation, assertion}`: состояния из последней записи журнала другого run того же `reports_dir`, чей `manifest.json` имеет тот же `snapshot_id` и тот же accepted head (оба без accepted — совпадение); последняя запись журнала была current на момент записи по §14. При нескольких кандидатах берётся run с самой поздней записью `review` в `tool-versions.json`, при равенстве или отсутствии — лексически больший run_id. Каталоги читаются только на чтение; повреждённые или неполные пропускаются. Поле — подсказка для чтения: бинарник не переносит прошлый вердикт в новый run, не включает его в основание §14 и в черновик `draft` (RULES: stale-свидетельства не становятся текущими автоматически).

## 26. Уроки пятого прогона, мелочи: `freshness` в `index`, якоря ТЗ вне snapshot, общий `files.json`

Три наблюдения пятого прогона 2026-09-17, не меняющие контракт решения: свежесть принятого индекса хост выводил косвенно (по отсутствию stale-признаков и сравнению `snapshot_id` соседних run); исключение одной нормы было определено разделом ТЗ в файле вне scope, и роль честно поставила contradicted; `files.json` копировался в каждый из шести каталогов заданий. Решение владельца 2026-09-18: три подпункта ниже (редакция 1.6). §1–10, §20, §24 не переписываются; §24.2 получает одну уточняющую фразу.

### 26.1 `freshness` в ответе `index`

В accepted-режиме `index` успешен только на fresh-индексе (stale или отсутствующий индекс — прежний отказ «принятый индекс отсутствует или stale; нужен reconcile»); ответ называет этот инвариант полем `freshness: "fresh"`, с той же семантикой, что у `reconcile CONFIG` и `check`. В declared-режиме понятия свежести индекса нет — поле отсутствует. Прочие поля ответа `index` не меняются.

### 26.2 Подсказка о якорях ТЗ вне snapshot

Текст нормы (condition, statement, verification, exceptions, unresolved) или кандидата может ссылаться на якорь раздела ТЗ узкой формы: `A-NNN` (две–четыре цифры) или `§N.N…` (номер с необязательной буквой). Якорь считается определённым, если какая-либо строка любого spec-файла snapshot (`source_set` и `references`) начинается с него после необязательных markdown-маркеров заголовка, списка, таблицы или выделения (`## 1.6.10A …`, `* A-204: …`, `| A-164 |`); упоминание в прозе определением не считается. Нормы с неопределёнными якорями получают advisory вида «REQ-…: якоря §1.6.10A, A-204 не определены в spec-файлах snapshot (source_set+references); норма может опираться на раздел вне scope» — в `advisories` ответов `index`, `check CONFIG RAW` (по кандидатам), `check CONFIG RAW DECISION` (по следующему состоянию) и `reconcile CONFIG` без RAW (по текущим записям); ключ появляется только при непустом списке. Подсказка — не оценка и не отказ (§20); ложные срабатывания допустимы. Ограничение названо честно: якорь, не названный в тексте нормы, подсказка не находит.

### 26.3 Общий `dispatch/files.json`

`prepare` пишет список файлов snapshot один раз в `reports_dir/<run>/dispatch/files.json`; каталог каждого задания содержит только `task.json`. Роли читают общий файл рядом со своим каталогом — он не принадлежит другой роли, изоляция §21.2 не нарушается. `retry` перезаписывает только `task.json`. Исторические run с `files.json` в каталогах заданий обслуживаются как прежде.

### 26.4 Противоречие в достижимой ветке

В пятом прогоне mapper увидел механизм противоречия в ветке восстановления, но по букве протокола оставил supported и назвал его только в limitations — норма выглядела подтверждённой обеими ролями. Протокол роли (`references/protocol.txt`) уточняется: противоречие в любой достижимой ветке (обработка ошибки, восстановление, повтор, откат) — `implementation=contradicted` с цитатой этой ветки; корректный штатный путь не отменяет его, и такое наблюдение не понижается до limitation при supported. Правила цитат §7 и форма ответа не меняются.

## 27. Уроки шестого прогона: проверка решения хоста без записи, statement прошлого решения, разность цитат ролей

Причина редакции 1.7 — шестой чистый прогон 2026-09-18: хост записывал решение в append-only журнал «вслепую» (в третьем прогоне ошибка счётчика стоила второго review_id), восстанавливал «почему прошлый хост сказал weak» из памяти сессии и считал вручную, какие строки цитирует только одна роль, чтобы решить о собственных цитатах. Решение владельца 2026-09-18: проверка решения без записи тем же путём, что и запись; `previous_host` со statement; разность цитат в `outcomes[]`. §14, §23, §25 и формы решения не меняются.

### REQ-SA-047 — Проверка решения хоста без записи

Условие: хост вызывает `validate CONFIG RUN_ID host DECISION`.

Требование: команда выполняет те же проверки, что `review CONFIG RUN_ID DECISION` (версия и идентичность, форма, `counts`, собственные цитаты хоста по snapshot, полнота §7 на объединении для version 3, полнота доставки, основание, дубликат и конфликт `review_id`, лимит журнала, повторная сверка snapshot), одним и тем же путём кода, но не пишет журнал, отчёты и запись провенанса. Ответ: `{valid: true, version, review_id, duplicate, review_state, requirements, own_citations}` — при `duplicate: true` (те же байты уже в журнале) `review_state` — текущее состояние журнала, иначе `"current"` — состояние, которое даст запись этих же байт сейчас; `own_citations` — ID норм с собственными цитатами хоста. Отказы — те же тексты, что у `review`. Слово `host` — псевдо-задание только этой команды; `submit` его не принимает.

Проверка: version 3 с цитатами хоста → `valid:true`, журнал и `tool-versions.json` байт-в-байт прежние, затем `review` принимает те же байты; повтор тех же байт после записи → `duplicate:true`; другие байты с тем же `review_id` — отказ «конфликт review_id»; неверные counts, испорченная quote, stale-основание, неполная доставка — те же отказы, что у `review`; `submit … host …` — «неизвестный TASK_ID».

Источник: отчёт шестого прогона 2026-09-18. Обоснование: append-only журнал не прощает ошибку формы; проверка тем же путём кода делает `valid:true` обещанием записи. Зависимость: SA-038, SA-044, SA-046, §14.

### 27.1 Statement прошлого решения

`previous_host` (§25.2) дополняется полями `statement` и `limitations` из той же последней записи журнала соседнего run (`assessments[i]` для version 1, `verdicts[i]` для version 2 и 3); пустые — `[]`. Поле остаётся подсказкой для чтения: бинарник не переносит прошлые вердикты и тексты в новый run.

### 27.2 Разность цитат ролей

Запись `roles.<role>` таблицы `outcomes[]` дополняется `citations` — числами цитат роли по группам `spec`, `code`, `tests` — и `only_here` — ссылками `{path, line_start, line_end}` (для `tests` — с `test_id`) на цитаты, которых нет у другой роли по той же норме; при отсутствии другой роли — все цитаты роли. Сравнение — по пути и диапазону строк, quote не копируется; `entries` остаются полным источником. Разность — подсказка для решения о собственных цитатах хоста, не оценка полноты свидетельств.

## 28. Мелочи шестого прогона: списки всегда присутствуют, протокол mapper по веткам, пустой statement при совпадении

Три наблюдения шестого прогона 2026-09-18, не меняющие форм решения. Решение владельца 2026-09-18 (редакция 1.8).

### 28.1 Списки всегда присутствуют

`advisories` в ответах `index`, `check CONFIG RAW [DECISION]` и `reconcile CONFIG` без RAW и `sdk` в ответах `prepare`/`tasks` присутствуют всегда — пустой список `[]`, а не отсутствие ключа; контракт «список» держится буквально. Файлы состояния (`state.json`) не меняются.

### 28.2 Протокол mapper: противоречие в ветке

Mapper ни в одном из шести прогонов не дошёл до противоречия в ветке восстановления, а в шестом понизил найденное противоречие до limitation при supported вопреки §26.4. Протокол роли получает прямое требование к mapper: «Систематически не значит подтверждающе: если ты нашёл противоречие в любой достижимой ветке, ставь implementation=contradicted с цитатой этой ветки — как и redteam; не понижай его до limitation при supported.»

### 28.3 Пустой statement при совпадении с ролями

В трёх прогонах подряд 34 из 51 statement решения были шаблонными. Для version 2 и 3 `statement` вердикта может быть пустым только когда `concur: both` и вердикт совпадает с текущими оценками обеих ролей по specification, implementation и assertion; бинарник проверяет это при записи и подставляет в общую форму отчёта стандартный текст «Совпадает с оценками обеих ролей.». При любом расхождении с оценкой роли или при `concur` ≠ both пустой statement — отказ без записи. При чтении журнала проверяется форма, как у остальных state-зависимых правил v2/v3.

## 29. Уроки седьмого прогона: чтение решения по одной норме, симметричный ответ `review`

Седьмой чистый прогон 2026-09-18 воспроизвёл итог шестого по всем нормам; хост по-прежнему читал statement, limitations и цитаты обеих ролей по норме jq-скриптом из ответа `review` размером 2 MB, применил правило «contradicts важнее relevant», которого в контракте не было, и получил от `review … DECISION` ответ беднее, чем от `validate … host`. Решение владельца 2026-09-18 (редакция 1.9): правило — в §14; два подпункта ниже.

### 29.1 Чтение по одной норме

`review CONFIG RUN_ID REQ-ID` — четвёртый аргумент вида `REQ-<буквы/цифры>-<цифры>` читается как ID нормы, а не путь DECISION (путь такого вида не бывает); команда read-only, провенанс не пишется. Ответ: `run_id`, `snapshot_id`, `freshness`, `requirement` (норма целиком), `outcome` (запись `outcomes[]` — состояния, limitations, `citations`, `only_here`, `agree`, `previous_host`), `roles` — по каждой роли с текущим результатом `task_id, attempt, statement, limitations, spec, code, tests` (полные цитаты), `host` — вердикт последнего решения по норме с `review_id`, состоянием журнала, формой, состояниями, statement, признаком собственных цитат и объединёнными цитатами (отсутствует, если решения нет), `executions` — фактические запуски тестов, упомянутых цитатами ролей и хоста. Неизвестный ID — отказ «неизвестный requirement_id». `entries` полного контекста не меняются.

### 29.2 Симметричный ответ записи решения

Ответ `review CONFIG RUN_ID DECISION` дополняется полями `version`, `requirements` и `own_citations` — теми же, что у `validate … host` (§27), в том числе при `duplicate: true`.

## 30. Мелочи седьмого прогона: промпт роли от `prepare`, индекс цитат по файлу, счётчики доставки

В семи прогонах хост собирал `prompt.md` роли копированием промпта прежнего run и правкой фраз протокола вручную — вопрос «протокол или промпт» стал неизмеримым; перед собственными цитатами (§25) хост искал jq по `entries`, кто и под какой нормой уже цитирует строку; `tasks` после полной доставки отвечал пустым списком без счётчиков. Решение владельца 2026-09-18 (редакция 1.10): три подпункта ниже; формы решения, §14/§23/§25 и зеркало `host-decision.md` не меняются.

### 30.1 Промпт роли и протокол из бинарника

Требование: `prepare` пишет один раз `dispatch/protocol.txt` — байты протокола роли, встроенного в бинарник (те же, что устанавливает `skill install` в `references/protocol.txt`), и в каталог каждого задания `dispatch/<task_id>/prompt.md` — промпт роли, собранный из задания: роль и её задача (mapper — систематическая оценка каждой нормы по всем достижимым веткам, redteam — независимое опровержение с оценкой каждого ID), SOURCE_ROOT (`project_root`), абсолютные пути TASK (`task.json`), FILES (`dispatch/files.json`), PROTOCOL (`dispatch/protocol.txt`), OUTPUT_PATH (`dispatch/<task_id>/result.json`) и рабочего каталога, команда VALIDATE с абсолютными путями бинарника и CONFIG, правила контекста и порядок работы, раздел SDK_HINTS: подсказки берутся из файла `sdk_hints.json` в каталоге задания, если хост его положил; иначе подсказки не передаются и это не доказывает отсутствие кода или теста. `retry` перезаписывает `task.json` и `prompt.md` новой попыткой; остальные файлы каталога (`result.json`, вспомогательные файлы роли, `sdk_hints.json`) не трогаются. Хост промпт не правит — иная постановка задачи пишется отдельным файлом рядом. Промпт не содержит путей `manifest.json`/`state.json` и имён проекта, кроме тех, что входят в `project_root` и пути CONFIG.

Проверка: после `prepare` в каждом каталоге задания лежит `prompt.md` с ролью, абсолютными путями task.json/files.json/protocol.txt/result.json, командой `validate CONFIG RUN_ID TASK_ID …` и упоминанием `sdk_hints.json`; `dispatch/protocol.txt` побайтно равен `references/protocol.txt` установленного skill той же версии; `retry` заменяет `prompt.md` и `task.json`, чужой файл каталога остаётся прежним.

Источник: отчёты `sa-clean` run 18-01/18-02 (промпт из прежнего run скриптом, правка фраз протокола вручную). Обоснование: единственный источник правил роли — протокол бинарника той же версии, что и `validate`. Зависимость: §24.2, §26.3, §19.2.

### 30.2 Индекс цитат run по файлу

Требование: `review CONFIG RUN_ID citations [PATH]` — read-only индекс цитат текущего состояния run: по каждой цитате обеих ролей (текущие результаты) и последнего решения хоста (объединённые цитаты вердикта) строка `{path, line_start, line_end, kind: spec|code|tests, test_id, role: mapper|redteam|host, task_id, requirement_id}` без quote (`test_id` и `task_id` пустые там, где не применимы); список отсортирован по path, line_start, line_end, role, requirement_id. PATH — точный относительный путь из snapshot; с ним список сужается до этого файла, неизвестный путь даёт `[]`. Команда отвечает и до полной доставки — по тому, что уже принято. Провенанс не пишется; `entries` и `outcomes[]` не меняются.

Проверка: без PATH — все цитаты обеих ролей и хоста в заданном порядке; с PATH — только этот файл, роли и нормы совпадают с `entries`; неизвестный путь → `[]`; `tool-versions.json` прежний.

Источник: отчёт `sa-clean` run 18-02 («этот path:line цитируется ролью X под нормой Y» — для решения о собственных цитатах под соседней нормой). Зависимость: §25, §27.2, §29.1.

### 30.3 Счётчики доставки в `prepare`/`tasks`

Требование: TaskBatch в ответах `prepare` и `tasks` дополняется полями `expected` (число заданий), `submitted` (принятые) и `delivery_complete` — теми же, что у `status`; `tasks` после полной доставки отвечает `tasks: []` с `submitted == expected` и `delivery_complete: true`.

Проверка: `prepare` → `submitted: 0`, `delivery_complete: false`; после всех `submit` `tasks` → `tasks: []`, `delivery_complete: true`; после `retry` — `submitted` уменьшается на один.

Источник: отчёт `sa-clean` run 18-02. Зависимость: §1–10 (поля добавляются, не меняются), §24.2.

## 31. Точная цитата `cite`, журнал `validate` роли, посторонний симлинк

Роли собирали цитаты `sed`+`jq` — главная причина промежуточных отказов `validate`, которых хост не видит; хост в четвёртом прогоне продолжил работу при постороннем симлинке внутри `.spec-audit/`, трактуя «symlink escape» узко. Решение (по поручению владельца 2026-09-18, редакция 1.11): три подпункта; REQ-SA-039 (`validate` ничего не пишет) не меняется.

### 31.1 `cite CONFIG PATH A B`

Требование: команда read-only. PATH — относительный путь под `project_root` CONFIG (выход за корень, симлинк на пути, каталог, не-UTF-8 — отказ), A и B — 1-based номера строк, A ≤ B ≤ число строк. Ответ — Citation `{path, line_start, line_end, quote}`, где quote построена по правилам §7 (`lineQuote`: точные полные строки без завершающего перевода строки, CRLF сохраняется внутри, завершающий CR снимается). Принадлежность файла snapshot и категории FILES не проверяется — это делает `validate`/`submit`; `reports_dir` не требуется и не создаётся, провенанс не пишется.

Проверка: цитата из `cite` проходит `validate` в ответе роли без правки; A > B, B за концом файла, каталог, `../` и симлинк — отказы с текстом причины; каталог отчётов после команды не появляется.

Источник: ROADMAP «Уроки четвёртого прогона», пункт 8. Зависимость: §7, REQ-SA-006.

### 31.2 CITE и журнал `validate` в промпте роли

Требование: `prompt.md` (§30.1) называет команду CITE с абсолютными путями бинарника и CONFIG и просит роль дописывать вывод каждого вызова VALIDATE (stdout и stderr, через `tee -a`, чтобы роль видела ответ и в терминале) в `validate.log` рабочего каталога задания — журнал ведёт роль, бинарник по REQ-SA-039 ничего не пишет; хост читает `validate.log`, когда роль сдаёт ответ с опозданием или с отказами.

Проверка: промпт содержит ` cite ` с путём CONFIG и `validate.log`; `TestPrepareDispatch`.

Источник: ROADMAP «Уроки шестого прогона», пункт 6. Зависимость: §30.1, REQ-SA-039.

### 31.3 Посторонний симлинк внутри workspace

Требование (launcher, SKILL.md): остановка нужна, когда CONFIG, `reports_dir` или путь к ним проходят через симлинк наружу либо когда `reports_dir` пересекается с источниками; посторонний симлинк внутри `.spec-audit/`, не участвующий в CONFIG, `reports_dir` и установленном skill, не читается, не удаляется и не считается остановкой — хост называет его в описании границы перед extraction. Бинарник по-прежнему отказывает на симлинке внутри run и в источниках (`noSymlinks`).

Проверка: `TestSkillFiles` — SKILL.md содержит правило; поведение бинарника не меняется.

Источник: ROADMAP «Уроки четвёртого прогона», пункт 11. Зависимость: SKILL.md граница workspace, REQ-SA-012.

## 32. Сводки: `index CONFIG summary`, `review CONFIG RUN_ID summary`

Ответы `index` (тела норм и список файлов, 264 KB на пилоте) и `review` (`entries` с цитатами обеих ролей, 0,9–2 MB) хост читал только через jq; после §24–§30 первое, что нужно шагу 6, — `outcomes[]`, `roles[]` и состояние решения. Решение (пункт 7 четвёртого прогона, очередь ведётся автономно по поручению владельца 2026-09-18, редакция 1.12): две read-only сводки по прецеденту ключевого слова (§27, §30.2). Полные ответы не меняются.

### 32.1 `index CONFIG summary`

Требование: тот же расчёт, что у `index` (snapshot, отказы на stale-индексе и устаревших `scopes` — те же), но вместо `requirements` и `files` ответ несёт `requirements_total`, `active_ids` (ID в порядке индекса) и `files_total`, а также `scopes` из CONFIG; `snapshot_id`, `profile`, `completeness_basis`, `semantic_completeness_proven`, `accepted`, `project_root`, `freshness` (accepted-режим) и `advisories` — как в `index`. Иное третье слово — отказ.

Проверка: сводка без ключей `requirements`/`files`, с теми же `snapshot_id`/`advisories`, `requirements_total` и `active_ids` равны полному ответу; в accepted-режиме `freshness: fresh`; `index CONFIG other` — отказ.

### 32.2 `review CONFIG RUN_ID summary`

Требование: контекст `review` без `entries`, `executions`, тел норм и содержимого решения: `run_id`, `snapshot_id`, `delivery_complete`, `freshness`, `state`, `basis_sha256`, `history`, `form`, `requirements_total`, `roles[]`, `outcomes[]` (как в §24.1, §25, §27.2), `agree` — число согласных норм, `disagree` — ID норм с `agree: false`. Команда read-only, отвечает и до доставки, провенанс не пишется.

Проверка: `outcomes[]` и `basis_sha256` равны полному ответу; JSON сводки не содержит `entries` и `quote`; `tool-versions.json` прежний; после решения `state`/`history`/`form` — как в `review`.

Источник: ROADMAP «Уроки четвёртого прогона», пункт 7. Зависимость: §24.1, §27.2, §29.1, §30.2.

## 33. Уроки восьмого прогона: индекс якорей, фильтры цитат, смена версии в run, текст нормы

Восьмой чистый прогон 2026-09-18 — первая приёмка нового раздела с нуля (44 нормы, четыре справочных файла, 43 минуты, отказов бинарника ноль). Хост писал extractor'у руками, какие строки 220-KB reference читать по якорям; `citations` без пути отдал 861 строку без фильтра по норме/роли; версия бинарника сменилась между `prepare` и `submit` без предупреждения; чтение нормы по-прежнему требовало jq-форматтера. Решение (очередь ведётся автономно по поручению владельца 2026-09-18, редакция 1.13): четыре подпункта; формы решения не меняются.

### 33.1 `anchors CONFIG`

Требование: команда read-only и не требует принятого индекса (служит приёмке): по spec-файлам CONFIG (`specs` — нормативные, `references` — справочные) строит индекс якорей узкой формы §26.2: для каждого якоря, упомянутого в нормативных файлах, — `mentions` (path, line_start=line_end) и `definitions` — строки любого spec-файла, где якорь определён по правилу §26.2; определение-заголовок тянется до следующего заголовка, определение-пункт списка/таблицы — до пустой строки или следующего определения, без завершающих пустых строк. Ответ: `spec_files`, `anchors[]` (по алфавиту), `undefined[]` — якоря без определения. Якорь, определённый в reference, но не упомянутый в нормативных файлах, не индексируется. Каталог отчётов не создаётся.

Проверка: упоминание в нормативном файле и определение-пункт в reference до пустой строки; заголовок — до следующего заголовка; неупомянутый якорь отсутствует; якорь без определения — в `undefined`; `runs` не создаётся.

Источник: отчёт `sa-clean` run rent-01 (extractor читал 220-KB reference выборочно; хост перечислял якоря руками). Зависимость: §20, §26.2.

### 33.2 Фильтры индекса цитат

Требование: единственный аргумент `review CONFIG RUN_ID citations X` трактуется как ID нормы (по шаблону `REQ-…`), роль (`mapper|redteam|host`) или иначе — путь файла (§30.2). Форма строк и порядок не меняются.

Проверка: фильтр по норме оставляет только её строки; по роли — только эту роль; оба сужают полный индекс.

### 33.3 Смена версии бинарника внутри run

Требование: публикующая команда (`submit`, `retry`, `review … DECISION`, `test`, `php-typed`) на run, чей первый record `tool-versions.json` назван другой версией, пишет WARN «run подготовлен другой версией бинарника; журнал версий запишет обе» с полями `prepared`/`current`; ответ и журнал не меняются (REQ-SA-040), read-only команды и `tasks`/`validate`/`draft` молчат.

Проверка: журнал с `prepare` от `0.0.1` — `tasks` без WARN, `submit` с WARN и второй записью текущей версии.

### 33.4 Текстовая форма нормы

Требование: `review CONFIG RUN_ID REQ-ID text` — тот же расчёт, что §29.1, но ответ `{requirement_id, text}`: одна читаемая сводка — заголовок нормы, условие/требование/проверка, принятые revision/clarity/исключения/unresolved, `agree` и прошлый вердикт хоста, затем по каждой роли состояния, задание/попытка, statement, ограничения и цитаты как `kind path:A-B` (тест — с test_id), затем вердикт хоста и фактические запуски. Контракт stdout (§1–10, один JSON) сохраняется — текст лежит в поле. Иное пятое слово — отказ.

Проверка: текст содержит ID и заголовок, блоки `[mapper]`, `[redteam]`, `[host]` с review_id, statement роли, `code path:A-B`, `test <id>`; `… REQ-ID html` — отказ.

Источник: отчёт `sa-clean` run rent-01 (show.sh поверх `review REQ-ID`). Зависимость: §29.1.

## 34. Обзор нескольких scope: `overview CONFIG...`

На пилоте появился второй раздел со своим scope-конфигом, ledger и run'ами; ROADMAP-этап «несколько разделов и обзор корпуса» начинается с карты того, что уже принято и решено по каждому scope. Решение (очередь ведётся автономно по поручению владельца 2026-09-18, редакция 1.14): одна read-only команда; инвентарь полноты корпуса, противоречия норм и неописанное поведение — не в этом разделе.

Требование: `overview CONFIG...` принимает один и более CONFIG и отвечает `{scopes[], totals}`. По каждому scope: `config` (как передан), `index_mode` (`declared|accepted`), `freshness` (`fresh` при успешном snapshot; в accepted-режиме при отказе snapshot — `stale`/`uninitialized` по ledger и текущим файлам, иначе `unavailable`; в declared-режиме `n/a`), `error` — текст отказа snapshot, если был, `requirements_total` (нормы snapshot или active-записи ledger), `accepted_head`, `runs` — число каталогов run с manifest, `latest` — последний подготовленный run (по `recorded_at` записи `prepare` в `tool-versions.json`, затем по имени): `run_id`, `prepared_at`, `tool_version`, `snapshot_current`, `delivery_complete`, `expected`, `submitted`, `host_review_state`, `review_id`, `form`, `implementation`/`assertion` — счётчики состояний последнего решения хоста, `contradicted` — ID норм с вердиктом contradicted (список GAP scope), `disagree` — ID расхождений ролей (только при полной доставке); `decided` — последний run с решением хоста в той же форме. `totals`: число scope, сумма `requirements_total`, сумма `runs`, суммы `implementation`/`assertion` по `decided` каждого scope. Отказ одного CONFIG (форма) — отказ команды; stale/повреждённый run — пропуск с DEBUG, не отказ. Ничего не пишется. Ответ не утверждает полноту корпуса: каждый scope называет только свой принятый набор.

Проверка: scope без run — `runs: 0`, `latest: null`; после `prepare` — `latest` без доставки, `decided: null`, `disagree: []`; после решения — `decided` с состоянием, формой, `disagree`; два CONFIG — `totals` по обоим, `accepted_head` у accepted-scope; stale accepted-scope — `freshness: stale` с `error`, карта отвечает; `tool-versions.json` прежний; `overview` без CONFIG — отказ.

Источник: ROADMAP «После ближайших этапов — несколько разделов и обзор корпуса»; пилот с двумя scope (02-finance, 11-allocation-rent). Зависимость: §12, §19 (журнал версий), §23–§25 (решения), accepted-index.md.

## 35. Уроки девятого прогона: уточнения промпта роли, краткая форма нормы

Девятый чистый прогон 2026-09-18 — повтор `11-allocation-rent` на том же snapshot с промптами от `prepare` (§30.1): все четыре роли сдали с первого `validate`, брали цитаты через `cite`, вердикт хоста по implementation совпал с run 01 на всех 44 нормах. Сравнение с хостовыми промптами дало семь уточнений текста и одну просьбу к чтению норм. Решение (очередь ведётся автономно по поручению владельца 2026-09-18, редакция 1.15).

### 35.1 Уточнения промпта роли

Требование: `prompt.md` (§30.1) дополнительно (а) называет форму `test_id` — PHPUnit: полное имя класса с namespace и метод, Go: `import/path::TestName`; (б) перечисляет запрещённые файлы по имени (AGENTS.md, CLAUDE.md, .ai-factory/**, .claude/**, .agents/**, спецификации вне FILES); (в) запрещает общий scratchpad сессии; (г) в строке redteam перечисляет ветки (штатный путь, обработка ошибки, восстановление/incident, повтор, откат) и «зелёный тест, закрепляющий противоречащее норме поведение»; (д) в п.3 перечисляет допустимые состояния и правило «принятую ambiguous-норму нельзя объявлять clear»; (е) в п.5 просит сводку счётчиками по состояниям; (ж) рабочий каталог назван с завершающим `/` и подкаталоги в нём разрешены. Формат `test_id`, который принимает `validate`, не сужается.

Проверка: `TestPrepareDispatch` — промпт содержит перечисленные фрагменты; redteam — перечень веток.

Источник: отчёт `sa-clean` run rent-02 (diff промптов; redteam с коротким текстом не поднял 039 до contradicted, n=1). Зависимость: §30.1, §28.2.

### 35.2 `review CONFIG RUN_ID REQ-ID brief`

Требование: как `text` (§33.4), но вместо списков цитат каждой роли и хоста — строка «цитаты: spec N, code N, tests N»; суждения, ограничения, прошлый вердикт и запуски остаются. Пятое слово, кроме `text`/`brief`, — отказ.

Проверка: `brief` содержит statement роли и блок `[host]`, не содержит `code path:A-B`, содержит «цитаты: spec».

Источник: отчёт `sa-clean` run rent-02 (при чтении 13 норм подряд хост резал списки цитат awk'ом). Зависимость: §33.4, §30.2 (`citations REQ-ID` — списки отдельно).

## 36. Уроки синтетического прогона 0.1.22: состояния хоста в отчёте, unknown при выносе за срез

Чистый прогон синтетического Go-проекта на 0.1.22 (две роли по промптам `prepare`, цитаты через `cite`, `validate` с первого раза, решение version 3 принято с первой подачи) повторил спор ролей из первого синтетического run: redteam ставит `missing` норме, чьи тесты ТЗ явно выносит за срез, хотя протокол требует не считать отсутствие в выданном контексте доказательством; хост искал вердикт по норме в `report.json` и не нашёл его в `requirements[]`. Решение (очередь ведётся автономно по поручению владельца 2026-09-18, редакция 1.16).

### 36.1 Состояния вердикта хоста в навигации отчёта

Требование: `requirements[].navigation.host_states` в `report.json` — строка `specification/implementation/assertion` вердикта хоста по норме (только когда решение есть); полный вердикт остаётся в `host_review.latest.assessments[]`, HTML не меняется.

Проверка: после решения `host_states` равна состояниям `host_review.latest.assessments[i]`; без решения поля нет.

### 36.2 Протокол: unknown при явном выносе за срез

Требование: `references/protocol.txt` после фразы об отсутствии свидетельства в выданном контексте: «Если ТЗ или задание явно выносит реализацию или тесты нормы за пределы среза, ставь unknown с этой причиной, а не missing.» Форма ответа роли не меняется.

Проверка: `TestSkillFiles`; текст в `references/protocol.txt`.

### 36.3 Уточнения SKILL.md и `overview`

Требование: SKILL.md говорит «statement may be left empty …» (написанный statement при совпадении допустим всегда), называет stderr у `draft` и место вердикта хоста в `report.json`; `overview` (§34) несёт `contradicted[]` — ID норм с вердиктом contradicted по решённому run.

Проверка: `TestSkillFiles`, `TestOverview`.

Источник: отчёт субагента-хоста run `run-20260918-synth22` (синтетика вне Git). Зависимость: §15 (отчёт), §19.2 (протокол), §34.

## 37. Уроки десятого прогона: вид определения и вложенные ссылки в `anchors`, сводка без журнала, `brief` без запусков

Десятый чистый прогон 2026-09-18 — третий раздел пилота с нуля (`05-inventory`, 28 норм, 30 минут, отказов ноль). `anchors` сработал: extractor получил только строки определений и перестал читать 220-KB reference «выборочно» (−25 % токенов на кандидата), но хост отбрасывал руками строки таблицы покрытия («| A-032 | P2/1.7.4 |» — тоже определение по §26.2) и сам собирал якоря, на которые ссылаются сами определения; `index … summary` печатал журнал приёмки целиком; `brief` заканчивался списком `not_recorded` на каждый тест. Решение (очередь ведётся автономно по поручению владельца 2026-09-18, редакция 1.17).

### 37.1 Вид определения и вложенные ссылки

Требование: элемент `definitions[]` ответа `anchors` (§33.1) — `{path, line_start, line_end, kind, references}`: `kind` — `heading` (строка начинается с `#`), `list` (`*`/`-`), `table` (`|`), иначе `text`; `references` — якоря узкой формы, названные внутри диапазона определения, кроме самого якоря, в порядке появления без повторов. Правило §26.2 не меняется: строка таблицы остаётся определением, хост отличает её по `kind`.

Проверка: пункт списка со ссылками → `kind: list`, `references` в порядке появления; строка таблицы → `kind: table`, `references: []`; заголовок → `heading`.

### 37.2 `index CONFIG summary` без журнала приёмки

Требование: в сводке (§32.1) поле `accepted` — `{head, history_total}` вместо полного `AcceptedSummary` с журналом пакетов; полный `index` не меняется.

Проверка: `accepted.head` равен полному ответу, `history_total` — числу пакетов.

### 37.3 `brief` без запусков

Требование: `review … REQ-ID brief` (§35.2) не печатает строки «запуск …»; `text` печатает их по-прежнему.

Проверка: `brief` без «запуск », `text` с ним.

Источник: отчёт `sa-clean` run inventory-01. Зависимость: §26.2, §32.1, §33.1, §35.2.

## 38. Уроки одиннадцатого прогона: остаток пакета в ответах, определения вне snapshot, `oversize_reason`

Одиннадцатый чистый прогон 2026-09-18 — четвёртый раздел пилота (`07-allocation-activation`, 62 нормы + 1 defer, 384 строки ТЗ, 6 ролей, отказов ноль) и первый заход к лимиту пакета: extractor уложился в 63 кандидата, хост отложил один по границе scope, но ни `check`, ни `reconcile` не назвали отложенное в ответе; `references` определений уводили в §-разделы других файлов корпуса, которых нет в snapshot (§2.1 оказался нужен для двух contradicts); advisory «>24» повторялся на неделимом подразделе. Решение (очередь ведётся автономно по поручению владельца 2026-09-18, редакция 1.18); лимит 64 кандидатов на raw и контракт приёмки не меняются (multi-raw пакет — решение владельца).

### 38.1 Остаток пакета в ответах

Требование: `check CONFIG RAW DECISION` и `reconcile CONFIG RAW DECISION` при первом применении отвечают полями `deferred` и `rejected` — ID кандидатов операций `defer`/`reject` в порядке DECISION (`[]` при отсутствии); `index CONFIG summary` в `accepted` добавляет `last_deferred`/`last_rejected` по последнему пакету журнала. Журнал и форма DECISION не меняются.

Проверка: DECISION с `defer C003` → `check` и `reconcile` называют `deferred: [C003]`, `rejected: []`; `index … summary` — `last_deferred: [C003]`.

### 38.2 Определения вне snapshot и вложенные якоря

Требование: `anchors CONFIG [PATH...]` — дополнительные относительные пути под `project_root` (файл или каталог; `*.md` каталога обходятся без символических ссылок; выход за корень или отсутствующий путь — отказ) дают определения для якорей вне spec-файлов snapshot: такие определения помечены `outside: true`, файлы перечислены в `outside_files`. Якорь, названный только в `references` какого-либо определения (не в нормативных файлах), получает собственную запись с `nested: true`, пустыми `mentions` и своими определениями — разрешение ровно на один уровень. Порядок и прочие поля §33.1/§37.1 не меняются; `undefined` учитывает и вложенные якоря.

Проверка: заголовок в дополнительном каталоге снимает якорь из `undefined` и помечен `outside`; якорь из `references` появляется как `nested` с определением; `../` и отсутствующий путь — отказы.

### 38.3 `oversize_reason` у scope

Требование: необязательное поле `oversize_reason` в элементе `scopes` CONFIG — непустая причина, почему scope выше рекомендации §20 остаётся целым (неделимый подраздел); для такого scope advisory «слишком большой scope» и WARN не выдаются, остальные scope проверяются как прежде; предел 64 и форма TaskBatch не меняются, `scopes` по-прежнему обязаны покрыть все active ID без повторов.

Проверка: scope из 25 норм с причиной — `advisories: []`; без причины — одна рекомендация.

Источник: отчёт `sa-clean` run activation-01. Зависимость: §20, §21, §22.3, §33.1, §37.1, accepted-index.md.

## 39. Второй пакет на том же scope: операция `keep`, диапазоны якорей

Двенадцатый чистый прогон 2026-09-18 (`01-foundation`, 620 строк) показал предел контракта приёмки: раздел даёт больше 64 обязанностей, а второй пакет на том же scope невозможен — каждый пакет обязан учесть все прежние active ID, и для неизменённой нормы нужен `rebind` с кандидатом в этом же raw, которого второй raw содержать не может (58 + 44 > 64). Контракт [принятого индекса](accepted-index.md) не переписывается; по прецеденту §22 (`reanchor`, REQ-SA-043) вводится отдельная операция с собственным контролем. Решение (очередь ведётся автономно по поручению владельца 2026-09-18, редакция 1.19; multi-raw пакет и извлечение по диапазону строк как контракт — по-прежнему решение владельца).

### REQ-SA-048 — Продолжение нормы без кандидата (`keep`)

Условие: DECISION содержит операцию `keep` с одним `previous` (active ID) и без `targets`.

Требование: норма продолжается без изменений — ID, revision, content hash, цитаты, `source`, clarity, unresolved, parents и статус `active` сохраняются байт-в-байт; меняется только `reason` записи. Операция допустима, только если каждый файл, который норма цитирует (`accepted.citations` и `source`), присутствует в `source_set` raw с тем же sha256, что в `source_set` пакета, принявшего или последним изменившего индекс; иначе отказ «keep REQ-…: источник <path> изменился или отсутствует в raw; норму нужно перепривязать кандидатом (rebind/revise/reanchor)». `keep` с `targets`, повтор ID или неизвестный ID — отказы прежними текстами. Полный учёт прежних ID (accepted-index.md п.1) сохраняется: `keep` — явное решение хоста по каждой норме, не сохранение по сходству строк; `matches` подсказки не влияют. `check CONFIG RAW DECISION` и `reconcile CONFIG RAW DECISION` отвечают `kept: [ID…]`; `assignments` не включают `keep`; нумерация новых ID продолжается. Второй raw по-прежнему перечисляет весь `source_set` и содержит только кандидатов своего диапазона; ответственность за то, что диапазоны пакетов не пересекаются по смыслу, остаётся на хосте (кандидат, дублирующий принятую норму, — `reject`/`rebind`, не второй accept).

Проверка: пакет 2 с `keep REQ-AI-001` + `accept C001` (другая строка) — `check` называет `kept`, `assignments` только новый ID REQ-AI-002; после `reconcile` две active-записи, первая байт-в-байт прежняя; `index … summary` — 2 нормы, `history_total: 2`; `keep` с target — отказ; `keep` после изменения цитируемого файла — отказ с именем нормы и файла.

Источник: отчёт `sa-clean` run foundation (пакет 2 отклонён «нужен полный учёт кандидатов и прежних active ID»). Обоснование: лимит 64 ограничивает raw, а не размер индекса; без `keep` scope не может вырасти. Зависимость: accepted-index.md п.1–4, REQ-SA-042, REQ-SA-043, §26.1 (свежесть).

### 39.2 Диапазоны якорей в `anchors`

Требование: упоминание вида `A-051–A-055` (тире любого вида, необязательные пробелы, ширина номера как у границ, не более 64 шагов) считается упоминанием всех промежуточных якорей; границы находятся обычным правилом. Только для `anchors`; advisory §26.2 не меняется.

Проверка: «A-900–A-902» даёт запись A-901 с определением; A-900/A-902 — как обычные якоря.

## 40. Уроки двенадцатого прогона: `previous_host` по содержанию нормы, `pending_ids`, VALIDATE как есть

Двенадцатый чистый прогон 2026-09-18 — двухпакетная приёмка `01-foundation` (58 + 44 норм через `keep`, §39) и два run (58 и 102 нормы). После второго пакета accepted head изменился, и `previous_host` (§25) исчез у 58 неизменённых норм — хост переносил вердикты run 01 вручную; при 12 заданиях хост вёл очередь волнами по `expected/submitted`; роль прогнала первый `validate` без `tee`, и отказ не попал в журнал; `matches` во втором пакете шумели строками-заголовками. Решение (очередь ведётся автономно по поручению владельца 2026-09-18, редакция 1.20).

### 40.1 `previous_host` по содержанию нормы

Требование (редакция 1.33): соседний run даёт `previous_host` (§25, §27.1) по каждой норме отдельно при равных ID, `content_hash` и `revision` — норма, продолженная `keep`, получает прошлый вердикт, новая норма пакета — нет; поле `same_files` считается по норме: `false`, если хотя бы один файл, процитированный объединённым вердиктом хоста по ней (spec, code, tests), изменил sha256 — тогда вердикт — история, а не текущее чтение (в `text`/`brief` — пометка «источники с тех пор изменились»); редакция 1.32 сравнивала весь набор файлов, и после любого коммита признак был `false` у всех норм. Первая редакция (1.20) снимала подсказку при любом изменении файлов, что обнуляло её ровно в сценарии перепрогона после фикса (run rent-2026-09-19-01: null у всех 44 норм). Правило выбора run (последний по `recorded_at` записи `review`, затем по имени) не меняется; поле остаётся подсказкой, вердикт не переносится.

Проверка: после `keep` + `accept` в run на новом head прежняя норма несёт `previous_host` из прежнего run, новая — нет; после изменения `source.go` — `previous_host` остаётся с `same_files: false`, при тех же файлах — `true`.

### 40.2 `pending_ids` в `prepare`/`tasks`

Требование: TaskBatch (§30.3) дополняется `pending_ids` — ID незавершённых заданий в порядке `tasks[]`; `[]` после полной доставки.

Проверка: `prepare` → все ID; после всех `submit` → `[]`.

### 40.3 VALIDATE только заданной командой

Требование: п.5 промпта роли (§30.1, §31.2) требует запускать VALIDATE ровно той командой, что дана (иначе вызов не попадает в `validate.log`).

### 40.4 Шум `matches` во втором пакете

Наблюдение без изменения поведения: в позднем пакете того же scope (§39) кандидаты непересекающихся подразделов получают подсказки `matches` с `overlap` 0,1–0,2 и `unique_shared: 0` по строкам-заголовкам шаблонных файлов; калибровка §22.1 сохраняется (порог 0,1 и `unique_shared` как маркер), фильтр не вводится — SKILL.md называет такие подсказки шумом.

Источник: отчёт `sa-clean` run foundation-01/02. Зависимость: §25, §27.1, §30.3, §31.2, §39.

## 41. Уроки тринадцатого прогона: имена полей в отказе, `ambiguous_over_clear`, `likely_duplicate`

Тринадцатый чистый прогон 2026-09-18 — `06-provider-integrations` в два пакета через `keep` без блокера (45 + 48 accept + 8 defer, 93 нормы, 12 ролей, 82/10/1). Отказ формы `defer` с `targets: [{candidate}]` не назвал недостающих полей; redteam поставил `ambiguous` принятой clear-норме — `validate` принял (протокол запрещает только обратное), но хост заметил расхождение лишь в своём билдере; `matches` при пакете-своде дали содержательные совпадения (overlap 73 %, `unique_shared: 0`), которые хост отложил как дубликаты. Решение (очередь ведётся автономно по поручению владельца 2026-09-18, редакция 1.21).

### 41.1 Имена полей в отказе формы

Требование: отказ «неполный или неизвестный набор полей результата» дополняется списками `нет [..]` и `лишние [..]` по JSON-ключам структуры; прочие тексты отказов (REQ-SA-015: содержимое цитат и ответа в текст не попадает) не меняются.

Проверка: объект с одним известным и одним чужим ключом → «нет [title verification], лишние [extra]».

### 41.2 `ambiguous_over_clear` в `outcomes[]`

Требование: роль вправе оценить принятую clear-норму как `ambiguous` (протокол §19.2 запрещает только объявлять принятую ambiguous-норму clear); `outcomes[]` (§24.1) по каждой норме несёт `ambiguous_over_clear` — список ролей, поставивших `ambiguous` норме с `accepted.clarity: clear` (`[]` иначе; в declared-режиме всегда `[]`); `agree` уже учитывает расхождение. Вердикт хоста по specification остаётся его явным решением; форма решения не меняется.

Проверка: после `retry` redteam с `ambiguous` — `ambiguous_over_clear: ["redteam"]`, `agree: false`; у других норм `[]`.

### 41.3 `likely_duplicate` в `matches`

Требование: подсказка `matches` (§21.1, §22.1) получает `likely_duplicate: true`, когда `overlap ≥ 0.5` — кандидат почти дословно повторяет принятую норму (свод, повтор подраздела); обычный исход — `defer`/`merge`/`reject`/`reanchor`, не второй `accept`. Порог и лимит подсказок не меняются; подсказка не решение.

Проверка: та же цитата → `likely_duplicate: true`; четверть общих строк → `false`.

Источник: отчёт `sa-clean` run provider-integrations-01. Зависимость: §21.1, §22.1, §24.1, REQ-SA-015. Наблюдения без изменения поведения: `overview` уже несёт `decided.implementation`/`assertion` по каждому scope (§34); объём references остаётся решением хоста (§20).

## 42. Уроки четырнадцатого прогона: дубликат по словам, кросс-scope память, диапазоны в отказе

Четырнадцатый чистый прогон 2026-09-18 — `09-messaging-provider-callbacks` (77 норм) и `10-inbound-client-protocols` (69 норм), по два пакета через `keep`. Свод §1.13.3 повторял принятые нормы своими строками — `likely_duplicate` по общим строкам не сработал ни разу (13 дубликатов, 0 подсказок); один и тот же дефект рынков хост решал восьмой раз вручную, потому что `previous_host` живёт внутри ledger; роли четыре раза получали отказ «цитата вне блока назначенного требования» и чинили его наугад. Решение (очередь ведётся автономно по поручению владельца 2026-09-18, редакция 1.22).

### 42.1 Дубликат по словам

Требование: подсказка `matches` (§21.1, §41.3) несёт `text_similarity` — Jaccard по множествам слов (буквы/цифры, длиннее двух символов, без учёта регистра) условия и утверждения кандидата и принятой нормы; `likely_duplicate` ставится и при `text_similarity ≥ 0,5`. Активные нормы без общих строк, но с `text_similarity ≥ 0,5`, добавляются в `matches` после строковых подсказок (`overlap`/`shared_lines`/`unique_shared` = 0) по убыванию сходства; общий лимит подсказок (`matchHintLimit`) сохраняется. Калибровка строкового порога §22.1 не меняется; подсказка не решение.

Проверка: кандидат другой строки с тем же утверждением → одна подсказка с `shared_lines: 0`, `likely_duplicate: true`; лимит четыре на кандидата с учётом словесных.

### 42.2 Кросс-scope память: `contradicted_code`

Требование: `overview CONFIG...` (§34) отвечает `contradicted_code[]` — по каждому scope из его `decided` run все code-цитаты вердиктов хоста с `implementation: contradicted` (объединённые свидетельства ролей и хоста): `{path, line_start, line_end, config, run_id, requirement_id}`, отсортировано по path, line_start, config, requirement_id. Это подсказка хосту нового scope («эти строки уже contradicted в scope X»), не перенос вердикта; `previous_host` внутри ledger не меняется.

Проверка: после решения с contradicted нормой `contradicted_code` называет её code-цитаты с `config` и `run_id`; строки приписаны только contradicted-нормам.

### 42.3 Допустимые диапазоны в отказе цитаты

Требование: отказ «цитата вне блока назначенного требования» (REQ-SA-006) дополняется «допустимые диапазоны: path:A-B, …» — `accepted.citations` нормы или её `source`; текст цитат в отказ не попадает (REQ-SA-015).

Проверка: отказ содержит диапазон принятой цитаты нормы.

Источник: отчёт `sa-clean` run messaging-01/inbound-01. Наблюдение без изменения поведения: пустой ledger любого scope имеет один и тот же `base_index` (хэш пустого журнала) — свежий CAS разных scope неразличим по значению, что нормально. Зависимость: §21.1, §22.1, §34, §41.3, REQ-SA-006, REQ-SA-015.

## 43. Уроки пятнадцатого прогона: `related` и `contradicted_elsewhere`

Пятнадцатый чистый прогон 2026-09-18 — малые разделы `08-webhooks` (28 норм, 28/0) и `03-technical-incident` (33 нормы, 32/1). Хост фильтровал 509 строк `contradicted_code` (§42.2) вручную, чтобы найти, что limitation обеих ролей по 03/011/020 — уже вынесенный дефект finance 023 / rent 039. Решение (очередь ведётся автономно по поручению владельца 2026-09-18, редакция 1.23): обратный индекс под нормой.

### 43.1 `related` в CONFIG и `contradicted_elsewhere` в `outcomes[]`

Требование: необязательное поле CONFIG `related` — список путей к соседним scope-CONFIG (относительно этого файла); оно не входит в manifest и не меняет `snapshot_id`. При чтении `review` (`summary`, полный контекст, `REQ-ID`, `text`/`brief`) для каждой нормы вычисляется `contradicted_elsewhere[]` — пересечения code-цитат ролей текущего run с `contradicted_code` (§42.2) решённых run соседних scope: `{config, run_id, requirement_id, path, line_start, line_end, role}`, без повторов по паре (scope, норма, роль); текст `brief`/`text` печатает «path:A-B (роль) уже contradicted в <scope>/<run> <ID>». Недоступный или повреждённый сосед пропускается с DEBUG. Подсказка не переносит вердикт и не участвует в `agree`.

Проверка: второй scope с `related` на первый после решения первого с contradicted-нормой — под нормой с теми же code-цитатами подсказка называет config, run и ID соседа; под другими нормами пусто; без `related` список пуст; `brief` содержит «уже contradicted в».

Источник: отчёт `sa-clean` run webhooks-01/incident-01 (grep по 509 строкам вручную). Наблюдения без изменения поведения: на «чистом» разделе память не с чем пересекать — считать закрытые ссылкой limitations, а не нормы; нормы-«граница окружения» (A-107 вне production) повторяются — оговорку окружения фиксировать как exception на приёмке (SKILL.md). Зависимость: §24.1, §34, §42.2.

## 44. Уроки шестнадцатого прогона: точность кросс-scope памяти, свёртка, пределы

Шестнадцатый чистый прогон 2026-09-18 — `04-pricing-entry` (94 нормы, два пакета, `related` на десять scope; 78/14/2). `contradicted_elsewhere` пришёл к 31 норме из 94: три нормы и одна limitation решены по памяти консистентно, но ~⅓ подсказок — ложные: нормы о маппинге получали их через строки PricingMarketResolver, которые роли других scope цитировали как подтверждающий контекст под contradicted-вердиктом селектора; на 030 `brief` печатал 36 строк подряд; словесный `likely_duplicate` не поймал пересказ §29 (английский pseudo-code против русской прозы); отказ типа JSON не назвал поле; `prepare` отказал на заранее созданном пустом каталоге run. Решение (по поручению владельца 2026-09-18, редакция 1.24).

### 44.1 Память только по противоречащим цитатам, `role_here`

Требование: `contradicted_code` (§42.2) берёт по contradicted-норме только code-цитаты ролей, чья собственная оценка `contradicted`, и собственные цитаты хоста (не объединённые свидетельства поддержавшей роли); элемент `contradicted_elsewhere` (§43.1) получает `role_here` — implementation той роли текущего run, чья цитата пересеклась. Контекстные строки внутри contradicted-оценки роли остаются известным пределом: разметки «противоречащая/контекстная» цитата в форме ответа нет, поэтому пересечение с ролью, которая здесь supported, хост читает как вероятный контекст.

Проверка: `contradicted_code` содержит только строки из contradicted-оценок ролей; на пилотном run pricing-entry из 31 нормы с подсказками 8 подкреплены contradicted-ролью здесь, 23 — только контекст.

### 44.2 Свёртка в `brief`/`text`

Требование: строка памяти печатается одна на пару (соседний scope, норма): «уже contradicted в <scope>/<run> <ID> (пересечений цитат: N; здесь роли тоже contradicted | здесь роли не contradicted — вероятно, контекстные строки)»; JSON `contradicted_elsewhere` не сворачивается.

### 44.3 Предел словесной меры

Наблюдение: `text_similarity` (§42.1) не ловит пересказ другим языком или стилем (pseudo-code против прозы) — известный предел; дедупликацию таких пересказов делает хост (`defer` с причиной), семантическая мера не вводится.

### 44.4 Имя поля в отказе типа

Требование: отказ «JSON не соответствует типам/полям контракта» дополняется «поле <путь> ожидает <тип>, получено <тип>» или «неизвестное поле <имя>»; значения в текст не попадают (REQ-SA-015).

### 44.5 `prepare` в заранее созданный каталог

Требование: «run уже существует» означает наличие `manifest.json`; каталог run без manifest (хост заранее создал `dispatch/host/` под свои файлы) `prepare` использует. Прочие проверки §24.2 не меняются.

Проверка: `prepare` в каталог с пустым `dispatch/host/` проходит; повторный `prepare` — отказ.

Источник: отчёт `sa-clean` run pricing-entry-01. Зависимость: §42.1–42.2, §43.1, REQ-SA-015, §24.2.

## 45. Сводная карта корпуса: `corpus OUT_HTML CONFIG...`

Владелец 2026-09-18: состояние проекта должно быть видно в одном месте, а не в одиннадцати `report.html`. Решение (редакция 1.25): статическая страница поверх `overview` (§34, §42.2, §44.1); отдельных аудитов, усреднений и заявлений о полноте корпуса она не делает.

Требование: `corpus OUT_HTML CONFIG...` выполняет расчёт `overview` по перечисленным CONFIG и записывает в `OUT_HTML` (файл хоста, целиком, не внутри run) одну HTML-страницу без скриптов (CSP `default-src 'none'`): итоги (нормы, run, счётчики implementation/assertion), таблица scope (имя каталога CONFIG, режим и свежесть индекса, нормы, run, решённый run со ссылкой на его `report.html` относительно OUT_HTML, если тот существует, состояние и форма решения, версия бинарника, признак изменившегося snapshot, счётчики, число расхождений ролей), раздел «Что требует внимания» — по каждому scope вердикты хоста решённого run, отличные от clear/supported/relevant, с заголовком нормы и statement хоста, и раздел «Код под противоречиями» — файлы из `contradicted_code` с числом норм и списком scope. Для этого `overview` дополняется `decided.attention[]` (`{id, title, specification, implementation, assertion, statement}`) и `decided.report` (относительная ссылка, только у `corpus`). Ответ команды — `{html, overview}`. Текст страницы экранируется шаблоном; strings из ответов ролей и решений остаются данными.

Проверка: после решения и `report` одного scope `corpus` пишет страницу с именем scope, contradicted-нормой, заголовком нормы внимания, ссылкой `../runs/<run>/report.html` и файлом кода; `corpus OUT` без CONFIG — отказ.

Источник: решение владельца 2026-09-18 после 16 прогонов (681 норма в 11 scope). Зависимость: §15 (отчёт run), §34, §42.2, §44.1.

### 45.1 GAP

Требование: `overview` (в `totals` и в `decided` каждого scope) и страница `corpus` несут `gap` — число норм, соответствие которых не доказано, в принятом в индустрии смысле gap analysis «требование → реализация → верификация»: `implementation` — вердикт хоста contradicted или unknown; `verification` — реализация supported, но assertion не relevant (missing, contradicts, weak, unknown); `specification` — норма ambiguous при supported и relevant. Каждая норма считается один раз в самом тяжёлом классе (implementation > verification > specification); `total` — сумма классов и равна числу норм раздела «Что требует внимания». Существующие счётчики и разделы не меняются; GAP — сумма вердиктов хоста, не новая оценка.

Проверка: решение с contradicted, двумя unknown и одной supported/weak → `gap {total 4, implementation 3, verification 1, specification 0}`, равен числу норм внимания; карточка «GAP» на странице.

Источник: решение владельца 2026-09-18. Зависимость: §14 (состояния), §45.

## 46. DECISION приёмки version 2: сужение statement кандидата

Хост пилота дважды (inventory C026, provider-integrations) принимал кандидата с утверждением шире текста ТЗ, потому что выбор был только «принять как извлечено» или «reject и потерять норму»; контракт [принятого индекса](accepted-index.md) запрещает хосту переформулировать. Решение владельца 2026-09-18 (редакция 1.27): вариант B — сужение, не редактирование; контракт не переписывается, вводится вторая версия формы DECISION с отдельным контролем. Тем же решением multi-raw пакет отклонён: разделы крупнее 64 обязанностей принимаются несколькими пакетами через `keep` (§39), вопрос закрыт.

### REQ-SA-049 — Сужение statement при принятии

Условие: DECISION с `version: 2` — та же форма, что version 1, но каждый target несёт `narrowed_statement` (`""`, когда не используется).

Требование: при `accept`/`revise`/`split`/`merge` непустой `narrowed_statement` становится `statement` принятой нормы; допустим только если он непуст, отличается от statement кандидата и каждое его слово (буквы/цифры, без учёта регистра) есть в statement кандидата — смысл можно убрать, добавить нельзя; иначе отказ с названием кандидата и добавленного слова. `reject`/`defer`/`reanchor` с непустым `narrowed_statement` — отказ; `rebind` с сужением отказывает сам (content hash меняется — это `revise`). Исходный statement остаётся в raw внутри журнала; журнал воспроизводит решение version 2 при каждом чтении; content hash, revision и `snapshot_id` считаются по суженному statement. Решения version 1 принимаются как прежде; иная версия — отказ до чтения источников. Форма ответов `check`/`reconcile` не меняется.

Проверка: добавление слова → отказ «добавляет слово»; тот же statement → отказ; `defer` с сужением → отказ; сужение → `check` valid, после `reconcile` запись и `index` несут суженный statement, журнал хранит оригинал в raw; `version: 3` — отказ.

Источник: отчёты `sa-clean` run inventory-01 (C026) и provider-integrations-01; решение владельца 2026-09-18. Зависимость: accepted-index.md п.1–4, REQ-SA-042, §39.

## 47. Динамика GAP: `delta` и `baseline_run`

Владелец 2026-09-18: перед исправлением продукта нужно зафиксировать точку «до», чтобы после перепрогона на карте корпуса было видно, что закрылось и что открылось. Решение (редакция 1.28): разность двух решений хоста, вычисляемая при чтении; ничего не записывается, качество фикса не измеряется.

Требование: `overview` (и `corpus`) по каждому scope с двумя и более решёнными run отвечает `delta`: `baseline_run` — решённый run, с которым сравнивается текущий `decided` (предыдущий решённый по времени `prepare`, либо run из необязательного поля CONFIG `baseline_run`, вне manifest, тогда `pinned: true`; отсутствующий или нерешённый `baseline_run` — WARN и сравнение с предыдущим); норма сравнивается только при равных ID, `content_hash` и `revision` (§40.1), иначе попадает в `incomparable` (новые, снятые, пересмотренные); `closed` — была GAP (§45.1) в baseline и не GAP теперь, `opened` — наоборот, `changed` — GAP в обоих при разных состояниях, `unchanged` — число совпавших; каждый переход несёт `id`, `title`, `from`/`to` как `specification/implementation/assertion`; `gap_before`/`gap_after` — счётчики обоих run. `totals` получает `closed`/`opened` — суммы по scope. Страница `corpus` показывает карточку «динамика GAP» и раздел «Динамика» по scope (baseline → текущий, GAP до/после, списки закрытых, открывшихся, изменившихся, несравнимые); scope с одним решением сравнивать не с чем.

Проверка: второе решение с закрытой contradicted-нормой и новой weak → `closed` 1, `opened` 1, `incomparable` 0, `totals.closed/opened` 1/1; третье решение без изменений против второго → 0/0; `baseline_run: r1` в CONFIG → сравнение снова с r1, `pinned`; страница содержит «Динамика» и «r1 (baseline) → r3».

Источник: решение владельца 2026-09-18; на пилоте (11 scope) динамика сразу показала foundation 01→02 (3 закрыто / 3 открылось / 44 несравнимых — второй пакет) и rent 01→02 (перестановки assertion). Зависимость: §25, §40.1, §45, §45.1.

## 48. Разбор внешнего review 2026-09-18

Владелец передал статическое review кода (выполнено до §30–§47; временный файл review не хранится — открытые пункты перенесены в ROADMAP). Разбор по пунктам: что подтверждено и исправлено (редакция 1.29), что уже закрыто прежними разделами, что остаётся решением владельца.

### 48.1 Полнота свидетельств для решения version 2 (пункт 1 review)

Требование: проверка §7 на объединённых свидетельствах (spec обязательна; supported/contradicted требуют code; relevant/weak/contradicts требуют тестовое свидетельство), введённая для version 3 (REQ-SA-046), действует и для version 2: вердикт `relevant` с `concur: redteam`, у которого нет тестовых цитат, отклоняется «оценка assertion требует тестовый источник»; отказ не пишет журнал. §23 «вердикты без цитат» означает, что хост не копирует цитаты, а не что положительное состояние допустимо без основания.

Проверка: `TestReviewV2` — `relevant` при `concur: redteam` без тестов → отказ; с `unknown` — принято, свидетельства только redteam.

### 48.2 Права бинарника до commit point `update` (пункт 2.4)

Требование: `atomicWrite` выставляет режим на временном файле до `sync`/`rename`; `update` больше не вызывает `Chmod` после публикации — любой отказ действительно оставляет прежние байты (§18).

### 48.3 Отмена дочерних процессов по сигналу (пункт 2.2)

Требование: контекст процесса создаётся через `signal.NotifyContext` (SIGINT/SIGTERM); таймауты `test`, `php-facts`, `php-typed` наследуют его — прерывание CLI останавливает дочерние процессы и контейнерные команды; очистка контейнера сохраняет собственный ограниченный таймаут.

### 48.4 Мелочи, закрытые тем же выпуском

`report.html`: группа `reference` подписана «справочных файлов ТЗ» (2.7); `install.sh`: таймауты `curl` (2.7); регулярные выражения вынесены из циклов, `sameStrings` → `slices.Equal` (3); CI на push/PR (`ci.yml`: vet, gofmt, `go test -short`, хэши приёмки) — 2.5 частично.

### 48.5 Закрыто прежними разделами

2.3 «каталог run без manifest блокирует prepare» — §44.5; 2.7 «dispatch не изолирован по run» — с §24.2 каталог лежит внутри `reports_dir/RUN_ID/`, два run не делят его; «причина невозможности snapshot маскируется» — `overview` называет `error` (§34).

### 48.6 Остаётся решением владельца

2.1 свежесть accepted-индекса при смене группы файла (specs ↔ references) — требует версионирования ledger; 2.3 восстановление прерванной установки skill (receipt последним) — staging-каталог; 2.5 независимые literal-векторы `counts` и проверка прозы `forbidden` в PHP-контроле — контроль не машиночитаем; 2.6 первая цитата из reference и секция отчёта — контракт §16.1/§20; 2.7 фильтр трейтов в `sdk-typed.php` — нужна контейнерная приёмка; 4 производительность — без измерений не трогать; 7 сравнение метода с простым AI-baseline — исследование (ROADMAP «Исследования»).

## 49. Публикация для заказчика: `publish OUT_DIR CONFIG...`, история run

Отчёты живут на машине прогонов; владелец 2026-09-18: нужна простая публикация — отдать HTML на любой сервер так, чтобы карта подтягивала все проходы, и выбирать проход по времени. Решение (редакция 1.30): статический сайт без серверной части и без скриптов на карте; выбор по времени — список решённых run со временем `prepare` в самой карте, а не отдельный элемент управления.

Требование: `publish OUT_DIR CONFIG...` выполняет расчёт `overview` и записывает в `OUT_DIR` (файл-дерево хоста, создаётся; существующие файлы перезаписываются, лишние не удаляются): `index.html` — карта корпуса (§45) со ссылками вида `<scope>/<run_id>/report.html`, `overview.json` — данные карты, и по каждому решённому run каждого scope копию его `report.html` (байт-в-байт) в `<scope>/<run_id>/report.html`, где `<scope>` — имя каталога CONFIG; run без `report.html` пропускается с DEBUG. `overview` дополняется `scopes[].history[]` — все решённые run scope, новые первыми: `run_id`, `prepared_at`, `review_id`, `host_review_state`, `gap`, `report` (относительная ссылка; у `corpus` — к `reports_dir`, у `publish` — внутри сайта). Карта показывает в строке scope раскрывающийся список «N решённых run» с временем, состоянием решения и GAP — история выбирается по времени ссылкой. Ответ: `{dir, index, reports, scopes}`. Ограничения: сайт статичен на момент публикации; отчёты run содержат цитаты ТЗ и кода и значения путей CONFIG в настройках редактора — публиковать только туда, где это допустимо; полнота корпуса не утверждается.

Проверка: два решённых run с отчётами → `index.html`, `overview.json`, `<scope>/p1/report.html`, `<scope>/p2/report.html`; ссылки в index на обе копии и «2 решённых run»; `history` новые первыми со ссылками сайта; копия отчёта байт-в-байт; `publish` без CONFIG — отказ.

Источник: решение владельца 2026-09-18. Зависимость: §15, §45, §47.

## 50. Инкрементальный run после изменения кода

После исправления продукта полный run по scope стоит как первый аудит (12 ролей, 3–4 M токенов на 100 норм), хотя изменились несколько файлов. Решение владельца 2026-09-19 (редакция 1.31): роли переоценивают только затронутое, остальные вердикты переносятся с провенансом и явной пометкой; полный run остаётся эталоном.

### REQ-SA-050 — `prepare CONFIG RUN_ID since PREV_RUN`

Условие: PREV_RUN — run того же `reports_dir`, доставленный полностью и имеющий решение хоста; текущий snapshot построен как обычно (свежий accepted-индекс).

Требование: по каждой норме текущего manifest бинарник решает, оценивают ли её роли заново или она переносится: заново — если (а) нормы с тем же `id`, `content_hash` и `revision` (§40.1) нет в PREV_RUN или у неё нет вердикта в последнем решении, (б) вердикт PREV_RUN — GAP (§45.1), (в) любой файл, процитированный объединённой оценкой хоста в PREV_RUN (spec, code, tests), изменил sha256 или отсутствует в текущем snapshot; иначе норма переносится. `manifest.incremental` = `{since_run, review_id, assessed[], reasons{id: new|gap|changed}, carried[]}`, где `carried[]` — вердикт хоста PREV_RUN по норме (состояния, statement, limitations, объединённые цитаты spec/code/tests) и `files` — цитируемые файлы с текущими sha256; `snapshot_id` считается до добавления поля и не меняется. Задания строятся только по переоцениваемым нормам; scope без них не получает заданий; run без заданий доставлен сразу. Ответ `prepare`/`tasks` несёт `incremental: {since_run, assessed, carried, reasons{}}`. Решение хоста (§14, §23, §25) по-прежнему покрывает все нормы: для перенесённой нормы `concur: carried` — свидетельства из `manifest.incremental.carried`, состояния должны совпадать с перенесёнными, иначе требуются собственные цитаты хоста (version 3); пустой statement сохраняет прежний; `mapper`/`redteam`/`both` на перенесённой норме и `carried` на переоцениваемой — отказы; version 1 — полные оценки, как прежде. `draft` предзаполняет перенесённые нормы (`concur: carried`, состояния, statement, limitations) — это прежнее суждение того же хоста, не роли. Чтение: `outcomes[].carried = {run_id, review_id, состояния}` без ролей и с `agree: true`; `review … summary` — `carried`/`assessed`; `text`/`brief` — строка «Перенесено из …»; `report` — флаг и фильтр `carried`, `navigation.carried_from`, подпись «перенесено из прежнего run»; `overview.decided.carried`/`since_run`, на карте — «инкрементально от …»; динамика §47 работает как обычно. Отказы: неизвестный PREV_RUN, run без решения или без полной доставки.

Ограничение (названо честно): перенос опирается на неизменность цитируемых строк; изменение файла, который норма не цитирует, но от которого зависит поведение, не возвращает норму ролям. Поэтому перенесённый вердикт — прежнее суждение на неизменных свидетельствах, не повторная проверка; полный run периодически и перед приёмкой замены старого аудита обязателен.

Проверка: полный run r1 с решением; `since nowhere`, `since` недоставленного run — отказы; `r2 since r1` без изменений — переоцениваются только GAP-нормы, остальные перенесены с причинами; правка нецитируемого файла не меняет набор, правка цитируемого возвращает норму ролям; `draft` предзаполнен; отказы `mapper` на перенесённой, `carried` на переоцениваемой, иные состояния без цитат; решение принято; `report` помечает перенесённые; `overview` показывает `carried`, `since_run` и динамику против r1.

Источник: решение владельца 2026-09-19; пилот после фикса рынков — 84 изменённых файла, 13 под contradicted, полный перепрогон восьми scope ≈ 25 M токенов. Зависимость: §14, §23, §25, §40.1, §45.1, §47.

## 51. Точность инкрементального run

Первый живой инкрементальный run (`inventory-2026-09-19-01`, 28 норм) переоценил 17 норм четырьмя заданиями и стоил 90 % полного: класс GAP переоценивался целиком (11 из 28, включая ambiguous-нормы, чьё состояние не зависит от кода), «changed» срабатывал на правку файла вне цитируемых строк, а стоимость роли почти не зависит от числа норм (6 норм — 277 k, 11 — 325 k: роль читает протокол, `files.json` и код вокруг нормы). Решение (редакция 1.33): экономить на числе заданий и переоценивать только то, что мог изменить код.

### 51.1 Отбор норм

Требование: в `prepare … since PREV_RUN` (§50) норма переоценивается, если (а) она новая или пересмотрена; (б) её implementation в PREV_RUN не supported (contradicted/unknown — implementation gap, всегда); (в) её assertion не relevant (verification gap) **и** хотя бы один файл группы `tests` изменился или появился с PREV_RUN; (г) хотя бы одна цитата объединённого вердикта хоста (spec, code, tests) больше не стоит на своих строках — сравнивается quote по `line_start`–`line_end` в текущем файле, а не sha256 файла. Ambiguous-норма с supported/relevant (specification gap) переносится: её состояние определяется текстом ТЗ, а не кодом. Причины в `manifest.incremental.reasons`: `new | gap | changed`.

### 51.2 Причины в чтении

Требование: `outcomes[].reassessed` — причина переоценки нормы в инкрементальном run (`new|gap|changed`; пусто в полном run); `text`/`brief` печатают «Переоценена в инкрементальном run: причина …»; `report.json` несёт верхнеуровневый `incremental` (как ответ `prepare`), а перенесённая норма получает `scope` своего scope-конфига.

### 51.3 Промпт роли

Требование: `prompt.md` инкрементального run заканчивается блоком «ИНКРЕМЕНТАЛЬНЫЙ RUN»: продолжаемый run, выданные нормы с причинами, число перенесённых норм и указание не искать и не оценивать остальные.

### 51.4 `plan CONFIG PREV_RUN`

Требование: read-only ответ `{since_run, review_id, assessed[], reasons{}, carried, tasks[]}` — что дал бы `prepare … since` без создания run; те же отказы, что у `prepare … since`.

### 51.5 Регруппировка заданий

Требование: в инкрементальном run переоцениваемые нормы всех scope собираются в задания `incremental-N` по не более `scopeAdvisoryRequirements` (24) норм в порядке scope-конфига; `focus` называет исходные scope и PREV_RUN; scope без переоцениваемых норм заданий не получает. На пилоте после фикса рынков это даёт 24 задания вместо 60 по семи scope (finance 2 вместо 6, inbound 2 вместо 8, provider-integrations 4 вместо 12).

Проверка: без изменений переоцениваются только implementation-gap-нормы; правка файла тестов возвращает weak-нормы; дописанный файл без сдвига цитат — не `changed`, сдвиг строк — `changed`; промпт содержит блок и причины; `plan` отвечает без run; задания `incremental-N`.

Источник: отчёт `sa-clean` run inventory-2026-09-19-01. Зависимость: §20 (рекомендация размера), §40.1, §45.1, §50.

## 52. Инкрементальный run: известные дефекты, причины, счётчики

Инкремент `finance-2026-09-19-01` (0.1.39) выполнил критерий: две роли по 19 норм — 0,76 M против 2,37 M полного (32 %), 24 минуты против 36. Оставшиеся замечания хоста: contradicted-нормы переоценивались каждый инкремент при неизменных файлах и давали тот же результат; причина `gap` не говорила, какой это GAP; в динамике «без изменений 51» не различало перенесённые и перечитанные. Решение (редакция 1.34).

### 52.1 Известные дефекты переносятся

Требование: contradicted-норма переносится, пока все её цитируемые строки стоят на месте (правило «changed» §51.1 распространяется и на неё): тест не может исправить противоречие, а противоречащие строки не изменились; такой перенос помечен `known_defect: true` в `manifest.incremental.carried[]`, `outcomes[].carried.known_defect`, `review … summary` (`known_defects`), ответе `prepare`/`plan`/`report.json` (`incremental.known_defects`), карте корпуса («из них N известных дефектов») и в `brief` («известный дефект: противоречащие строки не менялись»). Изменение любой цитируемой строки возвращает норму ролям с причиной `changed`. Нормы с implementation `unknown` переоцениваются всегда.

### 52.2 Причины и счётчики

Требование: причины переоценки — `new | implementation_gap | verification_gap | changed` (вместо общего `gap`), те же в промпте роли; `delta` (§47) дополняется `carried` — сколько из «без изменений» перенесено без переоценки; карта корпуса показывает это в строке динамики.

Проверка: без изменений переоцениваются только unknown-нормы, contradicted переносятся как известные дефекты (`known_defects`); правка тестов возвращает weak-нормы, известные дефекты остаются; сдвиг цитируемых строк возвращает всё; промпт называет `implementation_gap`/`changed`.

Источник: отчёт `sa-clean` run finance-2026-09-19-01. На пилоте по пяти оставшимся scope `plan` даёт 18 заданий вместо 46; contradicted-нормы затронутых веток возвращаются как `changed` — их строки фикс сдвинул. Зависимость: §50, §51.

## 53. Ухудшения внутри GAP и предупреждения перед решением

Инкременты пяти scope после фикса рынков (0.1.40): 18 ролей, 7,2 M против ≈16,1 M полных (45 %; 36–43 % на четырёх scope, 74 % на activation, где фикс перекроил buy-flow), GAP корпуса 288 → 253, кластер рынков закрыт. Два замечания хоста: переход supported → contradicted у нормы, уже бывшей в GAP (inbound 008), терялся в «изменилось»; `validate host` отказывал «assertion требует тестовый источник», когда хост повторял прошлый contradicts, а роли поставили missing — предупредить можно раньше. Решение (редакция 1.35).

### 53.1 Ухудшения в динамике

Требование: `delta` (§47) дополняется `worsened[]` — подмножество `changed`, где implementation стала хуже (supported → unknown → contradicted) или assertion хуже (relevant → weak → unknown → missing → contradicts); `totals.worsened`; карта корпуса показывает «ухудшилось внутри GAP» отдельным списком и счётчиком.

### 53.2 Предупреждения перед решением

Требование: `review … summary` несёт `advisories[]`: для переоцениваемой нормы, у которой `previous_host.assertion` ∈ {relevant, weak, contradicts}, а ни одна роль этого run не цитирует тестов, — предупреждение, что повторить вердикт можно только с собственной тестовой цитатой (version 3); список пуст, если предупреждать нечего.

Проверка: ухудшение implementation у GAP-нормы попадает в `worsened` и в `totals.worsened`; `advisories` в сводке.

Источник: отчёт `sa-clean` по пяти инкрементам 2026-09-19. Известный предел (не меняется): contradicted-норма возвращается ролям при изменении любой цитируемой строки, включая контекстные — разметки «противоречащая/контекстная» цитата нет. Зависимость: §47, §50–§52.

## 54. Перепрогон затронутых scope одной командой: `rerun RUN_ID CONFIG...`

После фикса хост готовил инкрементальные run пятью вызовами `prepare … since`, выбирая baseline руками. Решение (редакция 1.36, milestone ROADMAP): одна команда, роли — как обычно.

Требование: `rerun RUN_ID CONFIG...` для каждого CONFIG готовит инкрементальный run `RUN_ID` (§50–§52) от `baseline_run` этого CONFIG, а без него — от последнего решённого run; RUN_ID один на все scope (у каждого свой `reports_dir`) и подчиняется правилам slug. Scope без решённого run и без `baseline_run` — `skipped`; scope, где `RUN_ID` уже существует, — `skipped: run уже существует`; отказ `prepare` одного scope записывается в `error` и не прерывает остальные. Ответ: `{run_id, scopes[]: {config, since, run_id, assessed, carried, known_defects, tasks[], skipped?, error?}, tasks_total}` — очередь заданий для хоста. Роли, решения, `report`, `corpus`/`publish` — по каждому scope как прежде.

Проверка: три CONFIG — с решённым run, с `baseline_run` и без решения → два run подготовлены (baseline имеет приоритет), третий пропущен; повторный вызов с тем же RUN_ID пропускает готовые run; недопустимый RUN_ID — отказ.

Источник: milestone «Перепрогон затронутых scope одной командой»; инкременты пяти scope 2026-09-19 руками. Зависимость: §47 (`baseline_run`), §50–§52.

## 55. Журнал индекса version 2: классификация источников в основании пакета

Основание пакета хранило только path и sha256 файлов source_set, поэтому перенос неизменённого файла между `specs` и `references` при том же наборе оставлял индекс `fresh` вопреки §20 (внешнее review 2.1, milestone «Свежесть accepted-индекса при смене группы файла»). Решение (редакция 1.37): версионированный журнал; старые ledger не переписываются, политика для истории без классификации явная.

Требование: файл журнала индекса пишется в `version: 2` — каждый commit несёт `references` (отсортированные пути source_set, бывшие справочными при применении) и `classified: true`; журнал `version: 1` читается как прежде, его commits получают `classified: false` и пустой `references`; иная версия — отказ. Состояние индекса запоминает классификацию последнего пакета; после первого нового пакета файл сохраняется в version 2 целиком, прежние commits остаются неклассифицированными. Свежесть (§21, §26.1): помимо совпадения путей и sha256, при `classified: true` текущее множество справочных файлов должно равняться записанному — иначе `stale`, и `index` в accepted-режиме отказывает; без классификации сравниваются только байты (история до 1.37). Контракт [принятого индекса](accepted-index.md), форма raw и DECISION не меняются.

Проверка: после `reconcile` журнал version 2 с `references: [clarification.md]`; перенос файла в `specs` при тех же байтах → `stale`, `index` — отказ; возврат — `fresh`; журнал version 1 читается, не классифицирован, fresh при любой группировке; следующий пакет пишет version 2, старый commit `classified: false`; `version: 3` — отказ. На пилоте 11 журналов version 1 читаются, все `fresh`.

Источник: внешнее review 2026-09-18, пункт 2.1. Зависимость: §20, §21, accepted-index.md.

## 56. Релизная квалификация без `-short`

Внешнее review (2.5) назвало слепые зоны: релиз квалифицировался `go test -short` (без нативной поставки `TestNativeDistribution` и реального runtime), допускал любой отказ `update` при квалификации, ожидаемые `counts` строились той же функцией, что и проверялись, Actions не были закреплены. Решение (редакция 1.38, milestone «Релизная квалификация без -short»).

Требование: `release.yml` на обеих платформах выполняет `go test ./tool -count=1` полностью (поставка через `install.sh` по `file://`, реальный `go test` runtime, контейнерные тесты по-прежнему пропускаются без Docker), а `ci.yml` на push/PR — `go test -count=1 ./...`; квалификация `update` копии против прежнего `latest` обязана завершиться успешно с `updated: false` и не изменить байты — отказ `update` есть отказ релиза; все `uses:` закреплены полным SHA коммита с пометкой версии; `TestReviewValidateHost` держит литеральный вектор `counts` и мутирует каждое из девяти полей, ожидая отказ с именем поля. §18 не меняется.

Проверка: тег → job `build` на ubuntu и macos проходит полный набор; литеральный вектор совпадает с фикстурой и принимается, каждая мутация отклоняется по полю.

Источник: внешнее review 2026-09-18, пункт 2.5. Зависимость: §18, §23.

## 57. Установка skill через staging

Внешнее review (2.3): `skill install` писал файлы прямо в `.spec-audit/skill/` и receipt последним, поэтому прерывание между первым файлом и receipt оставляло каталог, который §18 классифицирует как чужой («без receipt») и который отвергают все четыре команды, включая `install --replace`. Фраза §18 «повторный `skill install` завершает частичную копию» не выполнялась. Решение (редакция 1.39, milestone «Установка skill через staging»).

Требование: когда `.spec-audit/skill` отсутствует или является заменяемой по `--replace` символической ссылкой, полный комплект файлов и receipt собираются в staging-каталоге `.spec-audit/.skill-staging-<случайный суффикс>` внутри `os.Root(DIR)` и публикуются одним `rename` в `.spec-audit/skill` (ссылка снимается непосредственно перед rename, её цель не трогается). Отказ ввода-вывода до публикации удаляет staging-каталог и не оставляет `.spec-audit/skill` вовсе — повторный `skill install` устанавливает заново; частичная копия без receipt новой версией не создаётся, поэтому строка §18 «повторный `skill install` завершает её» заменяется на «повторный `skill install` устанавливает заново». Наш каталог (`ours`) по-прежнему обновляется на месте по одному управляемому файлу: старый receipt сохраняет принадлежность каталога до записи нового последним, а прерванное обновление завершается следующим `update`. Оставшийся после аварийного завершения процесса staging-каталог (kill, потеря питания) — чужой для последующих команд: не читается, не переиспользуется и не удаляется; состояние `.spec-audit/skill` определяется только им самим. Классификация «чужой каталог без receipt» и отказ по ней не меняются: каталог с управляемыми файлами, но без receipt, по-прежнему не заменяется никакой командой.

Проверка: отказ записи при сборке свежей копии (`TestSkill/interrupted_install_leaves_nothing`) — `.spec-audit/skill` отсутствует, staging-каталога нет, повторный `install` успешен; оставленный staging-каталог не мешает установке и сохраняется байт-в-байт (`leftover_staging_ignored`); после каждой успешной команды staging-каталогов нет; замена symlink (`symlink_replace`) даёт реальный каталог, цель ссылки нетронута; чужой каталог без receipt отвергается и с `--replace` (`foreign_dir`).

Источник: внешнее review 2026-09-18, пункт 2.3. Зависимость: §18 (REQ-SA-037), §31.3 (политика постороннего симлинка).

## 58. Фильтр фактов экспортёра по множеству `files`

Внешнее review (2.7): `sdk-typed.php` ставит путём цитаты факта из трейта файл трейта, но не проверял, входит ли он в запрошенное множество `files`, хотя [контракт](php-sdk-contract.md) это требует; Go принимает цитаты только из `files`, поэтому один такой факт отклонил бы весь envelope. Решение (редакция 1.40, milestone «Фильтр трейтов в PHP-экспортёре»).

Требование: `FactSink` перед сборкой envelope отбрасывает факты, чей `citation.path` не входит в множество проанализированных файлов (`FileNode`), — контракт «путь цитаты ∈ `files`» выполняется экспортёром, а не только проверяется Go. Синтетический пример `acceptance/php-sdk/plain` содержит трейт `src/Support/Audited.php` и использующий его класс `src/AuditedStore.php`; контейнерная приёмка `TestExternalTypedPlain/trait_outside_files` запрашивает только класс и требует принятого envelope с `files` из одного пути и без цитат файла трейта, а полный прогон — фактов трейта.

Результат контейнерной приёмки 2026-09-19 (PHPStan 2.x): сценарий review не воспроизводится и без фильтра — PHPStan обходит тело трейта в контексте класса только когда файл трейта сам входит в анализируемые пути, поэтому факт с путём вне `files` прежний экспортёр не выдавал. Фильтр оставлен как исполнение контракта на стороне экспортёра; изменение `sdk-typed.php` меняет `sdk_sha256`, фикстуры `tool/testdata/typed-*.json` перезаписаны внешними тестами. `docs/php-sdk-contract.md` не меняется.

Проверка: `TestExternalTypedPlain` (Docker) — полный прогон содержит цитаты `src/Support/Audited.php`, прогон по `src/AuditedStore.php` принят без них; `TestTypedControl` на перезаписанных фикстурах.

Источник: внешнее review 2026-09-18, пункт 2.7. Зависимость: §17, контракт typed SDK (путь и цитата).

## 59. Владелец нормы в сводке — первая нормативная цитата

Внешнее review (2.6): §16.1 относил норму к файлу первой цитаты (`Source.Path`), а §20 допускает у кандидата справочную цитату первой; такая норма попадала в сводке отчёта под справочный файл, хотя REQ-SA-041 обещает у справочных файлов «ноль норм». Решение (редакция 1.41, milestone «Контракт первой справочной цитаты»).

Требование: производная `navigation.requirements[].section` и строки `navigation.sections[]` отчёта относят норму к первой цитате принятого требования (`accepted.citations`, для объявленных норм — `source`), чей файл не помечен `reference: true` в manifest run; норма без нормативной цитаты (невозможна после REQ-SA-041, возможна в старых журналах) остаётся у `Source.Path`. `Source`, `accepted.citations`, raw, журнал приёмки, manifest и `snapshot_id` не меняются — правило действует только на производную сводку (§16.1), поэтому прежние run и индексы не становятся stale, а отчёт старого run при пересборке новым бинарником меняет лишь сводку.

Проверка: `TestReferenceSources/report` — пакет из двух кандидатов, у второго справочная цитата первая: секция справочного файла показывает 0 норм, нормативного — 2, `navigation.section` нормы = нормативный файл, `requirement.source.path` = справочный; без правила тест падает.

Источник: внешнее review 2026-09-18, пункт 2.6. Зависимость: §16.1, §20 (REQ-SA-041).

## 60. Бенчмарки большого run и кэш проверенных источников

Внешнее review (4) назвало кандидатов: повторное чтение и хэширование файла на каждую цитату (в том числе при `status`), O(R×B) в `lineQuote`, полный DOM отчёта. Решение владельца — «без измерений не трогать». Измерения (редакция 1.42, milestone «Бенчмарки и повторное чтение файлов»).

Измерение. `tool/bench_test.go`: синтетический legacy-run из 96 норм в 4 scope, 48 кодовых файлов по ~200 КБ (2000 строк), обе роли доставлены, по четыре кодовые цитаты на норму (768 цитат), плюс отдельные `BenchmarkLineQuote` и `BenchmarkCheckCitations` (128 цитат по 48 файлам). До изменения (Apple M1): `status` 300 мс и 793 МБ аллокаций на команду, `report` 344 мс / 813 МБ, `review … summary` 334 мс / 793 МБ, `checkCitations` 40 мс / 119 МБ, `lineQuote` 200 КБ — 80 мкс. Причина — чтение состояния run заново проверяет каждую сохранённую цитату против источника: файл читался, хэшировался и разбивался на строки на каждую цитату. На пилоте (scope `02-finance`, 2531 файл, 21 МБ, 51 норма) все read-only команды укладывались в 0,6–1,4 с, `corpus` по 11 scope — 7,8 с; время там определяется сканом и хэшированием snapshot, не цитатами.

Требование: проверка цитат в пределах одной команды использует `sourceCache` — цитируемый файл читается из `os.Root(project_root)`, сверяется с SHA-256 manifest и разбивается на строки один раз; кэш создаётся на команду (чтение state, `submit`, `validate`/`review` решения хоста) и не переживает её. Все отказы прежнего пути сохраняются дословно: «снимок источников изменился», «цитата не совпадает с полными строками path:A-B», отказ symlink/размера при чтении. Семантика не меняется: команда по-прежнему проверяет каждую цитату против текущих байтов источника. После изменения: `status` 63 мс / 123 МБ, `report` 100 мс / 142 МБ, `review … summary` 57 мс / 123 МБ, `checkCitations` 17–20 мс / 59 МБ. `lineQuote` и HTML-отчёт не трогаются: измерения не показали в них узкого места.

Проверка: `TestSourceCache` — повторная цитата того же файла читает его один раз, неверная цитата и диапазон на кэшированном файле отклоняются прежним текстом, изменённый хэш manifest — «снимок источников изменился»; `go test ./tool -run '^$' -bench . -benchmem` воспроизводит цифры (порядок, не точные значения).

Источник: внешнее review 2026-09-18, пункт 4; решение владельца «только после измерений». Зависимость: §7, §8 (проверка цитат), REQ-SA-015.

## 61. Состав контекста роли и `dispatch/files.txt`

Роли пилота стоили 0,25–0,42 M токенов на 15–25 норм. Измерение по артефактам полного run scope `02-finance` (2531 файл snapshot, шесть заданий по 16–18 норм; read-only, редакция 1.43, milestone «Токены ролей»): фиксированный вход роли — `protocol.txt` 9,6 КБ, `prompt.md` 6,4 КБ, `task.json` 47–79 КБ (нормы с принятыми цитатами ТЗ — нужны целиком), `dispatch/files.json` **529 КБ, 15 189 строк** (2531 запись с `sha256` и `bytes`; хэши — 162 КБ, около трети файла); переменный — код: только процитированные ролью файлы составляют 39–77 файлов и 460–870 КБ на роль (нижняя граница прочитанного). В грубой оценке (3–4 символа на токен для кода и путей, ~2,5 для hex и русского текста) код даёт ≥ 130–250 K токенов на роль, `files.json` при полном чтении — ещё 150–170 K, `task.json` — 20–30 K, протокол и промпт — ≤ 6 K. Точной доли по каждому источнику артефакты не дают: стенограммы ролей бинарнику недоступны, а роль читает и файлы, которые не цитирует. Вывод: единственный источник, который можно убрать из контекста роли, не сужая список файлов (§20), — сам список в форме JSON с хэшами; код и `task.json` — содержание работы.

Требование: `prepare` рядом с `dispatch/files.json` (контракт §26.3, для инструментов и хоста — не меняется) пишет `dispatch/files.txt` — тот же список в том же порядке, по строке `категория<TAB>path`, где категория — `spec`, `code`, `tests` или `reference` для справочного файла ТЗ; без хэшей и размеров. `prompt.md` роли указывает в FILES на `files.txt` и предписывает не читать его целиком, а проверять путь `grep -F` и перечислять категорию/каталог `grep`; протокол роли не меняется (он говорит о FILES, не о формате). На пилоте `files.txt` того же run занял бы ~175 КБ одной строкой на файл (в 3 раза меньше JSON), а при работе через `grep` не входит в контекст вовсе. Оценка эффекта — по следующему живому прогону (сравнение токенов ролей на том же scope), не по этому разделу.

Проверка: `TestPrepareDispatch` — `files.txt` содержит ровно по строке на файл `TaskBatch.files` в том же порядке, без `sha256`, справочный файл — `reference`; `prompt.md` называет `files.txt` и запрет читать целиком; `files.json` по-прежнему равен списку `prepare`.

Источник: ROADMAP «Токены ролей» (P3.2), измерение 2026-09-19. Зависимость: §20 (список файлов не сужается), §26.3, §30.1.

## См. также

- [README и команды](../README.md)
- [Принятие кандидатов и жизненный цикл требований](accepted-index.md)
- [Отдельная спецификация применения к SMSPlace](smsplace-adoption.md)
