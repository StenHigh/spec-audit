[← Спецификация инструмента](tool-spec.md) · [README](../README.md)

# Применение spec-audit к SMSPlace

Версия: 0.4, 2026-09-16. Статус: принят аудит по разделам; локальная установка — встроенный skill релизного бинарника, skill/ссылки/конфиг трекаются в Git пилота по решению владельца 2026-09-16 (редакция REQ-SMS-001), результаты аудита остаются вне Git; не новая редакция бизнес-ТЗ и не разрешение на массовую миграцию.

SMSPlace — текущий пилот универсального инструмента. Бизнес-знания, пути, GitLab и pri-fly в этом документе относятся только к пилоту: они не становятся defaults ядра, общего skill или будущих проектов. Преобразование бизнес-ТЗ под spec-first/ISO-ready оформляется отдельным изменением этой спецификации применения и самого бизнес-ТЗ; оно не выполняется автоматически при разработке spec-audit.

## Цель для менеджера

Вынести стоимость и служебный контекст аудита за пределы продукта, сохранив возможность понять, где бизнес-требование действительно подтверждено и где тест оставляет пробел. Инструмент не должен создавать ложные продуктовые задачи или требовать поддерживать второй ручной источник требований.

## Граница владения

| Объект | Где остаётся истина |
| --- | --- |
| Бизнес-требования, протоколы, деньги, уточнения | Существующий `/TZ` и объявленная в SMSPlace иерархия источников |
| Реализация и тесты | Репозиторий SMSPlace |
| Изменение кода | Рабочий процесс pri-fly; не функция spec-audit |
| Задачи продукта | GitLab проекта |
| Механика внешнего аудита | Репозиторий spec-audit и его [спецификация](tool-spec.md) |
| Конфиг, прогоны, snapshots, observations, HTML | `.spec-audit/` в SMSPlace, исключённая из Git; прежние внешние прогоны остаются историей |

Бизнес-код и `/TZ` остаются read-only. Отдельным решением 2026-09-15 владелец разрешил `.spec-audit/` внутри SMSPlace и подключение уже существующего skill для самостоятельного использования. Только при настройке разрешены записи локального конфига, ссылок на инструмент/skill и исключений Git; во время аудита записи ограничены этой игнорируемой рабочей областью. Планы AI Factory и новые CELL в продукте не создаются.

## Требования применения

### REQ-SMS-001 — Без служебных артефактов в истории продукта

Условие: выполняется аудит SMSPlace или разрешённая владельцем локальная настройка.

Требование: результаты аудита (`reports`, история, отчёты разделов) остаются в игнорируемой части `.spec-audit/` либо в явно выбранной внешней рабочей области и не попадают в Git. Код инструмента не переносится в бизнес-модули; второй launcher не создаётся. Редакция 2026-09-16 (решение владельца): установленная копия skill `.spec-audit/skill/` с receipt, относительные ссылки `.agents/skills/spec-audit` и `.claude/skills/spec-audit`, `.spec-audit/config.yaml` и `scopes/*/config.yaml` (с относительным `project_root`, без абсолютных путей хоста) трекаются в Git пилота — иначе они отсутствуют в изолированных worktree-сессиях агентов и `/spec-audit` не распознаётся. Обновление skill (`spec-audit skill update`) меняет tracked-файлы и фиксируется отдельным chore-коммитом.

Проверка: до/после аудита `git status` пуст; `git check-ignore` подтверждает игнорирование `.spec-audit/reports`, `.spec-audit/history` и `scopes/*/reports`; tracked-набор ограничен skill, ссылками и конфигами; CLI отклоняет пересечение исходников и reports_dir.

### REQ-SMS-002 — Без изменения бизнес-норм

Условие: аудитор обнаружил неоднозначность или неполное свидетельство.

Требование: результат должен сохраняться как вопрос/ограничение аудита, не изменяя `/TZ` и не превращаясь автоматически в GitLab issue.

Проверка: норму, кодовый дефект и недостаточную силу теста можно различить в отчёте; внешних записей в GitLab нет.

### REQ-SMS-003 — Ограниченный предмет сравнения

Условие: владелец разрешил прикладной пилот на SMSPlace.

Требование: пилот должен сравнивать старый и новый подход на одном зафиксированном небольшом scope с одинаковой редакцией требований и кода.

Проверка: записаны snapshot, точные границы scope, известные ограничения и сопоставимые результаты, без утверждения статистического выигрыша по одному запуску.

### REQ-SMS-004 — Явное принятие извлечённых норм

Условие: старое ТЗ не содержит объявленных норм в профиле v0.1.

Требование: структурные Markdown-фрагменты должны оставаться кандидатами до независимой проверки сохранения смысла и принятия владельцем.

Проверка: число AST-узлов/строк не выдаётся за полноту требований; сгенерированный индекс не становится самостоятельным ручным ТЗ.

### REQ-SMS-005 — Сохранение текущих гарантий

Условие: действует ограниченная граница применения, принятая владельцем 2026-09-15.

Требование: действующие trace/re-audit и CI-гейты SMSPlace должны оставаться без изменений.

Проверка: этот этап не удаляет маркеры, индексы, CELL, guards, jobs или проверки CI.

### REQ-SMS-006 — Проверенный PHP runtime

Условие: разрешён анализ PHP или отдельный тестовый запуск.

Требование: PHP должен исполняться только в фактически проверенном контейнере SMSPlace, а запуск тестов общей БД — последовательно после отдельного разрешения хоста.

Проверка: чужой checkout/mount отклонён; host PHP не используется; синтаксический SDK не выдаётся за runtime evidence.

## Последовательность принятия

1. Завершить срез инструмента на независимых примерах и собственной новой спецификации.
2. Выбрать один прикладной scope; согласовать атомизацию без потери условий, обязательности и исключений.
3. Выполнить внешний read-only аудит и сравнить с действующим процессом; отдельно измерить ложные findings и пропуски, не только скорость.
4. Только после принятия результата решать, что переносить/отключать в продукте. Миграция обратима и имеет отдельный план.

Решения владельца 2026-09-15: сначала принят внешний повседневный аудит выбранных разделов, затем разрешены локальная `.spec-audit/` и подключение существующего skill. Старые CI и trace сохраняются. Приёмка инструмента задана в [§12 основной спецификации](tool-spec.md#12-контракт-ближайшего-выпуска). Контроль REQ-SMS-003 выполнен с ограничениями в отдельном same-edition запуске ниже; прежний A/B представления Task его не заменяет. Изменения бизнес-кода и PHP-тесты по-прежнему требуют отдельного разрешения.

Полное отключение старого trace требует отдельного решения о каждой сохраняемой гарантии. Этот документ не объявляет старый CI или исторические артефакты ненужными и не разрешает их удаление.

## Контроль 2026-09-15

Предмет: External Identifier Mapping, Foundation §1.7.2B и выбранные уточнения FAQ, TechnicalDesign, Default API и Partner API. Это не аудит всего Foundation или публичных API. В `.local/sms-pilot/provenance.json` сохранены исходный commit, точные границы и хэши оригинальных/очищенных файлов. Копия включает пять фрагментов ТЗ, 16 файлов кода и пять файлов тестов; 18 строк trace-метаданных удалены только из комментариев копии с сохранением координат. Это контекстное разделение, не ОС-песочница. Хост и selector не объявляются слепыми участниками.

Независимый spec-only эталон зафиксирован до извлечения. Новый extractor получил только фрагменты ТЗ; автор эталона проверил raw после его фиксации. Его оценка: сохранены 13 нормативных групп, все 29 кандидатов поддержаны; усиленных, лишних норм и смысловых дублей не обнаружено. Это суждение reviewer, не машинное доказательство полноты. Формальный `TestExternalExtractionReview` не пройден: G14 попал в знаменатель как требование, хотя является ограничением знания и сохранён в raw.limitations. Ещё два возражения относятся к смешению нормы с отсутствием контракта повторных команд и с правилами самого эксперимента. Gold, raw и отрицательный результат сохранены без подгонки.

Хост отдельно принял 28 норм. C005 отклонён именно как самостоятельная обязанность реализации: пояснение чтения примеров не требует blacklist публичных строк; его исходный смысл остаётся в журнале и ТЗ. Это отличается от классификации reviewer, а не переписывает его результат. Два unresolved по выбору ветви нормализации сохранены как ambiguous. Связанные требования о namespace и исходном/нормализованном значениях передаются вместе: их отдельная проверка без соседних ограничений была бы неполной.

Все конфиги, raw, решения и отчёты остаются в `.local/sms-pilot/`, исключённом из Git. Runtime — none: ни PHP, ни Artisan, ни тесты общей БД не запускаются; код и assertions исследуются статически. Даже полная доставка ответов не означает подтверждённое исполнение или отсутствие дефектов.

Две свежие роли получили собственные задания по 28 принятым нормам и выбранную копию файлов, без gold, старых CELL и истории принятия. Оба исходных ответа приняты валидатором с первой попытки без правки. Удаление trace-метаданных не обеспечило полной слепоты: в обычных комментариях остались исторические пояснения. После явного согласования восьми разногласий хост оставил 18 норм со статическим подтверждением реализации, девять unknown и один конкретный статический контрпример, найденный обеими ролями. Это оценки ограниченного пакета, не сертификат продукта; исполнение всех тестовых свидетельств — not_recorded. Подробности, исходные оценки и основания согласования находятся в `.local/sms-pilot/reports/host-reconciliation.json`, JSON/HTML — в `reports/mapping-v1/` той же рабочей области.

Старый trace прочитан только после фиксации обоих raw. Его mapping-вердикты относятся к 30 июля, а точный audit snapshot не установлен; код и ТЗ с тех пор менялись. REQ-SMS-003 в части одинаковой редакции не выполнено: сравнение точности, стоимости и ускорения не состоялось. Найден конкретный случай, где старый PASS_HIGH на весь набор инвариантов подкреплён лишь assertion отдельного поля конфигурации; это предел данного свидетельства, не оценка всего старого подхода. Одновременно найдены два релевантных тестовых файла, отсутствовавших в новой выборке, включая прямую проверку enum. Поэтому missing в пакете не означает отсутствие проверки в проекте. Поздние находки сохранены отдельно, зафиксированный run не переписан; бизнес-задачи не создавались.

Измерены 189 922 байта выбранных файлов и около 264 КБ файлов вместе с заданием на каждую роль; два raw-ответа занимают около 294 КБ. Окно параллельного аудита — 747 секунд, включая диспетчеризацию, инструменты и ожидание результата хостом, не чистое время моделей. Usage, денежная стоимость и сопоставимый старый замер недоступны. Повторы цитат заметны, но сокращать их с потерей соседних условий нельзя.

По этим результатам выполнен следующий ограниченный контроль ниже. Массовый перенос ТЗ, увеличение лимита 64, graph/cache и отключение действующих гейтов не разрешены. Для runtime-подтверждения статических контрпримеров по-прежнему требуется отдельный разрешённый запуск в проверенном контейнере продукта.

## Контроль типизации и контекста, 2026-09-15

До кода заморожен отдельный [контракт с восемью сценариями](../acceptance/context-control.json). Его helpers находятся только в `tool/context_control_test.go`; production CLI, Result/submit и прежние baseline не менялись. В `.local/context-control/` подготовлены те же пять проекций ТЗ и полное разрешённое дерево tracked PHP для поиска: 2462 файла, с исходными/очищенными хэшами. 1888 строк metadata-комментариев заменены с сохранением координат; inline history остаётся возможным, полной слепоты и ОС-песочницы нет.

Новый spec-only gold создан до свежего извлечения. Получены 18 нормативных кандидатов и девять notes. Отдельный reviewer-учёт: 11/12 нормативных групп preserved, одна partial — архитектурный запрет остался только в context. Все 18 сформулированных кандидатов обоснованы. Пять групп notes сохранены, но дополнительное корректное пояснение отсутствует в frozen gold: одно gold objection, формальный unsupported не означает выдуманного содержания. `TestExternalContextReview` отказал; отрицательные исходы сохранены. Хост принял 18 кандидатов только для одинакового ограниченного сравнения, не объявляя полноту ТЗ.

Свежий selector самостоятельно нашёл вынесенные helper и test на синтетическом примере, затем искал по PHP-дереву. Первый результат сохранён; одна адресная доработка по enum/type/cast/schema добавила пять файлов с прямыми assertions, включая пропущенный enum-test. Общая выборка осталась 45 PHP-файлов: пять транзитивных зависимостей не включены, непросмотренные кандидаты и недоступные данные названы явно. Это refinement, не новый слепой проход и не доказательство полного покрытия.

`full-v2` и `compact-v2` получили одинаковые 18 норм, 50 файлов и snapshot. Перед dispatch проверено точное восстановление всего Task, включая исключения, unresolved, revision и источники. Два полных canonical Task занимают 168146 байт, два compact — 117214: меньше на 30,3% без смыслового сокращения. Разрешённые исходники занимают 647087 байт; стартовые PHP-диапазоны — 197078 из 628867 байт полных PHP-файлов. Это размеры доступного материала, не фактически прочитанные токены. Окно четырёх параллельных ролей — 1044 секунды с инструментами/диспетчеризацией/ожиданием. Usage, деньги и speedup недоступны.

Все четыре raw прошли неизменённый submit с первой попытки. В сопоставлении по роли specification/implementation совпали 36/36, assertion labels — 31/36; один запуск не доказывает семантическую эквивалентность или преимущество точности. JSON/HTML обоих run сохраняют оценки ролей, а отдельный `reports/host-comparison.json` — согласование каждой нормы. Хост оставил 13 реализаций supported в пределах контекста, три contradicted по двум независимым статическим сценариям и два unknown. Это не новые задачи продукта и не runtime-доказательства.

Особенно важен общий неподтверждённый вывод: четыре роли перенесли правило входящего public протокола на supplier response. После фиксации всех raw хост прочитал соседние нормативные разделы о направлениях Partner API и exact-key наблюдении; они сохранены отдельно в `reports/supplementary-context.json` с исходными хэшами и не подмешаны в первоначальный snapshot. Применимость этого запрета к выбранному пути не установлена: не объявлены ни product gap, ни доказанная корректность. Согласие ролей не компенсирует неполную выборку самого ТЗ.

Go test/vet, адресный race и native build проверены на macOS ARM64. PHP, Artisan и продуктовые тесты не исполнялись; execution остаётся not_recorded. Источники SMSPlace и старые baseline сохранены, бизнес-артефакты и issues не создавались. REQ-SMS-003 остаётся открытым: этот A/B сравнивает представления нового инструмента, а не старый re-audit на той же редакции.

Следующий малый контроль выполнен отдельно в [основной спецификации, A2–A3](tool-spec.md#124-конечная-приёмка-и-статус): роли различили request/response и full/partial assertion; извлечение сохранило обязанности отдельно от rationale и вопроса. Реализовано явное согласование хоста через review с историей и JSON/HTML. Положительный синтетический результат не переписывает отрицательный контроль этого бизнес-snapshot. Требовавшееся затем сравнение проведено отдельно ниже.

## Сравнение на одной редакции, 2026-09-15

В `.local/comparison-20260915/study.json` зафиксирован завершённый с ограничениями контроль Foundation §1.7.2B: бизнес-commit `f4ea779740472bdd1098b2e6b72ef0f455a770d7`, общий PHP-инвентарь 2499 файлов, snapshot новой ветки `69add983b530b2ece71c374f9160ebf8d6fc5f577dca5c46046ed4fa4e09b032`. Старой ветке доступны полное ТЗ и дополнительные не-PHP файлы, новой — выбранные смыслообразующие проекции и PHP. Это одинаковая редакция общих источников, но не одинаковый информационный бюджет.

Оба подхода нашли два статических контрпримера: несовпадение Unicode-нормализации записи и поиска; конфликт назначений внутри импорта словаря, скрытый успешным обновлением. Новый путь извлечение → принятие 16 норм → две роли → отдельный review → JSON/HTML завершён без ремонта raw. Итог хоста: 12 supported, 2 contradicted, 2 unknown; 15 weak assertions и один relevant. Исполнение продуктовых тестов — not_recorded; одна исходная разница ролей сохранена после согласования.

REQ-SMS-003 / A5 закрыты как ограниченный сравнительный запуск, не доказательство статистического превосходства. Цена и токены неизвестны; окна времени несопоставимы как speedup. Неисполненные тесты и диагностические ограничения старой ячейки не скрыты. Новые задачи и изменения SMSPlace не создавались; исходные отрицательные контроли сохранены. HTML-карта принята владельцем отдельным ручным просмотром.

## Чистая AI-сессия, 2026-09-16

Новая сессия Claude Code (Opus 5) в checkout `development`, без прежней памяти, через установленный launcher (`.claude/skills/spec-audit`, релиз 0.1.x): scope `02-finance` = `TZ/final_tz/02-finance.md`, run `finance-2026-09-16-01`. Извлечение одним свежим агентом дало 51 кандидата (153 цитаты); DECISION `accept-001`: 49 принято (REQ-AI-001…049, 5 ambiguous), 2 отложено — роль в процессах 1.8.x (определяющие разделы в модулях 07/11) и `FinanceSystem.md` как источник истины (документ вне scope из-за лимита 64 кандидатов на пакет). Обе роли доставлены с первой попытки; host review `host-review-001` current: implementation supported 49/49, assertion relevant 42 / weak 6 (007, 010, 017, 024, 031, 041) / missing 1 (029 — `price_source` компенсирующей `on_demand_buy_reversal` не тестируется); роли разошлись только по 009 и 041. `test`/`php-typed` не авторизованы.

Риски вне буквы норм, перепроверенные хостом по коду и зафиксированные в limitations решения (для владельца проекта, не автоматические задачи): запись `on_demand_buy_reversal` после коммита в `CancelActivation::confirmWithSupplierAfterOperatorCancel` только логирует ошибку без повтора; триггер append-only `supplier_cost_operations` пропускает DELETE при GUC `smsplace.allow_supplier_cost_history_reset='on'`; `AdminQaDefaultApiFixtureCommand --force` пишет balance напрямую без entries и в production.

Бизнес-код, ТЗ и CI не менялись; `git status` чист, tracked-набор ограничен skill/ссылками/конфигами. Инцидент: параллельно из другой сессии бинарник обновлялся 0.1.0→0.1.3, scope-конфиг переведён на относительный `project_root` (source_set и `fresh` сохранились). Уроки для инструмента — в ROADMAP spec-audit; независимый синтетический проект через тот же launcher ещё не пройден.

## Чистая AI-сессия, 2026-09-17: справочный источник и три scope

Вторая чистая сессия Claude Code в checkout `development` через launcher 0.1.5 после решения §20: scope-конфиг `02-finance` получил `references: {paths: [TZ/FinanceSystem.md]}`, прежний индекс стал stale. Новое извлечение — 51 кандидат / 243 цитаты, из них 50 в `FinanceSystem.md` только там, где норма на него отсылает; у всех кандидатов есть нормативная цитата, guard `reconcile` не срабатывал. DECISION `accept-002`: 36 revise, 4 split, 4 merge, 2 defer — rebind ни одного, потому что свежее извлечение переформулировало все кандидаты; 49 active (REQ-AI-001…062, 13 retired), revision у набора выросла до 2 без изменения смысла большинства норм. Ambiguous стало 8 вместо 5: справочник закрыл 2 нормы (039, 046), оставил 3 (027, 040, 049) и открыл 5 новых там, где смягчает безусловные формулировки §1.13.5 (045 блокировка кошельков, 048 admin_adjustment в минус, 052/053 состав purchase/refund-сценариев, 055 агрегируемая величина open_contingent) — это вопросы к владельцу ТЗ, а не дефект инструмента.

`index` при `scopes: []` вернул `advisories` (49 норм, 2512 файлов); хост разделил набор на три scope по подразделам ТЗ (economics 16 / supplier-cost 16 / ledger 17), повторный `index` — без предупреждения, `prepare` принят с первого раза. Run `finance-2026-09-17-01`: шесть ролей параллельно, все с первой попытки благодаря `validate`; по данным harness — 325–393 k токенов и 14–21 мин на роль, ≈2,16 M токенов суммарно, окно ролей ≈21 мин, окно prepare→review 29 мин (прошлый run: 507/593 k и 27/36 мин на одну роль при одном scope). Хост дополнительно потратил ≈20 мин на извлечение/DECISION и ≈7 мин на сборку host DECISION (711 KB). Host review current: supported 48 + contradicted 1 (REQ-AI-023 — на пути восстановления инцидента ключи `finance_snapshot` координатора и reconcile-executor не совпадают, сумма по умолчанию помечается как provider_response, зелёный тест закрепляет дефолт; код не менялся, прошлый run это пропустил), relevant 38 / weak 9 / missing 1 (REQ-AI-024 `occurred_at` — ни одного assertion) / contradicts 1; прежний missing по `price_source` вошёл в REQ-AI-060 как weak. Расхождения ролей — 6, хост разобрал каждое. Ограничения записаны в решении: PHP/PHPUnit не запускались, SDK не импортирован, собственные нормы `FinanceSystem.md` в этом run не аудировались, A-152 и модули 07/11 вне snapshot.

Уроки для инструмента (решение владельца, очередь — ROADMAP): нет dry-run для RAW/DECISION (хост снова писал зеркало `legacyValidate` и вычислял `content_hash` для выбора rebind/revise); после смены source_set сопоставление кандидат↔прежний ID делалось вручную по пересечению цитат; общий scratchpad параллельных ролей — одна роль выполнила одноимённый скрипт другой (результат совпал байт-в-байт, но правило «свой подкаталог на роль» нужно в launcher); host DECISION с полными цитатами тяжёл; формулировку «16–24 нормы» уточнить: граница подраздела важнее нижнего предела. Бизнес-код, ТЗ и CI не менялись; конфиг с тремя scope закоммичен в `development`.

## Чистая AI-сессия, 2026-09-17 (второй цикл): `check`, подсказки, изоляция ролей

Третья чистая сессия Claude Code через launcher 0.1.6 на обновлённом ТЗ (`development` ушёл вперёд: единственное смысловое изменение — разрез по роду поставщика/рынку и `source_stage→source_kind`; индекс был stale). Извлечение — 53 кандидата / 199 цитат (49 из `FinanceSystem.md`), 13 ambiguous. `check CONFIG RAW`: top-1 подсказки совпал с выбором хоста у 45 из 50 привязанных кандидатов (включая split/merge); 4 ложных top-1 — из-за строк, общих для многих кандидатов (заголовки таблиц и вводные строки считаются наравне с определяющими), в двух случаях верный ID оказался вне трёх подсказок; `fields_equal` false у всех 53 — свежий извлекатель переформулирует всё, rebind снова 0. DECISION `accept-003`: 45 revise, 2 split, 1 merge, 1 accept, 2 defer → 51 active (17 retired); `check CONFIG RAW DECISION` за одну итерацию предсказал `next_head` и все assignments, apply совпал полностью. Побочный эффект контракта: у 33 норм revision = 3 после двух переизвлечений без смены смысла, а ambiguous выросло с 8 до 12 (033, 039, 041, 063 — извлекатель вынес в unresolved то, что прежняя редакция держала оговорками; хост не может править clarity кандидата в DECISION, а reject осиротил бы ID).

Run `finance-2026-09-17-02`: три scope (16/18/17, §1.7.6A целиком в supplier-cost), шесть ролей параллельно в собственных `dispatch/<task_id>/`, все с первой попытки через `validate`; по данным harness — 336–427 k токенов и 19–25 мин на роль, extractor 137 k / 12 мин, приёмка extraction→apply ≈ 18 мин (в прошлый раз ≈ 20 мин с ручным зеркалом проверок), весь прогон ≈ 55 мин. Host review current после двух записей: первая содержала арифметическую ошибку хоста в summary (вердикты по нормам те же), append-only журнал потребовал новой записи с новым basis. Итог: supported 50 + contradicted 1 (REQ-AI-023 — тот же путь восстановления инцидента, код не менялся), relevant 41 / weak 8 (новые — 014 по новой части ТЗ и 063 по маппингу) / missing 1 (REQ-AI-024 `occurred_at`) / contradicts 1. Узкое место хоста — чтение 102 оценок и повторение чужих цитат в 823 KB host DECISION, не запись.

Уроки для инструмента (решение владельца, очередь — ROADMAP): rebind с сохранением прежнего принятого содержания (хост утверждает эквивалентность, revision и content hash не меняются) — иначе счётчик редакций меряет факт переизвлечения, а не смысл; веса подсказок — строки, общие для многих кандидатов/записей, взвешивать ниже или показывать уникальные общие строки; `index` при устаревших `scopes` не называет scope и ID; в dry-run с DECISION `freshness:"stale"` для пакета, который закрывает stale-ность, сбивает — нужен явный признак соответствия base_index; облегчённая форма host DECISION с машинной сверкой counts (ошибка хоста была бы поймана до записи). Бизнес-код, ТЗ и CI не менялись; scope-конфиг с перегруппированными `scopes` под новые ID не закоммичен — решение владельца.

## Чистая AI-сессия, 2026-09-17 (третий цикл): host DECISION version 2

Четвёртая чистая сессия Claude Code через launcher 0.1.8 — повтор scope 02-finance на текущем коде `development` после коммитов 17.09 (владелец выбрал повтор, а не новый scope); индекс accept-003 (51 норма) fresh, ТЗ по scope не менялось, приёмка/`reanchor`/`check` не понадобились. Run `finance-2026-09-17-03`: шесть ролей параллельно в собственных `dispatch/<task_id>/`, все приняты с первой попытки; по данным harness — 341–427 k токенов и 23–28 мин на роль (≈ 2,35 M токенов, wall clock ≈ 28 мин), результаты 116–207 KB; единственный отказ `validate` у роли — сдвиг диапазона цитаты на строку. Расхождения ролей — 4 из 51 (023, 035, 007, 041); хост верифицировал чтением кода только 023 и 035.

Решение хоста — первое живое `version: 2`: 40 KB против 823 KB в run 02, одна запись, `counts` (считались jq по verdicts) приняты с первого раза, `concur` both 50 / redteam 1 (REQ-AI-023 — решено в пользу redteam, цитаты mapper противоречие не покрывали); шаг 6 от последнего submit до `review_state: current` ≈ 4 мин против ≈ 15 мин и второй записи в run 02. Итог по нормам: supported 50 / contradicted 1 (REQ-AI-023 — третий прогон подряд при неизменном коде, инструмент воспроизводим) / missing 1 (REQ-AI-024); relevant 39 / weak 10 (новые — 031 и 035: по 035 mapper заметил фикстуры `owner_agent_id => null` у не-anchor assertions, redteam — нет; итог по assertion зависит от того, какая роль заметила вакуумную фикстуру) / ambiguous 12. Бизнес-код, ТЗ и CI не менялись; журнал и ledger — только чтение.

Что хост делал руками и чего не хватало (очередь — ROADMAP «Уроки четвёртого прогона»): нарезал `dispatch/<task_id>/{task.json,files.json}` из TaskBatch через jq (macOS `sed` испортил кириллицу в трёх промптах — исправлено до dispatch); собирал таблицу «норма × роль → spec/impl/assertion + расхождение» из 1,6 MB ответа `review` — самая дорогая операция шага 6; 39 statement на совпавшие нормы — шаблонная строка, содержательные только у 12 материальных норм; по 035 хост проверил строки фикстур, которых у ролей нет как Citation, — в v2 их негде записать, кроме полного v1; подпись «по обеим ролям» у вердикта weak, которого redteam не давал, читается двусмысленно; `index`/`reconcile`/`review` по 0,9–1,6 MB читались только jq-срезами; посторонний симлинк `.spec-audit/history/pricing-factor` вне CONFIG/reports_dir — SKILL.md не говорит, останавливает ли он аудит (хост трактовал узко и продолжил). Вне run: `spec-audit --help`/`--version` отклонялись как неизвестная команда; `skill update --dir .spec-audit` отвечал «каталог skill отсутствует», хотя ожидал корень проекта — оба и подпись «по обеим ролям» исправлены в 0.1.10 (§24.3–24.5).

## Чистая AI-сессия, 2026-09-17 (четвёртый цикл): `dispatch` из `prepare`, `outcomes[]`, `draft` — и возврат к version 1

Пятая чистая сессия Claude Code через launcher 0.1.10 на том же snapshot (`b72d1e18…`) и индексе accept-003, что run 03 — прогон на повторяемость ролей, не на новое свидетельство. Run `finance-2026-09-17-04`: `prepare` создал шесть `dispatch/<task_id>/` с `task.json`/`files.json` (529 KB × 6 идентичных копий — не мешает), хост положил туда только `prompt.md` (ASCII-замена run-id через `LC_ALL=C sed`); роли сами держали скрипты и `validate` в своих каталогах, чужие не трогали; все шесть приняты с первой попытки (18–27 мин, 0,34–0,46 M токенов, ≈2,3 M всего); отказов бинарника в run — ноль. `outcomes[]`: agree 48, расхождения 026/035/060; фильтр `agree==false or impl!=supported or assertion!=relevant` дал 12 норм из 51 — statements и цитаты хост читал только по ним через `entries`. Шаг 6 ≈ 7 мин, весь прогон ≈ 35 мин.

Решение записано в **version 1** (726 KB, 910 цитат). Причина — REQ-AI-023: обе роли в этом прогоне supported/relevant по штатному write-пути (mapper увидел incident-механизм, но по букве протокола оставил supported, назвав его только в limitations прозой), а вердикт хоста contradicted держится на строках координатора/исполнителя/теста, которых ни одна роль не цитирует; v2 (`concur: mapper|redteam|both`, без собственных цитат) записать это честно не может. `draft` использовался за review_id/basis/порядок норм; assessments v1 хост собрал скриптом как union цитат ролей. Итог по нормам совпал с run 03 по всем 51 только благодаря четырём решениям хоста против/между ролями (023, 026, 035, 060): на идентичном snapshot обе роли 023 не увидели — обнаружение дефекта здесь функция вариативности ролей, а хост без памяти прошлых решений записал бы 51/51 supported. Исключение 060 определено якорем A-204 вне `source_set` scope; redteam честно поставил contradicted с limitation, хост опроверг чтением вне snapshot и записал это в limitations. Стабильно weak во всех прогонах: 007/041/049/050/051/063 (структурные пробелы тестов); плавают 026/035/060 (оценочные суждения — граница исключения, косвенный assertion, вакуумная фикстура). Бизнес-код, ТЗ и CI не менялись; журнал и ledger — только чтение.

Уроки для инструмента (очередь — ROADMAP «Уроки пятого прогона»): собственные цитаты хоста в v2 (пункт 4 четвёртого прогона поднимается первым) либо `draft` для формы 1 с предзаполненными assessments из union ролей; `outcomes[]` без limitations ролей прячет ключевой сигнал; прошлый current host-вердикт по той же норме при том же snapshot/accepted head в контексте `review`; явное `freshness` в ответе `index`; подсказка на `check`/`index` о якорях ТЗ вне `source_set`+`references`; протокол mapper «противоречие в достижимой ветке = contradicted».

## Чистая AI-сессия, 2026-09-18 (пятый цикл): version 3, `previous_host`, протокол 0.1.13

Шестая чистая сессия Claude Code через launcher 0.1.13 на том же snapshot (`b72d1e18…`) и индексе accept-003, что run 03/04 — третий прогон подряд без изменения кода и ТЗ. Run `finance-2026-09-18-01`: `prepare` положил общий `dispatch/files.json` (529 KB, один на run) и `task.json` в шесть каталогов; хост генерировал только prompt.md; роли 6/6 с первой попытки (16–26 мин, 0,36–0,43 M токенов, ≈2,39 M всего; redteam стабильно на 4–8 мин и ≈40 k токенов дороже mapper того же scope); отказов бинарника — ноль (промежуточные отказы `validate` у ролей хосту не видны). `outcomes[]` с `limitations` и `previous_host` из run 04 дали триаж за минуту: 9 расхождений плюс одна согласная норма (035: обе роли relevant, прошлый хост weak) — её хост перепроверил только из-за `previous_host` и подтвердил вакуумные `owner_agent_id => null` фикстуры. Решение — **version 3**: 51 KB против 726 KB v1 в run 04, принято с первой записи, собственные цитаты хоста по 023 (executor/coordinator) и 035 (фикстуры тестов), больше нигде — правило «только там, где роли не цитировали» применялось буквально. Шаг 6 ≈ 6 мин от последнего submit, плюс 12–15 мин на сборку statements скриптом (34 согласные нормы — шаблон); весь прогон 36 мин.

Итог по нормам изменился при неизменном коде: 46 supported / 5 contradicted / relevant 35 / weak 10 / missing 1 (024). REQ-AI-023 — шестой прогон подряд, впервые redteam сам довёл incident-ветку с доводом достижимости; mapper не дошёл ни в одном из шести прогонов («подтверждающая» стойка останавливается на первом корректном резолвере). REQ-AI-038/044/068 — один корень: QA/demo-провижининг (artisan, HTTP QA-консоль) пишет `finance_accounts.balance` без операции и проводок, комментарий в коде сам признаёт нарушение §5.1, прецедент починки через `AdminAdjustmentService` уже есть; с run 03 прежние хосты выносили это как вопрос владельцу при supported — ledger-redteam применил правило 0.1.13 явно. REQ-AI-060 — новое чтение: сбой записи reversal после commit глотается без инцидента/повтора, зелёный тест закрепляет исход; mapper увидел ту же ветку и понизил до limitation при supported — нарушение протокола 0.1.13 mapper-ролью, видно только через `outcomes.limitations`. Роли различали границу: 025/040/043 (env-гейтированный teardown, недостижимый credit) остались supported с limitation. Хост записал в limitations решения: изменения вердиктов — изменение чтения и протокола, не дрейф кода; A-204 остаётся вне snapshot (подключить `00-overview-common.md` как reference при следующей приёмке); контекстное разделение ролей — инструкционное. Бизнес-код, ТЗ и CI не менялись; журнал и ledger — только чтение.

Уроки для инструмента (очередь — ROADMAP «Уроки шестого прогона»): dry-run решения (`check` для `review DECISION`) — хост пишет в append-only журнал вслепую; `previous_host` без statement прошлого решения — «почему weak» брали из памяти; разность цитат ролей по норме; шаблонные statement для согласных норм (пункт 5 четвёртого прогона); `advisories`/`sdk` как всегда присутствующие списки; шаблон prompt.md от `prepare`; журнал `validate` в каталоге задания; протокол mapper — прямое требование по веткам; `draft` с предзаполнением согласных вердиктов — владелец ранее отклонил (решение остаётся суждением хоста), хост просит снова. Продуктовые наблюдения — в решении хоста (023: три строки; 038/044/068: прецедент AdminAdjustmentService; 060: инцидент/повтор либо оговорка в ТЗ).

## Чистая AI-сессия, 2026-09-18 (шестой цикл): `validate … host`, пустые statement, mapper по веткам

Седьмая чистая сессия Claude Code через launcher 0.1.15 на том же snapshot (`b72d1e18…`) и индексе accept-003, что run 03–18-01 — четвёртый прогон подряд без изменения кода и ТЗ. Run `finance-2026-09-18-02`: роли 6/6 с первой попытки (22–28 мин, 0,38–0,43 M токенов, ≈2,37 M всего), отказов бинарника — ноль (встроенный `validate` у ролей тоже с первого раза). Хост: таблица `outcomes[]` → чтение statement/limitations обеих ролей по 15 нормам (12 расхождений + 023/060/035) скриптом по `review.json` → limitations всех согласных → перечитка кода по 035 → `draft` → сборка → `validate … host` (один раз, `valid:true`, `own_citations` 035/044/068) → `review` → `report`; 6 минут, потому что сборщик решения был готов до прихода ролей; весь прогон 36 мин; хост ≈ 0,56 M токенов.

Итог 46 supported / 5 contradicted (023, 038, 044, 060, 068) / relevant 35 / weak 10 / missing 1 — совпал с run 18-01 по всем 51 нормам. Под протоколом 0.1.15 (плюс та же фраза в промпте mapper) все три mapper сами поставили contradicted на 023/060 и 038/044/068: впервые за семь прогонов обе роли согласны по implementation по всем нормам; 12 расхождений — только по assertion (mapper weak «ветка сидеров тестом не покрыта» против redteam contradicts по зелёному тесту, который пинит balance без проводок). Хост взял contradicts на 038/044/068, добавив тест цитатой под 044/068 (redteam цитировал его только под 038), и применил правило «worst-wins» при одновременных relevant-тестах штатного пути и contradicts-тесте ветки — этого правила в контракте решения нет. Пустой statement — у 24 из 39 согласных норм; ещё 12 согласных получили короткий statement, потому что limitations ролей несли наблюдение владельцу, которое иначе осталось бы только в entries. `previous_host` решил 035 (роли relevant, прошлый хост weak → перечитка → weak с теми же цитатами хоста); `statement` прошлого решения хост с памятью не читал. Бизнес-код, ТЗ и CI не менялись; журнал и ledger — только чтение.

Уроки для инструмента (очередь — ROADMAP «Уроки седьмого прогона»): ответ `review … DECISION` асимметричен `validate … host` (нет `version`/`own_citations`); чтение по одной норме (`review --requirement`/`show`) вместо jq по 2 MB; «этот path:line цитируется ролью X под нормой Y» для решения о собственных цитатах; правило assertion при одновременных relevant и contradicts тестах — записать в контракт; генерация prompt.md бинарником из task.json + protocol.txt (дрейф протокол↔промпт делает вопрос «протокол или промпт» неизмеримым). Продукт: пять contradicted держатся на одном коде с 16.09 (новая улика — DemoProfileSeeder с balance без проводок); наблюдение economics-redteam про `agreed_supplier_payout_amount=null` для partner-rent — нормы вне snapshot. Закрыто в 0.1.16 (§29: `review … REQ-ID`, симметричный ответ, правило contradicts) и 0.1.17 (§30: `prompt.md`/`protocol.txt` от `prepare` — хост больше не копирует промпт прежнего run; `review … citations PATH`; счётчики в `tasks`).

## Чистая AI-сессия, 2026-09-18 (седьмой цикл): новый раздел `11-allocation-rent` с нуля

Восьмая чистая сессия Claude Code (Opus 5) через launcher 0.1.16 — первая приёмка нового раздела ТЗ с нуля по решению владельца: `TZ/final_tz/11-allocation-rent.md` (154 строки, 22,6 KB), новый scope `11-allocation-rent` с `references` по якорям раздела (`00-overview-common.md` — A-015…A-207, 220 KB; `04-pricing-quote-and-sale.md` — §15.3/16.2/16.5; `04-pricing-data-model.md` — §4.4.2/22.6; `02-finance.md`). Extractor (свежий субагент): 7 мин, 0,13 M токенов → 44 кандидата, 305 цитат, 9 ambiguous; хост сверил три сомнительных места с текстом (подтвердились), `check RAW` valid, advisories `[]`, `matches` `[]` (индекс пустой — по построению); решение accept ×44 (35 clear / 9 ambiguous), остатка нет; `check RAW DECISION` → `base_index_current`, REQ-AI-001…044; `reconcile` head `570d396d…`; `index` — fresh, advisory «>24» → два scope по 22 (`buy-rent` §1.6.4+§1.8.9, `period-extend-refund` §1.7.8+§1.8.10). Вся приёмка 13 мин (хост 4 мин после extractor). Подсказки, которым не нашлось работы: `matches` и `reanchor` (первый индекс), advisory о якорях (все якоря были в references заранее). Run `rent-2026-09-18-01`: `prepare` на 0.1.16, четыре роли по 22 нормы с промптами хоста (по шаблону прежних run), все 4 с первой попытки (18–24 мин, 0,34–0,42 M токенов, ≈1,48 M на роли); бинарник хоста обновлён до 0.1.19 между `prepare` и первым `submit` (журнал версий записал обе; хост записал в summary решения «0.1.16→0.1.17» со слов ведущей сессии — неточность в append-only журнале, вердиктов не касается). Отказов бинарника — ноль на 30 вызовов. Шаг 6: `outcomes[]` agree 36 / 8 расхождений (019, 021, 022, 029, 032, 034, 039, 044), `previous_host` нет (первый run на разделе); чтение 13 норм через `review … REQ-ID` (обёрнуто в show.sh — jq по roles/outcome), `citations <path>` подтвердил за один вызов, что собственные цитаты хоста не нужны; `draft` → version 3 → `validate … host` valid с первой попытки → `review DECISION` current; хост-шаг 6 мин, стена run 30 мин, весь заход 43 мин, ≈0,11 M токенов хоста.

Итог 39 supported / 5 contradicted (004, 005, 019, 032, 039) / relevant 30, weak 12, contradicts 1 (032), missing 1 (040); 9 ambiguous. Расхождения решены: 019/032/039 — contradicted (вывод redteam, `concur: both`), 021/022/029/034 — relevant, 044 — weak; 032 — contradicts по правилу §14 из 0.1.16 (зелёный `ExtendRentControllerTest` закрепляет отказ без cache row, тогда как `RentExtendPriceResolverTest`/`PricingV2RentExtendAcceptanceTest` доказывают продление без неё). Проверено ведущей сессией только чтением: `tool-versions.json` (prepare 0.1.16, 4×submit + review 0.1.19), `review … summary` (0.1.19) — current, agree 36, те же 8 ID, 44 нормы; `host-reviews.json` 44 KB; `index … summary` — fresh, 44 active, 2534 файла, advisories `[]`. Skill в пилоте обновлён до 0.1.19 и закоммичен вместе с конфигом scope локально (`e97cf4044` на `development`); push отложен — `origin/development` ушёл вперёд (26 файлов), а прежние skill-коммиты 0.1.11–0.1.16 на remote отсутствуют; по правилу рабочего цикла merge/rebase автоматически не делается, checkout остаётся на прежнем snapshot до следующего прогона.

Уроки для инструмента (ROADMAP «Уроки восьмого прогона»; (а)–(д) закрыты в 0.1.20, §33): (а) чтение нормы требовало jq-форматтера → `review … REQ-ID text`; (б) `citations` без пути — 861 строка без фильтра по норме/роли → фильтры; (в) extractor читает 220-KB reference выборочно, хост перечислял якоря руками → `anchors CONFIG`; (г) хост не может сузить statement принимаемого кандидата (C026 — overstated из reference): нужна операция «accept с редакцией» — контракт приёмки, решение владельца; (д) смена версии бинарника между `prepare` и `submit` без предупреждения → WARN; (е) хост пишет версию в summary руками и ошибается — журнал версий уже источник, в форму решения версию не добавляем; (ж) промпты ролей были хостовыми — сравнить с генерируемыми `prepare` в следующем заходе. Продуктовые наблюдения — в решении хоста (004/005: ядро rent не реализует A-015 «множество рынков до перебора»; 019: QA-консоль пишет rent allocation в finished; 032: операторский ExtendRentController требует cache row и делает её retail потолком; 039: renew без anchor теряет факт) и вопросы к ТЗ (demo-профиль как единственный rent-каталог; pristine на внешнем acquire; A-187 ↔ «без тихого дозавершения» (037); форма ledger refund_rent (043); источник суммы inventory_renew без цены (038); величина потолка ExtendRentCommand.maxPrice (033)) — владельцу.

## Чистая AI-сессия, 2026-09-18 (восьмой цикл): повтор `11-allocation-rent` с промптами от `prepare`

Девятая чистая сессия Claude Code через launcher 0.1.20 на том же snapshot (`b6f5b7f6…`) и индексе accept-001, что run rent-01 — прогон на сравнение промптов (урок ж восьмого прогона). Run `rent-2026-09-18-02`: `prepare` записал `dispatch/files.json`, `dispatch/protocol.txt` (побайтно равен протоколу skill) и по `task.json` + `prompt.md` на роль; ролям передан только путь к их `prompt.md`; все шесть записей журнала версий — 0.1.20. Роли 4/4 с первого `validate` (`validate.log` — по одному вызову, `valid: true`), все четыре брали цитаты через `spec-audit cite` из своих сборщиков; 18–23 мин, 0,36–0,42 M токенов — как в run 01; чужие каталоги и общий scratchpad не трогали. Шаг 6 инструментами 0.1.20: `review … summary` → agree 37 / 7 расхождений (011, 019, 022, 025, 028, 032, 037; пересечение с run 01 — 019, 022, 032), `previous_host` из run 01 по всем 44; `review … REQ-ID text` ×13 (show.sh не понадобился), `citations REQ-AI-039` показал, что no-anchor ветку под нормой не цитирует никто — хост сохранил contradicted с собственными цитатами через `cite`; `draft` → version 3 (18 пустых statement, собственные цитаты по 021/039/044) → `validate … host` valid с первой подачи → `review DECISION` current. Хост-шаг 5 мин, стена run 29 мин; отказов бинарника — ноль.

Итог 39 supported / 5 contradicted (004, 005, 019, 032, 039) — вердикт хоста по implementation совпал с run 01 на всех 44 нормах; assertion: 30 relevant / 12 weak / 1 contradicts / 1 missing (те же счётчики, четыре перестановки при неизменном коде: 025 и 027 weak→relevant — redteam нашёл тесты FK/миграции; 017 и 029 relevant→weak — «auto-resolve без auto-complete» и mock-идентичность провайдера не показаны тестами). Вариативность ролей: 039 — в run 01 redteam contradicted, в run 02 обе роли supported с limitation о той же ветке (случай «supported с limitation о другой ветке — сигнал» из SKILL.md сработал у хоста). Сравнение промптов: сгенерированный совпадает по структуре с хостовым, отличия — CITE, `validate.log`, PROTOCOL из dispatch, SDK_HINTS файлом, но короче в семи местах (test_id, имена запрещённых файлов, scratchpad, перечень веток у redteam, допустимые состояния, сводка счётчиками, подкаталоги) — закрыто в 0.1.22 (§35.1); просьба «text без списков цитат» — `review … REQ-ID brief` (§35.2). Проверено ведущей сессией только чтением: `tool-versions.json` (6 × 0.1.20), `review … summary` (current, agree 37, те же 7 ID), `own_citations` 021/039/044, `previous_host` у 44 норм, `validate.log` у четырёх ролей, `cite` в сборщиках, `dispatch/protocol.txt` = skill. Продуктовых новостей нет (те же пять contradicted); наблюдения ролей владельцу: DevRoutesGuardTest проверяет локальную копию предиката, ProviderBasePriceResolver читает рынок как Internal, а не через PricingMarketResolver (латентно, fallback-провайдер).

## Чистая AI-сессия, 2026-09-18 (девятый цикл): третий раздел `05-inventory` с нуля

Десятая чистая сессия Claude Code через launcher 0.1.22 — второй новый раздел с нуля: `TZ/final_tz/05-inventory.md` (145 строк, 12,6 KB; §1.6.2, §1.7.4, §1.7.5, §1.8.8), scope `05-inventory`, references `00-overview-common.md` и `02-finance.md` (§1.7.6A). `anchors CONFIG` до extraction: 11 якорей, `undefined: []`, по каждому два определения — пункт глоссария и строка таблицы покрытия (хост отбрасывал вторую руками; вложенные ссылки определений — A-070/A-101/A-191/A-212 — собирал сам); extractor получил только строки определений: 5 мин, 0,10 M токенов, 13 tool-uses (rent: 7 мин, 0,13 M, 24) и без limitation «reference прочитан выборочно» → 28 кандидатов, 102 цитаты, 7 ambiguous; `check RAW` valid, advisories `[]`; хост сверил расширения из reference (C019, C023, C028 — с цитатами); accept ×28 (21 clear / 7 ambiguous), остатка нет; `check RAW DECISION` → `base_index_current`; `reconcile` head `199a1805…`; `index … summary` fresh, 28 active, advisory «>24» → scopes `phone-identity-number` (001–021) / `quackr-topup` (022–028, подраздел не резался); `summary` печатал журнал приёмки целиком. Приёмка 7 мин. Run `inventory-2026-09-18-01` (`prepare` 0.1.22, семь правок промпта на месте): 4/4 роли с первого `validate` (у одной — две проверки, обе valid), все через `cite`; 12–18 мин, 0,28–0,40 M токенов, ≈1,36 M на роли; отказов бинарника — ноль на 24 вызова. Шаг 6 (5 мин): `review … summary` — agree 24 / 4 расхождения (001, 003, 014, 026); чтение через `brief` (шум — хвост `[host] решения нет` и `not_recorded` на каждый тест); собственных цитат нет; version 3 с первой подачи; `overview` по трём scope — 123 нормы, 10 run, 112 supported / 11 contradicted, 87 relevant / 28 weak / 6 contradicts / 2 missing, все три `decided` current.

Итог 27 supported / 1 contradicted (023) / relevant 22, weak 6 (003, 020, 021, 023, 024, 026), contradicts 0, missing 0; 7 ambiguous. Расхождения: 001/014 — relevant (mapper нашёл advisory-lock и relation-тесты), 003/026 — weak. Проверено ведущей сессией только чтением: `tool-versions.json` (6 × 0.1.22), `overview` по трём scope — те же итоги. Уроки для инструмента (ROADMAP «Уроки десятого прогона»; закрыты в 0.1.23, §37): `anchors` — `kind` определения (строка таблицы покрытия ≠ пункт глоссария) и `references` (якоря, названные самим определением); `index … summary` без журнала приёмки; `brief` без строк запусков; `overview.contradicted[]` (уже в §36.3); промпт 0.1.22 — править нечего. Продукт: REQ-AI-023 — после отказа по лимиту `BuyActivation` исключает Quackr и селектор идёт на fallback-рынок вопреки §1.8.8 — тот же корень, что rent 004/005 (правила A-015 нет ни в rent-, ни в activation-селекторе; один фикс закрывает три нормы двух scope; A-015 включить в citations 023 при следующей приёмке); вопросы к ТЗ — modem pool (003), редактируемость phone_identity_id (001), country override и incident при покупке (009), недостижимый per-number sync (010), нормализация legacy e164 (011), граница owned == max (023), стартовый статус докупленного (028), GUC-обход DELETE в SCO (019) — владельцу.

## Обычный рабочий цикл

1. До снимка проверить ветку и чистоту checkout. Если требуется разрешённое владельцем обновление, на чистой `development` подготовка сессии выполняет `git pull --ff-only origin development`; при расхождении веток остановиться, не создавать merge/rebase автоматически. Spec-audit сам Git не изменяет. Затем выбрать GitLab-задачу: прочитать исполнителя, комментарии и связанные merge requests, сверить исправление с checkout. Открытый issue может ждать QA; статус не доказывает ни дефект, ни исправность. Выбранный CONFIG задаёт точные нормативные границы, живой `project_root` SMSPlace, полные файлы ТЗ, код/тесты и изолированный `reports_dir` в разрешённой рабочей области.
2. Извлечь и принять ограниченный набор норм; `source_set` учитывает все выбранные spec-файлы, но принятый набор не выдаётся за полный индекс этих файлов. `scopes` распределяет все active ID, а не фильтрует их. Рекомендация для следующего цикла `02-finance` (tool-spec §20, релиз 0.1.5): добавить в scope-конфиг `references: {paths: [TZ/FinanceSystem.md]}` — документ хэшируется и цитируется для определений, но кандидаты извлекаются только из `02-finance.md`; после приёмки при `advisories` в `index` задать явные `scopes` по подразделам 1.7.x/1.8.x (≈16–24 нормы, граница подраздела важнее нижнего предела, все active ID ровно один раз). С релиза 0.1.6 перед `reconcile CONFIG RAW DECISION` выполняются `check CONFIG RAW` (проверки raw, `matches` — подсказки к прежним active ID) и `check CONFIG RAW DECISION` (предсказание ID и редакций без записи); каждая роль работает в собственном `reports/dispatch/<task_id>/`. С релиза 0.1.7 переприёмка после правки ТЗ идёт через `reanchor`: если подсказка (с `unique_shared` > 0) указывает на норму, чья принятая формулировка по-прежнему верна, ID, revision и содержание сохраняются, переезжают только цитаты; `revise` — только при смене смысла; после приёмки `index` сам называет устаревшие ID в `scopes`. Оба изменения делают текущий индекс scope stale и требуют новой полной приёмки в чистой сессии — отдельное разрешение владельца; run `finance-2026-09-16-01` остаётся историей. Один run соответствует одной редакции: обе роли, неизменённые raw и полный review с человеческим пояснением проблемы/влияния/следующего действия. Уже выполненная правка не требует искусственно восстанавливать дефект или ждать повторной разработки.
3. Findings с проблемой, влиянием, точными цитатами и предлагаемой проверкой передаются разработческому процессу pri-fly. По уточнению владельца 2026-09-15 spec-audit предназначен только для аудита: не меняет код/ТЗ и не запускает pri-fly. Исправление и его разрешения находятся за границей инструмента.
4. После изменения кода — новый run без переноса старых assessments/receipts. Если ТЗ не менялось, повторное принятие индекса не нужно; если изменился любой выбранный spec-файл, требуется полное явное reconcile/rebind/revise. Старый run остаётся историей. Тесты — только по REQ-SMS-006; без разрешения остаётся not_recorded.
5. Новый review и HTML показывают, что закрыто, что осталось weak/unknown и чем подтверждён запуск. С релиза 0.1.8 решение хоста записывается в форме `version: 2` (вердикт и `concur` на норму, `counts` сверяет бинарник) — без копирования цитат ролей; полная форма `version: 1` — только для собственных свидетельств хоста. С релиза 0.1.9 каталоги ролей `dispatch/<task_id>/` создаёт `prepare`, `review` отдаёт таблицу расхождений `outcomes[]`, а черновик решения печатает `draft CONFIG RUN_ID` — хост сохраняет его в `dispatch/host/` и заполняет. С релиза 0.1.11 черновик — `version: 3`: собственные цитаты хоста добавляются внутрь вердикта по норме, где они нужны (возврат к v1 не требуется); `outcomes[]` показывает limitations ролей и `previous_host` — прошлый вердикт хоста на том же snapshot и accepted head, только как подсказку. С релиза 0.1.12 список файлов лежит один раз в `dispatch/files.json`, `index` называет `freshness`, а `index`/`check`/`reconcile` подсказывают о якорях ТЗ (`§N.N`, `A-NNN`), не определённых в spec-файлах snapshot; случай 060 (якорь не назван в тексте нормы) подсказка не покрывает — читать смежные разделы по-прежнему задача хоста. С релиза 0.1.14 перед `review … DECISION` хост вызывает `validate CONFIG RUN_ID host DECISION` — те же проверки без записи; `outcomes[]` несёт statement прошлого решения и `only_here` — строки, которые цитирует только одна роль. С релиза 0.1.15 statement согласной нормы можно оставить пустым (бинарник подставит «Совпадает с оценками обеих ролей.»), а протокол mapper прямо требует contradicted при противоречии в достижимой ветке. С релиза 0.1.16 норму читают целиком командой `review CONFIG RUN_ID REQ-ID`, а правило «contradicts важнее relevant» записано в §14. Статус GitLab не меняется автоматически. Рабочая приёмка требует свежий отчёт по живому checkout, воспроизводимые команды и отсутствие изменений бизнес-исходников или служебных записей вне разрешённой рабочей области; она не означает готовность всего корпуса.

`fresh` означает совпадение снимка с локальным `project_root`, а не актуальность удалённой `development`. Во время run checkout не обновляют; обнаруженное внешнее изменение делает старый результат историческим и требует нового run. Снимок-копия также явно называется историческим, не живым.

Текущий проход по GitLab #130 ограничен аудитом по явному ответу владельца «Только аудит, бизнес-код пока не менять». PHP-тесты не разрешены, runtime остаётся none. Исправление уже выполнено другим разработчиком: задача назначена `garpik`, комментарий от 14 сентября указывает на влитый !1157 / `186bc8057` и ожидание QA. `opened` не означает, что нужно заново писать код; spec-audit проверяет готовую редакцию и не закрывает задачу автоматически.

### Аудит #130: старая и исправленная редакции

Из 12 кандидатов приняты восемь норм, четыре соседних отложены с причинами. Между двумя run ТЗ и принятый индекс не менялись; повторное извлечение не потребовалось. В обоих run исходные ответы приняты с первой попытки без правки, согласование хоста записано отдельно. Продуктовые тесты не запускались: execution — not_recorded.

`pricing-factor-v1` прочитал тогдашний локальный checkout `f4ea779740472bdd1098b2e6b72ef0f455a770d7` и подтвердил старую потерю factor между расчётом и записью: 4 supported / 3 contradicted / 1 unknown. Три нарушения относились к одной [#130](https://gitlab.com/smsplace/backend/-/work_items/130), не к трём новым задачам. При выборе задачи хост не учёл исполнителя и комментарий о уже влитой правке; это исправлено в рабочем порядке выше. После внешнего обновления checkout отчёт корректно стал stale / outdated; его raw и `host-review.json` не переписаны.

`pricing-factor-v2` проверил готовое исправление в checkout `a80ee51aad31e2461dcc575a17151e85388b4c34`, содержащем !1157 / `186bc8057`; snapshot `b453525b77458f459cde564cd9725af3335bd86214591fb6afe5b6c1c6ee887f`. Передача factor в state теперь есть, появились DB-assertions. Итог хоста: **5 supported / 2 contradicted / 1 ambiguous/unknown**, assertions — 7 weak / 1 missing. Один спор ролей разрешён с явным обоснованием, не усреднением. Отчёт fresh / review current на момент выпуска; сверены 2513 файлов, 116 цитат, 2668 уникальных якорей и 3119 ссылок без разрывов.

Остались два статических контрпримера минимальной цены: при old=100, выбранном factor=0.92, floor=100 сохраняется 1 вместо 0.92; при old=1, factor=0.92, floor=2 сохраняется 0.92 вместо единицы для floor-only. Подробные условия, цитаты и необходимые сквозные DB-регрессии — в `host-review-v2.json` и новом HTML. Это результат для QA существующей #130, не выполненное тестовое воспроизведение и не новые issues. No-demand остаётся вопросом к ТЗ, не объявлен дефектом. Код, GitLab и старые CI/trace хост не изменял; pri-fly и pull/merge не запускал.

Оба JSON/HTML сохранены под `.local/daily-smsplace/pricing-factor-reports/<run-id>/`. Исходный отрицательный вывод не выдаётся за состояние исправленного checkout; следующий run нужен только при новой редакции или новых принятых свидетельствах.

### Локальный запуск выбранного раздела

Рабочий каталог команд — checkout spec-audit. Для #130 используется `.local/daily-smsplace/pricing-factor.yaml`: `project_root` указывает на живой SMSPlace, `reports_dir` — на внешний `.local/daily-smsplace/pricing-factor-reports`, `runtime.kind=none`. Пять полных spec-файлов доступны как контекст; приняты восемь норм, не весь pricing-корпус. Полные файлы нужны и для проверки свежести: даже правка вне выбранного абзаца делает принятый индекс stale.

```bash
./bin/spec-audit status .local/daily-smsplace/pricing-factor.yaml pricing-factor-v2
./bin/spec-audit report .local/daily-smsplace/pricing-factor.yaml pricing-factor-v2
```

Для прежних внешних прогонов хост читает `skills/spec-audit/SKILL.md`, передавая ему явные абсолютные пути бинарника и CONFIG. CONFIG, raw, принятый индекс и отчёты в `.local` не входят в Git: одно клонирование инструмента их не восстанавливает. Новая локальная установка описана ниже; исторические конфиги и raw не перемещаются и не переписываются.

После отдельного изменения кода через pri-fly хост сначала обновляет status/report старого run. Если выбранное ТЗ не менялось, продолжение использует прежний принятый индекс:

```bash
./bin/spec-audit index .local/daily-smsplace/pricing-factor.yaml
./bin/spec-audit prepare .local/daily-smsplace/pricing-factor.yaml pricing-factor-v3
```

`pricing-factor-v3` — пример нового ещё не занятого ID, не выполненный сейчас проход. Далее нужны свежие mapper/redteam, submit обоих raw, отдельный review и report по протоколу лаунчера; один prepare не является повторным аудитом. Если изменилось ТЗ, сначала полный reconcile. Старые ответы/receipts не копируются, stale нельзя обойти retry или заменой hash.

### Самостоятельное использование из SMSPlace

Бинарник установлен релизным `install.sh` в `~/.local/bin/spec-audit` вне бизнес-проекта и обновляется `spec-audit update`. Skill встроен в бинарник: с разрешения владельца 2026-09-16 `spec-audit skill install --dir <checkout> --host both --replace` заменил прежний symlink на checkout инструмента реальным каталогом `.spec-audit/skill/` с receipt; ссылки `.agents/skills/spec-audit` и `.claude/skills/spec-audit` остались относительными (`kept`). Зависимость от checkout spec-audit снята; повторный `skill update` — `updated:false`. Первая новая сессия Claude Code в пилоте не увидела `/spec-audit`: desktop-сессии работают в изолированном worktree, где игнорируемых файлов нет. Решение владельца 2026-09-16 — трекать skill, ссылки и конфиг (коммит `chore` в `development`); отчёты и история остаются игнорируемыми. `init CONFIG` по-прежнему создаёт только YAML; установка skill — отдельная явная команда ([§18](tool-spec.md#18-поставка-версия-обновление-и-установка-skill)). Бизнес-код, ТЗ и CI не менялись; проверка из новой AI-сессии — следующий этап.

В `.spec-audit/config.yaml` заданы `/TZ`, PHP-код и тесты, `runtime.kind=none`. Это карта расположения источников, а не принятый полный индекс. Для каждого запрошенного раздела skill использует `.spec-audit/scopes/<scope>/config.yaml` с необходимыми нормативными источниками; индекс, raw, решения и HTML живут в соседнем `reports/`. Новая область не переиспользует принятый индекс другой области. Лимит 64 активных требований остаётся; составной запрос делится явно, без заявления полноты всего ТЗ.

Из корня SMSPlace проверка настройки без ИИ и без записи:

```bash
spec-audit reconcile .spec-audit/config.yaml
```

Первый ответ `freshness=uninitialized` означает, что нормы ещё не извлечены и не приняты, а не что ТЗ пустое. Смысловой запуск в сессии: `$spec-audit проверь раздел TZ/final_tz/04-pricing-engine.md` в Codex или `/spec-audit проверь раздел TZ/final_tz/04-pricing-engine.md` в Claude Code. Хост читает необходимые соседние нормы, принимает ограниченный набор, запускает независимые роли и делает review; бизнес-код и GitLab не меняются.

Для доступа к предыдущему отчёту из той же папки разрешена ссылка `.spec-audit/history/pricing-factor` на прежний внешний каталог: это история, не новый аудит и не перенос raw. Ни конфиг, ни отчёты, ни локальные ссылки не восстанавливаются клонированием Git. Полное `/TZ` и `.spec-audit` не передаются субагентам вместе: каждая роль получает только свой Task и разрешённые исходники.

HTML с графиками и настройками принят владельцем ручным просмотром 2026-09-15. Это приёмка интерфейса в его среде, не проверка каждого редактора на всех ОС.

## Обкатка PHP/Laravel SDK

Запланированное расширение задано в [§17 спецификации инструмента](tool-spec.md#17-php-и-laravel-sdk); оно предназначено для Laravel-проектов вообще. Для SMSPlace ожидаемая польза — точнее находить реализацию за сервисами/интерфейсами, Eloquent и связанный тестовый контекст, не добавляя бизнес-правила в SDK.

На 2026-09-16 в `composer.json` SMSPlace уже объявлен Larastan, а `phpstan.neon.dist` подключает его, baseline и собственные проектные правила. Это существующая проверка бизнеса, **не интеграция со spec-audit**. Рабочий `php-facts` использует php-parser, а локальный CONFIG по-прежнему имеет `runtime.kind=none`.

Порядок пилота: сначала независимые PHP/Laravel-примеры инструмента, затем согласованный небольшой scope SMSPlace на одной редакции ТЗ/кода, с SDK и без него. Перед запуском отдельно разрешить Larastan bootstrap и проверить Docker mount/изоляцию. Учесть фактические includes, stubs и локальные overrides анализатора; его штатный `tmpDir: storage/framework/cache/phpstan` не использовать для служебных записей аудита. Временный конфиг/кеш/выгрузка — только в разрешённой audit-области. Composer, CI, baseline и проектные PHPStan rules не правятся; при несовместимой среде нужен отказ и отдельное решение, а не обход guards. Рабочие БД/очереди/сервисы при bootstrap не используются.

Связи SDK служат подсказками, не автоматической заменой `/TZ`, `@tz`, тестов или прежних CI-гарантий. Выполнение этого плана не разрешает менять бизнес-код и не означает, что старый trace уже можно удалить.

## См. также

- [Спецификация инструмента](tool-spec.md)
- [README](../README.md)
